# 《山驛每旬總有一批封繩對不上》

## 劇本規格頭
- 劇本名稱：《山驛每旬總有一批封繩對不上》
- `script_id`: `ootb-repeatable-mountain-post-seal-cord-001`
- 劇本版本：1.0.0
- 運行類別：可重覆任務
- `replay_policy`: `repeatable`
- 連續性：`independent`
- 重跑基型：兩者（日常可再發生＋小型分支）
- 冒險分類：一般冒險
- 劇本規模：小型（S=1）
- 建議玩家人數：2–4 名 PC
- 建議角色等級：6–10 級
- 歷練基準等級 R：8
- 預計遊戲時間：2–3 小時
- 主要地域：巴蜀道山路驛運網中的一處本輪新驛站
- 可能社會名譽：俠名、惡名；【巴蜀道民間】
- 需要引用：`內容庫/拿手.md`【識字】【格物推理】【洞察】【生存】【說服】
- 拿手判定權威：`遊玩規則/核心/拿手與非戰鬥.md`

## 每個 game_id 的實例化
在玩家接觸第一條有意義線索前完成並保存：
1. 鎖定 `variant_id`：`cord-miscount` 或 `cord-swapped-load`；不得因玩家猜測切換。
2. 生成 `<LOC#1: 名?-驛>` 的 `名?`；新 `game_id` 重生成，同局續跑保留。
3. 生成全部 NPC 姓名：`<NPC#1: 姓?-名?>` 驛丞、`<NPC#2: 姓?-名?>` 押運腳夫、`<NPC#3: 姓?-名?>` 收貨人。新局重生成；同局保存；不同局同一 NPC# 只是功能槽位。
4. 生成 `<PROP#1>`「問題封繩」外觀：麻黃／灰麻／棕麻三選一；功能固定為本輪問題貨批的封繩。
5. 生成 `<CLUE#1>`「封結差異」呈現：少一轉／結尾反向／尾端長短異常三選一；只改表象，不改證據力度。
6. 玩家可見帳頁、標牌與口供全部使用本局 LOC／NPC／PROP／CLUE 映射。
若合法上下文已知上一局名稱／外觀，本局避開即可；不得越權搜尋未載入舊存檔。

## 世界內可重覆原因
山驛每旬處理不同商戶與不同腳夫的普通貨批；封繩、交接與夜間臨時堆貨錯誤可自然再次發生。每個 `game_id` 是另一宗貨批事件，不是上一宗倒帶；人物、驛站專名與關鍵封繩均為新世界實體。

## 變體
### `variant_id: cord-miscount`
`<NPC#2: 姓?-名?>` 夜裡把兩捆同規格貨先拆一捆查潮，重封時少繞一轉，怕扣工錢便沒報；貨物未被偷換。必要來源：`<PROP#1>` 原貨繩纖維連續、帳重前後一致、`<NPC#2: 姓?-名?>` 手上同類麻屑。

### `variant_id: cord-swapped-load`
雨夜臨時挪貨時，`<NPC#3: 姓?-名?>` 的夥計把兩批外觀近似貨換了堆位；`<NPC#2: 姓?-名?>` 按錯堆重新封繩，造成封結與帳頁對不上，但沒有偷貨。必要來源：地面拖痕交叉、兩批重量與帳頁各自對得上但位置相反、`<NPC#3: 姓?-名?>` 知道昨夜曾叫人挪棚。

每個變體內，問題封繩、帳頁／重量與人物／拖痕至少兩路可互證；不屬本次變體的決定性線索不生成。

## 出發前整備
- 整備類型：一般。
- 安全空檔：24小時（48個空檔回合）。
- 開始位置：本輪 `<LOC#1: 名?-驛>` 前一站客舍；角色前一日已接到次日普通貨交接覆核差事。
- 可正常取得：普通食水、乾糧、繩索、紙墨與常見工具。
- 已知限制：路程不足半日，沒有已知敵對武人；事件的具體封繩異常要到次日驗貨才會出現。
- 整備期間危機：無；兩時辰倒數於次日驗貨時開始。
- 是否可快速略過逐項確認：可以；不生成未持有物。
- 出發前交接：逐名固定隨身、載具與留存。

## 開場與人物
本輪 `<LOC#1: 名?-驛>` 有一批貨的 `<PROP#1>` 與帳頁記載不一致。驛丞 `<NPC#1: 姓?-名?>` 暫扣貨批兩個時辰，之後若仍說不清就按「疑似調包」報官。

- `<NPC#1: 姓?-名?>` 知道貨重與帳頁都在，最怕把普通失誤誤報成刑案；若角色【巴蜀道民間】相對名譽為正且身份可確認，會主動提供上一班交接頁；為負則需正式申請並耗10分鐘。
- `<NPC#2: 姓?-名?>` 知道自己重封過貨，怕扣工錢而隱瞞；取得客觀封結／重量矛盾後願完整說明。
- `<NPC#3: 姓?-名?>` 在 `cord-swapped-load` 知道昨夜叫人移過棚位；在 `cord-miscount` 只知道正常收貨，不知道重封細節。

## 核心線索與方法
- `<PROP#1>` 與正常封繩可直接並排；`<CLUE#1>` 是本輪可見差異。
- 智力（格物推理）DC13，10分鐘：成功辨出 `<CLUE#1>` 是重封造成而非割斷再接；失敗只耗時，取得另一段原繩或實際重做一次封結後可重試。
- 智力（識字）DC13，10分鐘：比對帳頁重量／堆位；成功快速指出哪兩欄互相矛盾；失敗不抹除數字，可請 `<NPC#1: 姓?-名?>` 逐筆朗讀核對，耗20分鐘。
- 直覺（洞察）DC13：與 `<NPC#2: 姓?-名?>` 或 `<NPC#3: 姓?-名?>` 交談，成功看出對方隱瞞的是怕被追責的操作失誤，只證明有所保留；失敗後須有新物證才能重試。
- 直覺（生存）DC13：查看棚地拖痕；只在 `cord-swapped-load` 提供「兩批曾交換位置」的深入資訊；在 `cord-miscount` 中只確認正常搬貨，不生成錯誤決定性線索。
- 魅力（說服）DC13，10分鐘：以「先確認貨物是否短少，再分責任」勸涉事者補完整口供；失敗後須先拿到一項物證矛盾才可重試。
- 可直接成立：重新稱重、重新示範封結、把兩批貨按帳頁逐件對位；這些方法較慢但不要求骰。

## 危機
0–2格，每格一時辰。2格時 `<NPC#1: 姓?-名?>` 封存貨批報官；玩家仍可繼續查，但當日交接失敗。任何調查失敗不會憑空令貨物短少。

## 終局與結算
`E` = 角色等級折算後、個人額外歷練加成之前的實得基礎總歷練。

### `ending_id: cord-cleared`｜顯示：封繩說清了｜完成結局
查清本輪變體真相，確認貨物無實質短少並完成交接。實際參與核對封繩、帳頁、人物或貨物任一核心工作者取得完整歷練。
- 歷練：計略4、江湖3、行旅3；基礎10，R8。
- 銀兩：整隊15兩實際取得，`shared_asset_id: cord-reward-15`，共同持有、可分割，由玩家決定分配。
- 名譽：俠名`+0.1x E`；惡名`0.0x E`；【巴蜀道民間】`+0.2x E`。

### `ending_id: cord-safe-hold`｜顯示：先封存，沒冤人｜完成結局
未完全查清，但證明不足以指控偷換，安全封存等下一班覆核。
- 歷練：基礎10，R8。
- 銀兩：10兩實際取得，整隊共同持有、玩家決定分配。
- 名譽：俠名`+0.1x E`；惡名`0.0x E`；【巴蜀道民間】`+0.1x E`。

### `ending_id: cord-false-accusation`｜顯示：貨沒少，冤名留下｜失敗結局
玩家在缺乏證據時公開毆打、勒索或強迫普通涉事者認罪。
- 歷練：計略3、江湖4、行旅3；基礎10。
- 銀兩：0。
- 名譽：俠名`0.0x E`；惡名`+0.2x E`；【巴蜀道民間】`-0.3x E`，只對可可靠歸因者成立。

### `ending_id: cord-leave`｜顯示：貨留驛中｜任務放棄
實質完成一項核心核對者基礎3；未介入0；無酬金；名譽全0。已封存或已重新稱重的世界狀態不倒帶。

## 重覆獎勵上限
- 歷練：同一角色第1、2次合資格完整完成可正常取得；第3次起本 `script_id` 基礎歷練為0。
- 銀兩：每個新事件的世界內工錢仍存在；同一角色前3次可參與玩家分配，第4次起其個人不得從本劇本固定酬金取得持久財產，未分配份額留給其他合資格角色或由委託方收回。
- 俠名：最多前2次非零；第3次起固定`0.0x E`。
- 惡名：每次若本局確有新的公開可歸因粗暴事件，照常結算。
- 【巴蜀道民間】正向：最多前3次非零；第4次起正向`0.0x E`；負向只在新事件可可靠歸因時照常。
- 無武學、永久特殊裝備、門派資格或跨劇本前置獎勵。

## 偏離與實例隔離
玩家可直接稱重、重做封結、請第三方覆核而不擲骰；若毀掉全部物證，仍可安全封存但不能生成新證據。任何固定世界人物不得作本輪必要 NPC。新 `game_id` 不承接上一事件的 NPC、地點、燒毀、仇怨、貨批或事故狀態；同一 `game_id` 暫停／續跑則必須保留本局全部映射、物件持有與已公開資訊。
