# 九筐茶磚有一筐總在驗封後少兩角

## 劇本規格
- `script_id`: `ootb-repeat-nine-tea-baskets-missing-corners`
- 劇本版本：1.0.1
- 運行類別：可重覆任務；`replay_policy`: `repeatable`；重跑基型：日常可再發生＋小型分支
- 連續性：`independent`；冒險分類：一般冒險；類型：商貨／調查／追截
- 建議角色等級：11–15級；歷練基準R13；小型；3–5人；3–4小時
- 社會名譽：【本道商旅】、【本道官府】；內容庫：`內容庫/拿手.md`、`內容庫/敵人圖鑑.md` 的【鏢師】

## 實例化與重跑
新 `game_id` 在第一條有意義線索前鎖定且全局固定 `variant_id`，**不得因玩家猜中、猜錯、改路或跳場景切換**：`sample_theft`（切角取樣私售）／`seal_swap`（以劣茶替換）。重生成 `<NPC#1: 姓?-名?>` 茶商、`<NPC#2: 姓?-名?>` 驗貨人、`<NPC#3: 姓?-名?>` 腳夫頭、`<NPC#4: 姓?-名?>` 接貨者、`<LOC#1: 名?-茶棧>`、`<LOC#2: 名?-水門>`；同局保存映射，合法已知上一局時避開，不搜尋未載入 save。
- `<PROP#1>` 異常茶筐：核心貨物；不可變因果＝驗封後被再次開動；形態池＝竹筐／藤筐／木條筐。
- `<PROP#2>` 封籤：證明封存時點；不可變因果＝留下重穿或移貼；形態池＝紙籤／木牌／麻繩鉛扣。
- `<CLUE#1>` 茶屑路徑：指向水門；不可變因果＝責任者搬運時遺落；形態池＝碎屑／茶粉／包紙纖維。
- `<CLUE#2>` 驗貨簿：核對原數量品質；不可變因果＝封前九筐完整；形態池＝線裝簿／散頁夾／掛牌抄簿。
形態不得改真相、責任者、DC、證據力度或結局；同局保存持有、破壞、轉移、公開狀態。每局為新貨批；現銀每局一次；兩項正向相對名譽各最多3次。

## 出發前整備
一般整備；安全空檔24小時（48空檔回合）。開始於本道商鎮，可購食水、乾糧、繩索、燈油、普通旅具並租坐騎。翌日酉時驗封，子時前核清。逐角色記攜帶格上限／實際隨身物／已用格；逐載具記承載格上限／實際載物／已用格；記留存位置。

## GM真相、人物與時間線
`sample_theft`：`<NPC#3>` 收 `<NPC#4>` 銀錢，驗封後切取名茶磚角作黑市樣品；失誤是茶屑與封籤移位。`seal_swap`：`<NPC#2>` 以劣茶替換兩角上等磚，準備由 `<NPC#4>` 帶走真貨。`<NPC#1>` 重商譽，證據成鏈後願封棧。`<NPC#4>` 固定使用`內容庫/敵人圖鑑.md`的【鏢師】完整條目，不加模板、不替換武學或數值；求脫身而非死戰。正向【本道官府】可令巡檢提前見證；高惡名令茶商要求第三方在場，均不直接證明真相。

## 必要結論與冗餘
1. 驗封後仍被動過：`<PROP#2>`、`<CLUE#1>`、值守口供。
2. 缺角成因：切面／劣茶物證、`<CLUE#2>`、包材痕跡。
3. 接貨方向：茶屑、門簿、`<NPC#4>` 行動。全部來源永久失去且未保存交叉資料，進 `tea_unresolved`。

## 場景與判定
`<LOC#1>` 茶棧：合理查看自動發現缺角。智力（格物推理）DC18，20分鐘，辨切取或替換；失敗後取得另一完整磚或老茶師見證才可重試。智力（識字）DC16核簿，失敗後找到原收貨單才可重試。正式拿手依 `內容庫/拿手.md`、`遊玩規則/核心/拿手與非戰鬥.md`。

後巷：直覺（感應）DC17追 `<CLUE#1>`，10分鐘；失敗使接貨者多20分鐘，新增目擊或第二段痕跡後可重試。正式輕功可走屋脊捷徑，無者繞行多10分鐘。

`<LOC#2>` 水門：魅力（說服）DC18以減責換貨位；失敗後須新增實證、封路或抓同夥才可重試。`<NPC#4>` 低於四分之一氣血且退路被封即投降；奪貨、封船或取得可立即追索位置均為非擊殺勝利。

## 危機與偏離
酉時驗封；+1小時取貨；+2小時交接；+3小時離鎮。提前猜中可守棧或水門，定責仍需兩路資料。責任者死亡不抹物證。放棄則貨物流走。

## 終局與結算
完整參與＝至少兩主要場景或一主要場景＋追截；中途加入每少一場景少1點相應基礎歷練，最低2點。`E`＝等級折算後、個人加成前實得基礎總歷練。
### `tea_recovered`｜茶貨歸棧｜成功
至少兩路資料成立且真貨追回。歷練：江湖4、計略3、行旅2、武林2＝11。整隊20兩實得、玩家分配。合法見證：俠名 `+0.2×E`、【本道官府】`+0.2×E`；公開保全無辜腳夫則【本道商旅】另 `+0.2×E`。
### `tea_truth_only`｜茶簿成案｜部分成功
責任成立但貨已離鎮。歷練：江湖4、計略4、行旅1＝9；整隊12兩實得；【本道官府】`+0.1×E`，其餘0。
### `tea_unresolved`｜封籤無證｜失敗
歷練：江湖3、行旅2＝5；無尾款。通常名譽0；蓄意毀證且身份可靠傳開則惡名 `+0.2×E`、【本道官府】`-0.2×E`。