# 渡倉每旬總有一箱麻繩少兩丈

## 劇本規格
- `script_id`: `ootb_repeatable_ferrywarehouse_rope_shortage`
- 劇本版本：1.0.1
- 冒險分類：一般冒險
- 運行類別：可重覆任務
- `replay_policy`: `repeatable`
- 連續性：`independent`
- 劇本規模：小型
- 重跑基型：兩者
- 建議人數：3–5；建議等級：6–9 級；歷練基準：R=8
- 預計時間：2–3 小時
- 地區：荊湖道普通渡運聚落
- 可能的社會名譽變化：俠名、惡名、【荊湖道民間】
- 需要引用的內容庫條目：`內容庫/拿手.md`、`內容庫/相對名譽對象.md`

## 新局實例化
每個新 `game_id` 是另一宗渡倉短料事件。先鎖定 `variant_id`，再生成 `<LOC#1: 名?-渡>`、`<LOC#2: 名?-倉>` 與全體 NPC；同局續跑保持映射。已知上一實例名稱時避開重用，不搜尋未載入舊存檔。不同實例同編號只是功能槽位。

- `variant_id: wet_cut`：麻繩受潮，管倉人剪去最爛兩丈，再以乾廢麻補重量。
- `variant_id: ferry_borrow`：夜班渡工為修臨時渡纜借走兩丈，換班時未完成入帳。

第一條有意義線索前鎖定變體，不得按玩家猜測更換。
- `<NPC#1: 姓?-名?>`：管倉人；`<NPC#2: 姓?-名?>`：夜班渡工；`<NPC#3: 姓?-名?>`：收貨腳夫領班。
- `<PROP#1>`：短料麻繩箱；固定功能為帳面長度與實長不符。木色、繩捆顏色、普通批號每局重生成，不改證據力度。
- `<CLUE#1>`：作業痕跡；`wet_cut` 為潮斑＋新切口，`ferry_borrow` 為臨時纜結＋未完成借條。次要載體外觀每局重生成。
所有玩家可見帳頁、口供、標籤跟隨本局映射；上局毀損、人物、地點、物件與結局不帶入。

### 防刷
同一角色首次與第二次完成本 `script_id` 可正常取得本篇歷練與現銀分配；第三次起基礎歷練與正向現銀為 0，但仍可承擔世界後果。正向【荊湖道民間】由本篇累計最多 `+0.6x E`，俠名最多結算兩次；惡名與負面相對名譽每局按實際可歸因後果結算，不封頂。

## 世界真相
### wet_cut
三日前 `<NPC#1>` 發現 `<PROP#1>` 因漏瓦受潮，怕整箱報廢自賠，剪掉兩丈爛繩，以乾廢麻補重量。`<NPC#3>` 收貨只秤重未量長。今晚 `<NPC#1>` 會換掉濕墊草；即使如此，新切口、漏瓦與收貨記錄仍可重建真相。

### ferry_borrow
前夜渡纜磨裂，`<NPC#2>` 從 `<PROP#1>` 借兩丈修纜，寫了半張借條，卻因救一艘走錨小船而未完成交接。借條留在渡頭工具匣，倉簿未記。`<NPC#1>` 不知此事，誤疑腳夫偷拿。兩日後舊纜換下、工具匣清理，爭議會升成互相索賠。

## 出發前整備
- 一般；24 小時（48 個空檔回合）；開始位置：本局 `<LOC#1>` 附近客舍。
- 普通食水、繩索、紙墨、燈油、旅用品可購買並可寄存；無特殊缺貨。
- 玩家已知：全案在渡口與倉房，不跨日野外；正式受命後一日內須給出可重查結論。
- 整備期間不推進該一日期限；可快速略過逐項確認，但不生成未持有物品、免費製作或恢復。
- 出發前逐名固定攜帶格、隨身／載具物品、留存位置與承載上限。

## 開場與名譽
渡頭三戶共同出 9 兩查核酬金。`<NPC#3>` 交出收貨秤記與箱封。公開俠名或【荊湖道民間】正向相對名譽使腳夫主動開放卸貨區；高惡名使旁觀者不願單獨作證，但必要帳記仍可取得。負向【荊湖道民間】時，`<NPC#3>` 要求另一名腳夫陪同查物，增加半刻鐘，不提高 DC。

## 必要結論
### A：短缺是在收貨後形成，或收貨時被遮掩
1. `<PROP#1>` 箱封未整體破壞，自動可見。
2. `智力（格物推理）DC13` 比較箱重、乾麻與填充物，確認重量不能證明長度；失敗耗一刻鐘，取得新秤或逐段量後可重試。
3. `<NPC#3>` 的秤記與口供證明只驗重量。

### B-wet：剪繩是為處理受潮
1. `<CLUE#1>` 新切口旁有潮硬纖維；2. 倉角漏瓦下有同材質濕麻屑與新換墊草；3. `<NPC#1>` 面對兩項物證會承認剪除。

### B-borrow：兩丈用於修渡纜
1. 現用臨時纜有與 `<PROP#1>` 同批麻纖維與相符長度；2. 工具匣半張借條有取用時刻；3. `<NPC#2>` 的救船時間與其他渡工記憶吻合。

## 場景
### `<LOC#2>` 倉房
可查 `<PROP#1>`、屋瓦、墊草、秤記。`智力（識字）DC13` 可從批號與收貨欄找交接空白；失敗耗一刻鐘。`敏捷（巧手）DC13` 可不拆壞箱封抽出底層填充；失敗破壞箱封，使「封條完整」來源失效，其他來源保留。

### `<LOC#1>` 渡頭
可查渡纜、工具匣、夜班交接板。`直覺（感應）DC13` 從新舊磨痕看出一段纜剛換過；失敗後須等下一班靠岸讓纜受力顯色差才可重試。`力量（體能）DC13` 協助拉船靠岸，可令渡工立即停手談話；失敗只多等一班船。

### 腳夫歇棚
`<NPC#3>` 不願背偷竊責任。`魅力（說服）DC13` 可使他召集兩名腳夫共同回憶；失敗則改以原始秤記逐筆對時。高俠名使其主動說出「只秤重沒量長」，高惡名使其只在眾人面前回答。

## 終局與偏離
可公開查明、私下促成補帳賠付，或錯指無辜者。無必要戰鬥。任一 NPC 死亡，物證與另外來源仍可完成相應變體。只有角色同時焚毀 `<PROP#1>`、帳記、`<CLUE#1>` 並使所有知情者失去作證能力，才進入證據不足。

## 結局
### `ending_id: rope_shortage_explained`｜「短數有據」｜完整結算
條件：一日內證成當局變體責任鏈並提出可重查證據。
- 基礎歷練：調查 2、江湖 1；合計 3（R=8、小型 B=3）。完整參與須至少兩個主要場景且驗證 A 或 B 一項來源；中途加入按實際來源每項調查 1、終局江湖 1，最高 3。
- 物質：9 兩實際取得，隊伍共有、可分割，結算時分配；未達共識共同持有。
- 名譽：俠名 `0.2x E`；惡名 `0.0x E`；【荊湖道民間】`+0.3x E`，由渡戶、腳夫與船工傳播。

### `ending_id: rope_shortage_settled`｜「先把船開走」｜完整結算
條件：查明直接原因並恢復渡運，但未保全完整責任證據。
- 基礎歷練：調查 1、江湖 1；合計 2。
- 物質：6 兩實際取得，共同、可分割；餘 3 兩未取得。
- 名譽：俠名 `0.2x E`；惡名 `0.0x E`；【荊湖道民間】`+0.2x E`。

### `ending_id: rope_shortage_false_blame`｜「錯帳成怨」｜失敗結算
條件：公開指控無辜者且在相反證據出現後拒絕更正。
- 基礎歷練：江湖 1；合計 1，只給實際參與者。
- 物質：酬金未取得。
- 名譽：俠名 `0.0x E`；惡名 `0.2x E`；若指控可可靠歸因到角色，【荊湖道民間】`-0.3x E`；匿名且無可靠歸因則 `0.0x E`，私人怨恨仍成立。

新 `game_id` 產生另一宗新事件；不重置角色卡既有歷練、惡名、相對名譽或履歷。本篇不作其他劇本必要前置。