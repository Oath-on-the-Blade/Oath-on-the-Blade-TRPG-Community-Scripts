# 渡倉每旬總有一箱麻繩少兩丈

## 劇本規格
- `script_id`: `ootb_repeatable_ferrywarehouse_rope_shortage`
- 劇本版本：1.0.0
- 冒險分類：一般冒險
- 運行類別：可重覆任務
- `replay_policy`: `repeatable`
- 連續性：`independent`
- 劇本規模：小型
- 重跑基型：兩者
- 建議人數：3–5
- 建議等級：6–9 級
- 歷練基準等級：R=8
- 預計時間：3–4 小時
- 地區：荊湖道的普通渡運聚落
- 可能的社會名譽變化：俠名、惡名、【荊湖道民間】
- 需要引用的內容庫條目：`內容庫/拿手.md`、`內容庫/相對名譽對象.md`

## 可重覆實例規則
每個新 `game_id` 都是一宗新的渡倉短料事件。開局先生成 `<LOC#1: 名?-渡>` 與 `<LOC#2: 名?-倉>`，再生成全部 NPC 姓名；同一 `game_id` 暫停／續跑保持映射。若合法上下文已知上一實例名稱，避開重用，但不得為此搜尋未載入舊存檔。不同實例的同一 NPC/LOC 編號只是功能槽位。

GM 在玩家接觸第一條有意義線索前鎖定一個 `variant_id`，不得因玩家猜測改換：
- `variant_id: wet_cut`: 倉內一批麻繩受潮，管倉人為避免整箱報廢，私下剪去最爛兩丈再把箱重補成原數，造成長度短缺。
- `variant_id: ferry_borrow`: 夜班渡工為臨時修纜借走兩丈，原想天亮補回，但替班交接失誤，借用沒有入帳。

每局重新生成：
- `<NPC#1: 姓?-名?>`：管倉人。
- `<NPC#2: 姓?-名?>`：夜班渡工。
- `<NPC#3: 姓?-名?>`：收貨腳夫領班。
- `<PROP#1>`：短料麻繩箱；固定功能為「帳面長度與實長不符」，外箱木色、繩捆顏色與普通批號每局重生成，這些表象不改證據力度。
- `<CLUE#1>`：長度差的作業痕跡；`wet_cut` 為潮斑＋新切口，`ferry_borrow` 為渡頭臨時纜結＋未完成借條。載體細節每局可變，但因果不可變。

所有玩家可見帳頁、口供與標籤都跟隨本局映射。上局的毀損、人物、地點、獎勵與結局不帶入新局。

### 重覆獎勵上限
同一角色首次完成本 `script_id` 可取得正常歷練、現銀與正向名譽；之後新 `game_id` 仍可遊玩並保留世界後果，但現銀個人分配累計最多折算為隊伍總額 18 兩的兩次份額，正向【荊湖道民間】相對名譽最多從本劇本累計 `+0.6x E`，俠名最多累計兩次本篇正向倍率。負面名譽與惡名不因重跑而封頂；每次都按實際可歸因後果結算。

## 世界真相與變體
### wet_cut
三日前 `<NPC#1>` 發現一角屋瓦漏水，`<PROP#1>` 受潮。他怕整箱報廢要自賠，剪去最爛兩丈，把乾燥廢麻塞入箱底補重量。`<NPC#3>` 只按重量驗收，未量長度。今日渡工實際取用才發現短料。若無人介入，今晚 `<NPC#1>` 會換掉潮濕墊草，明日仍能由切口、屋瓦漏痕與收貨記錄重建真相。

### ferry_borrow
前夜渡纜突然磨裂，`<NPC#2>` 從 `<PROP#1>` 借走兩丈修纜，寫了半張借條，卻在換班時被急召去救一艘走錨小船。借條留在渡頭工具匣，倉簿未記。`<NPC#1>` 只知道箱子短料，懷疑腳夫偷拿。若無人介入，兩日後舊纜被換下，借條也可能隨工具匣清理丟失，爭議升成互相索賠。

## 出發前整備
- 整備類型：一般
- 安全空檔：24 小時（48 回合）
- 開始位置：本局 `<LOC#1>` 附近客舍
- 補給：普通食水、繩索、紙墨、燈油、旅用品可正常購買；可寄存物品
- 已知限制：任務全在渡口與倉房，不跨日野外；午後船班繁忙
- 玩家已知：委託是核實一箱麻繩為何少兩丈，要求一日內給出可重查結論
- 危機：正式受命後起算一日；整備時間不消耗期限
- 可快速略過逐項確認：是，但不生成未持有物品或免費恢復
- 出發前交接：逐名確認攜帶格、隨身／載具物品、留存位置與承載上限

## 開場與名譽
渡頭三戶共同出九兩作查核酬金。`<NPC#3>` 交出收貨秤記與箱封。公開俠名或【荊湖道民間】正向相對名譽會使腳夫主動讓角色查看卸貨區；高惡名會使旁觀者不願單獨作證，但必要帳記仍可取得。負向【荊湖道民間】相對名譽時，`<NPC#3>` 要求另一名腳夫全程在場，增加每次私下查物半刻鐘，不提高 DC。

## 必要結論
### A：短缺是在收貨後形成，或收貨時已被遮掩
1. `<PROP#1>` 箱封未整體破壞；合理查看自動取得。
2. `智力（格物推理）DC13` 比較箱重、乾麻與填充物，可判斷「重量正常不代表長度正常」；失敗耗一刻鐘，取得新秤或拆箱逐段量後可重試。
3. `<NPC#3>` 的秤記與腳夫口供證明只驗重量。

### B-wet：剪繩是為處理受潮
1. `<CLUE#1>` 的新切口旁仍有潮硬纖維。
2. 倉角屋瓦下有同材質濕麻屑與新換墊草。
3. `<NPC#1>` 面對漏水位置與切口時會承認剪除，但先辯稱「只是救貨」。

### B-borrow：兩丈被拿去修渡纜
1. 渡頭現用臨時纜有與 `<PROP#1>` 同批麻纖維與相符長度。
2. 工具匣內半張借條記有取用時刻，沒有簽完交接。
3. `<NPC#2>` 能說出取繩後救船的時間，與其他渡工記憶互相印證。

## 場景
### `<LOC#2>` 倉房
可檢查 `<PROP#1>`、屋瓦、墊草、秤記。`智力（識字）DC13` 可從批號與收貨欄找出交接空白；失敗只耗一刻鐘。敏捷（巧手）DC13 可在不拆壞箱封的情況抽出底層填充物；失敗會破壞箱封，但內容仍可查，且使「封條完整」這一來源失效，其他來源保留。

### `<LOC#1>` 渡頭
可查看渡纜、工具匣與夜班交接板。`直覺（感應）DC13` 能從新舊磨痕看出其中一段纜剛換過；失敗後只有等下一班靠岸、讓纜受力顯出色差，才可重試。力量（體能）DC13 可協助拉船靠岸，換取渡工立即停手說話；失敗只令角色多等一班船。

### 腳夫歇棚
`<NPC#3>` 願意公開秤記，但不願背偷竊責任。魅力（說服）DC13 可使他召集兩名收貨腳夫共同回憶；失敗則須用原始秤記逐筆對時。高俠名使他主動說出「只秤重沒量長」；高惡名使他只在眾人面前回答。

## 終局與偏離
角色可公開查明、私下促成補帳與賠付，或錯指無辜者。沒有戰鬥必要。若任一 NPC 死亡，物證與另外兩個來源仍能完成相應變體。若角色同時焚毀 `<PROP#1>`、相關帳記及 `<CLUE#1>`，又令所有知情者失去作證能力，則只能進入證據不足結局。

## 結局
### `ending_id: rope_shortage_explained`｜「短數有據」｜完整結算
條件：在一日內證成當局變體的責任鏈並提出可重查證據。
- 基礎歷練：調查 5、江湖 3、計略 2；合計 10。
- 完整參與：至少參與兩個主要場景，且親自取得／驗證 A 或 B 的一項來源；中途加入按實際完成的核心來源每項調查 2、終局江湖 2，最高 10。
- 物質：9 兩實際取得，隊伍共有、可分割，由玩家結算時分配；未達共識維持共同持有。
- 社會名譽：俠名 `0.2x E`；惡名 `0.0x E`；【荊湖道民間】`+0.3x E`，由渡戶、腳夫與往來船工傳播。

### `ending_id: rope_shortage_settled`｜「先把船開走」｜完整結算
條件：查明直接原因並恢復渡運，但未保全完整責任證據。
- 基礎歷練：調查 3、江湖 3、計略 2；合計 8。
- 物質：6 兩實際取得，共同、可分割；餘 3 兩未取得。
- 社會名譽：俠名 `0.2x E`；惡名 `0.0x E`；【荊湖道民間】`+0.2x E`。

### `ending_id: rope_shortage_false_blame`｜「錯帳成怨」｜失敗結算
條件：角色公開指控無辜者且拒絕在相反證據出現後更正。
- 基礎歷練：調查 2、江湖 1；合計 3，只給實際參與者。
- 物質：酬金未取得。
- 社會名譽：俠名 `0.0x E`；惡名 `0.2x E`；【荊湖道民間】`-0.3x E`。負面變化只在指控可被當地共同體可靠歸因到角色時成立；若角色從未公開身份，則該相對名譽為 `0.0x E`，但被錯指者仍保留私人怨恨。

## 新局邊界
每個新 `game_id` 重置本篇 NPC、LOC、PROP/CLUE 形態與變體，並產生另一宗新事件；不重置角色卡已累積的歷練、惡名、相對名譽或其他世界履歷。本篇不作任何其他劇本的必要前置。