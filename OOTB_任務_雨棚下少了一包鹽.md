# 雨棚下少了一包鹽

## 劇本規格
- `script_id: ootb-repeatable-missing-salt-under-awning-v1`
- 劇本版本：1.0
- 運行類別：可重覆任務
- `replay_policy: repeatable`
- 連續性：independent
- 重跑基型：兩者（日常可再發生＋小型分支）
- 冒險分類：一般冒險
- 建議角色：3–5 人，6–10 級
- 歷練基準等級：R=8
- 劇本規模：小型
- 預計時間：2–3 小時
- 類型：市集糾紛、追索、調查
- 可能的社會名譽變化：無持久社會名譽變化；既有名氣可影響證人是否主動開口
- 需要引用的內容庫條目：`內容庫/拿手.md`

## 可重覆實例化
每個新 `game_id` 都是一宗新的貨包短缺事件。先鎖定 `variant_id`，再生成本輪資料；玩家接觸第一條有意義線索後不得更換變體。

- `variant_id: porter_mixup`：搬運時誤把一包鹽送進鄰攤貨車，後來有人怕賠錢而掩飾。
- `variant_id: debt_seizure`：攤主欠一筆短債，債主手下趁混亂取走一包抵債，但沒有合法取得攤主同意。

本輪實例槽位：
- `<NPC#1: 姓?-名?>`：鹽攤掌櫃，委託人。
- `<NPC#2: 姓?-名?>`：臨時腳夫；變體一為誤送者，變體二為目擊者。
- `<NPC#3: 姓?-名?>`：鄰攤掌櫃；變體一為收到錯貨者，變體二為看見取貨方向者。
- `<NPC#4: 姓?-名?>`：變體一為怕被扣工錢而改口的領工；變體二為取貨的債主手下。
- `<LOC#1: 名?-市>`：本輪市集。
- `<LOC#2: 名?-茶棚>`：腳夫歇腳處。
- `<PROP#1>`：失蹤鹽包；固定功能是委託貨物，外層形態每局從「麻布雙繩／粗布單繩／藤篾外套布袋」選一，鹽重與所有權不變。
- `<CLUE#1>`：搬運標記；每局從「炭筆短線／紅土指印／麻繩結位」選一，功能均是區分同批貨包。

新 `game_id` 重新生成全部 NPC、LOC 與可變 PROP/CLUE；同一 `game_id` 暫停或續跑保留既有映射、持有、破損、公開狀態。若合法上下文已知上一實例資料，避免完全照抄，但不得搜尋未載入舊存檔。不同實例的同一 ID 只是功能槽位，並非同一人物、地點或物件改名。玩家可見口供、標記與貨單都使用本輪映射。

### 世界內再次發生原因與防刷
市集每日有大量散裝貨由臨時腳夫轉運，錯送、短債與趁亂取貨都可在不同人物、不同市集自然再次發生。每次事件以 `game_id + LOC#1 + NPC#1` 區分。

同一角色每個 `game_id` 最多取得一次本篇歷練與報酬。歷練終身最多取得 2 次；第 3 次起仍可遊玩，但本篇基礎歷練為 0。現銀報酬每局可取得，但單一角色終身由本 `script_id` 分得的委託報酬累計上限 12 兩；超出部分由委託人改以當餐、短程寄物等當局即耗服務結清，不形成持久資產。無永久物品、正向相對名譽或其他可累積永久獎勵。

## 出發前整備
- 整備類型：一般。
- 安全空檔：24 小時（48 個空檔回合）。
- 開始位置：距 `<LOC#1>` 半日步程內的普通聚落。
- 可正常取得：普通食水、客棧、日常雜貨與常規服務。
- 已知限制：無特殊缺貨；委託只要求查回一包貨，事件本身尚未形成急迫追殺。
- 玩家已知行程：市集與周邊巷道內活動，通常當日可返，不跨日。
- 整備期間危機：24 小時整備完成後才進入本輪事件；不倒扣事件時間。
- 可快速略過逐項確認：是；但不代表免費購買、製作、補水或取得未持有物品。
- 出發前交接：逐名確認攜帶格上限、隨身物、載具物品、留存位置，所有承載不得超限。

## 開場
`<NPC#1>` 在 `<LOC#1>` 收攤點貨時發現 `<PROP#1>` 少了一包。貨值不足以驚動大勢力，但足以吃掉數日利潤。其願付整隊 12 兩查明去向；若貨仍完整，取回即可；若已拆散，查明責任與可追討對象也算完成。

若角色已有較高名氣，`<NPC#2>` 與 `<NPC#3>` 會先承認自己確實看過搬運過程；惡名很高則他們只回答直接問題，但不改 DC。此影響只改資訊主動程度，不產生持久名譽變化。

## GM 真相與時間線
### 共通
辰末：貨車抵市；`<NPC#2>`、`<NPC#4>` 等人搬貨。午前：短暫陣雨，貨包移到雨棚下。午正：`<PROP#1>` 離開原堆。未時：`<NPC#1>` 點貨發現短少並找人。

### porter_mixup
`<NPC#2>` 按錯 `<CLUE#1>`，把 `<PROP#1>` 推上 `<NPC#3>` 的貨車。`<NPC#4>` 看見後怕領工責任被追究，叫 `<NPC#2>` 先說「可能在雨棚被偷」。`<NPC#3>` 不知多收一包，貨車停在市後裝車巷。若玩家不介入，申末貨車離市，之後追討成本大增。

### debt_seizure
`<NPC#1>` 確有短債，但約定三日後結清。`<NPC#4>` 自作主張提前拿貨抵債，沒有攤主同意；`<NPC#2>` 看見其把 `<PROP#1>` 搬往 `<LOC#2>` 後巷，因怕惹事只說「有人取貨」。`<NPC#3>` 看見 `<NPC#4>` 來時先問過掌櫃是否在場。若玩家不介入，申末 `<NPC#4>` 把鹽轉交債主車隊，爭議升級。

## NPC 運行
### `<NPC#1>`
知道貨單、債務（若適用）與自己的貨包標記；不知道實際搬運。核心利益是保住薄利與信用。願公開貨單，不願一開始談私人欠債。若角色提出「債務可能相關」且沒有公開羞辱，會承認欠款。若被威脅，停止合作但不攻擊。

### `<NPC#2>`
想保住工錢。變體一知道自己可能搬錯，害怕被扣工；變體二知道取貨方向，害怕債主報復。看到與自己口供矛盾的物證後會改口。若被逼走，可由 `<CLUE#1>` 與 `<NPC#3>` 補足必要結論。

### `<NPC#3>`
只保護自己生意，不想背偷貨名聲。變體一若檢查車上便會發現多貨；變體二只知道 `<NPC#4>` 的到場與離開方向。若角色先指控其偷竊，態度惡化但仍允許在兩名市役旁觀下查車。

### `<NPC#4>`
變體一想掩蓋管理失誤，展示正確搬運記號後會承認；變體二認為「欠錢拿貨天經地義」，但知道自己沒有書面抵債同意。若被堵住，優先辯解、交還尚未轉走的貨，不主動死鬥。

## 線索網
核心結論 A「貨包如何離開原堆」至少有三源：`<CLUE#1>` 在原堆與去向處的對應；`<NPC#2>` 的搬運記憶；`<NPC#3>` 對貨車／來人的目擊。

核心結論 B「是失誤還是主動取貨」至少有兩源：變體一可由 `<NPC#3>` 車上多出的同批貨與 `<NPC#4>` 改口互證；變體二可由 `<NPC#1>` 的到期債據與 `<NPC#4>` 無提前抵債授權互證。

若所有口供來源都離場，物證仍可完成判斷；若玩家焚毀貨單又拆散標記，且同時放走所有 NPC，則證據鏈永久不足，進入「無法坐實」結局。

## 主要場景
### 雨棚貨堆
自動資訊：地面沒有強搶翻動；同批貨仍按 `<CLUE#1>` 排列。合理查看即可發現少一個對應位置。

深入方法：智力（格物推理）DC13 可從受潮、拖痕與堆疊判斷貨包是搬運時離堆，耗時 10 分鐘；失敗只多耗 20 分鐘並留下不確定範圍，取得新目擊口供或實際對照另一堆後可重試。直覺（感應）DC13 可注意領工與腳夫對「哪一排先搬」說法不同，成功只指出矛盾，不直接判責。

直接成立：逐包核對 `<CLUE#1>` 可在 20 分鐘內確認是否有同批貨混入別堆。不能成立：只看一眼普通鹽粒便認定唯一來源，鹽本身沒有個體指紋。

### 裝車巷與 `<LOC#2>` 周邊
變體一：`<NPC#3>` 貨車仍在；合理獲准查車即可找到多出的 `<PROP#1>`。若強行闖車，市役會要求停手，事件轉為公開爭執。

變體二：`<LOC#2>` 後巷有搬運壓痕與短暫擱包留下的鹽粉。直覺（生存）DC13 可在 10 分鐘內確認去向至債主臨時車位；失敗令追查多耗 20 分鐘，向茶棚客人問路後可重試。魅力（說服）DC13 可讓 `<NPC#4>` 在看到債據到期日後先交貨再談債；失敗時其要求 `<NPC#1>` 親自到場，增加 20 分鐘但不封死結局。

## 危機時鐘
進場起計：0–60 分鐘，各人仍在市內；60–120 分鐘，`<NPC#4>` 開始催車；120 分鐘後，變體一貨車離市、變體二鹽包轉交車隊。離市後仍可由既有證據完成責任判斷，但本局不再能立即取回貨物。

## 偏離處理
提前猜中：只要玩家能提出可驗證方法，直接讓其查相應物證，不要求走完場景。殺傷 NPC：市役介入，該 NPC 口供消失但物證冗餘仍在；若因此令全部來源永久消失，進失敗結局。拒絕委託後自行查：可查，但無委託報酬。直接賠掌櫃 12 兩：只解決短期損失，真相未明，算「止損未查明」。

## 終局與結算
完整歷練的個人參與條件：角色至少實際參與一個主要場景，並參與取得一項核心結論或終局交涉；只在最後到場者不取得本篇基礎歷練。

### ending_id: salt_returned
顯示名稱：貨歸原主。條件：查明因果並在轉走前取回 `<PROP#1>`，可選擇協調責任而不必羞辱任何人。
- 基礎歷練：`江湖 1、計略 1、洞察 1`，合計 3。
- 委託報酬：整隊 12 兩，實際取得；由 `<NPC#1>` 支付，可分割，由玩家自行分配，未分配前為共同持有。
- `<PROP#1>`：歸還 `<NPC#1>`，角色未取得。
- 俠名 `0.0x E`；惡名 `0.0x E`；相對名譽無變化。

### ending_id: truth_without_goods
顯示名稱：去向已明。條件：證據足以查明責任，但貨已轉走或玩家選擇不追回。
- 基礎歷練：`江湖 1、計略 1、洞察 1`，合計 3。
- 委託報酬：整隊 8 兩，實際取得；可分割，由玩家分配。
- 貨物：未取得／已轉走；`<NPC#1>` 持有對責任人的追討資訊。
- 俠名、惡名、相對名譽均 `0.0x E`／無變化。

### ending_id: evidence_lost
顯示名稱：無法坐實。條件：玩家自行令所有合理來源不可回復，或在證據不足時停止。
- 基礎歷練：`江湖 1`，合計 1。
- 報酬：未取得；若 `<NPC#1>` 主動提前給過款項，視為應返還，結算為返還義務。
- 社會名譽：無變化。

## 重跑結算提醒
先套本篇歷練與現銀累積上限，再進行角色等級折算與個人加成。任何新 `game_id` 都不得把上一局 NPC、LOC、PROP、CLUE 的實例狀態帶回來。