# 短驛每旬總有一封貨單晚到一刻

## 劇本規格
- `script_id`: `ootb-repeatable-low-waybill-delay-001`
- 劇本版本：1.0.0
- 運行類別：可重覆任務
- `replay_policy`: `repeatable`
- 連續性：`independent`
- 重跑基型：兩者（日常可再發生＋小型分支）
- 類型：低等級一般可重覆任務；冒險分類：一般冒險
- 建議玩家：3–5；建議等級：3–5級；歷練基準 R：4
- 劇本規模：小型；預計 2 小時；武力層級：低；預期戰鬥：0 場
- 主要地區：河洛道一處新生成短驛 `<LOC#1: 名?-驛>`、附近貨棧 `<LOC#2: 名?-棧>` 與兩地間街巷
- 核心目標：查清一封貨單為何比同批貨晚到一刻，讓當日貨物可按時放行
- 銀兩與物質獎勵：整隊 8 兩；同一角色由本 `script_id` 可累積取得現銀最多 2 次，之後仍可遊玩但該角色不分本篇委託金
- 可能的社會名譽變化：俠名、惡名、【河洛道民間】；同一角色由本 `script_id` 的正向【河洛道民間】倍率最多結算 2 次，負向不受此防刷上限豁免
- 需要引用的內容庫：`內容庫/拿手.md`【識字】【洞察】【格物推理】【感應】【說服】；`內容庫/相對名譽對象.md`【河洛道民間】

## 新局實例化
每個新 `game_id` 在第一條有意義線索前：
1. 重新生成全體 NPC 可變姓名；同局續跑保留。
2. 重新生成 `<LOC#1>`、`<LOC#2>` 專名；同局保留。不同局同 ID 只是功能槽位。
3. 鎖定 `variant_id`：`V-A_錯封補送` 或 `V-B_代簽繞路`；不得依玩家猜測更換。
4. 重新生成 `<PROP#1>` 貨單載體的非必要表象（麻紙／竹紙；灰繩／褐繩）與 `<CLUE#1>` 路上痕跡的非必要表象（泥點／灰粉）；功能與證據力度不變。同局保留映射、持有與破壞狀態。
5. 若合法上下文已知上一實例姓名、地名或表象，避開完全照抄；不得越權搜尋未載入舊存檔。

世界內重複原因：短驛每旬處理不同貨棧、不同腳夫與不同貨單；誤封、代簽、臨時補送與繞路都會在新的日常貨運中再次發生。每局是另一宗事件，上一局的人物、地點損壞與結果不帶入。

## 出發前整備
一般整備；24 小時（48 回合）；開始於河洛道普通市鎮。可取得一般食宿、紙筆、燈油與常用工具。玩家已知事件全在一個市鎮內、不跨日，貨棧在兩個時辰後封今日帳。整備期間不推進該期限；正式接案後開始計時。可快速略過逐項確認，但不產生免費購買、製作或休息。出發前逐名固定隨身、載具、留存位置與攜帶格。

## 開場
短驛書手 `<NPC#1: 姓?-名?>` 出示 `<PROP#1>`：同批三封貨單中，它晚到一刻，令 `<LOC#2>` 的一車普通貨物暫不能放行。書手付整隊 8 兩查清「晚到是否只是流程錯誤，還是貨單曾被人換手」。

有正向【河洛道民間】相對名譽或顯著俠名者，貨棧掌櫃 `<NPC#2: 姓?-名?>` 主動開放收發簿；顯著惡名或負向名譽時，他要求夥計在場。必要線索與 DC 不變。

## 兩個真相變體
### `variant_id: V-A_錯封補送`
`<NPC#2>` 的學徒 `<NPC#3: 姓?-名?>` 把 `<PROP#1>` 誤封進另一車的紙套，發現後怕被扣工錢，拆出再讓跑腿 `<NPC#4: 姓?-名?>` 補送。晚一刻來自補送，沒有偽造貨單。
- 時間線：貨單寫妥 → 誤封 → 車夫指出紙套多一封 → `<NPC#3>` 拆出 → `<NPC#4>` 抄近巷補送 → 晚一刻到驛。
- `<CLUE#1>`：近巷泥點／灰粉與 `<NPC#4>` 鞋底、衣角相符。
- `<PROP#1>`：封繩有一次解開再綁的壓痕，但墨跡、簽押均原始。

### `variant_id: V-B_代簽繞路`
貨棧記帳人 `<NPC#3: 姓?-名?>` 臨時離開照顧家人，請跑腿 `<NPC#4: 姓?-名?>` 把已寫好的 `<PROP#1>` 送去短驛；`<NPC#4>` 不識近路，先繞到錯街問路。`<NPC#3>` 怕被掌櫃責怪，回來後謊稱自己親送。貨單本身仍真實。
- 時間線：貨單寫妥 → `<NPC#3>` 臨時離開 → `<NPC#4>` 代送 → 走錯街 → 問路折返 → 晚一刻到驛 → `<NPC#3>` 回來後謊稱親送。
- `<CLUE#1>`：錯街泥點／灰粉與當地路面相符。
- `<PROP#1>`：封繩未被重綁；只有跑腿握持造成的普通折痕。

兩變體共同最壞結果：兩個時辰後貨棧封帳，該車延至次日放行並產生小額倉租，無生命危險。

## NPC 認知與行動
`<NPC#1>` 知道到件時間，不知道途中經過；利益是維持短驛記錄可信。`<NPC#2>` 知道貨單由棧內開出，不知道延誤細節；利益是今日放貨。`<NPC#3>` 在 A 知道誤封並隱瞞；在 B 知道代送並謊稱親送；兩者都怕扣工錢。`<NPC#4>` 在 A 知道自己補送；在 B 知道自己代送走錯路；兩者都願在角色不先威嚇時說實話。

## 必要結論與冗餘
A：①貨單曾誤封再拆：重綁壓痕、錯車紙套內同批紙屑、`<NPC#3>` 坦白。②晚到由補送造成：`<CLUE#1>`、`<NPC#4>` 口供、驛口賣水人記得其奔跑到達。每項任兩來源。
B：①貨單沒有被換封：原封連續壓痕、貨棧剩繩切口吻合、驛書手確認到件時封口完整。②晚到由代送走錯路造成：`<CLUE#1>`、錯街攤販記得問路、`<NPC#4>` 口供。每項任兩來源。

任何 NPC 離開都不會抹除全部來源；若 `<PROP#1>` 被毀，收發簿、紙套／剩繩與口供仍能收束，但不能再判斷細部封口狀態。

## 場景
### `<LOC#1>` 短驛
自動可見到件簿與 `<PROP#1>`。
- `智力（識字）DC10`，10 分鐘：對比三封同批貨單的時刻、欄位與簽押；成功確認內容沒有後改。失敗可花 30 分鐘逐欄核對直接取得。
- `智力（格物推理）DC10`，10 分鐘：檢查封繩壓痕。A 成功見一次重綁；B 成功確認壓痕連續。失敗不毀證，需取得貨棧同批剩繩作新比較才可重試。

### `<LOC#2>` 貨棧
- 自動資訊：收發簿記三封同時寫妥。
- `直覺（洞察）DC10`：詢問「誰親手送」時，成功發現 `<NPC#3>` 避談一刻鐘空檔；只證明隱瞞，不證明哪個變體。
- `魅力（說服）DC10`：以「查流程、不先扣工錢」交換坦白。失敗後需帶回封繩證據、路上目擊或其他新實質條件才可重試。

### 兩地間街巷
- `直覺（感應）DC10`，15 分鐘：沿角色已知路線找 `<CLUE#1>` 對應痕跡。成功把跑腿行程連到近巷（A）或錯街（B）；失敗後詢問沿路攤販 30 分鐘可取得另一來源。
- 不能成立：僅靠一處泥點／灰粉直接辨認責任人；痕跡只證明路線。

## 危機與偏離
接案後兩小時封帳，每 30 分鐘一格。第 4 格前只要證明貨單真實且沒有換單風險，`<NPC#2>` 可請 `<NPC#1>` 註記後放行。玩家可先放貨再追責、直接找跑腿、比對封繩或查目擊者。公開指控偽造會令貨棧暫停合作 30 分鐘，出示實證後恢復。

## 終局
### `ending_id: waybill_delay_explained`｜晚到有了交代
條件：確認本局變體的延誤原因，且證明貨單真實。
- 基礎歷練：調查 2、江湖 1、計略 1，共 4。
- 完整參與：至少實際查過貨單／貨棧／街巷之一，並參與最後核實。
- 物質：整隊 8 兩實際取得、共同持有可分割；角色達現銀 2 次上限後不分該角色份額，其他未達上限者照常分配，總支付按合資格人數由 GM 依原分配比例扣除該角色份額。
- 俠名 `0.1x E`；惡名 `0.0x E`；【河洛道民間】`+0.1x E`，由貨棧、短驛與街坊傳開；達正向 2 次上限後該角色本項為 `0.0x E`。

### `ending_id: waybill_safe_only`｜先讓貨走
條件：證明貨單真實並安全放行，但未查明晚到原因。
- 基礎歷練：調查 1、江湖 1，共 2。
- 物質：整隊 4 兩，適用同一現銀 2 次上限。
- 俠名 `0.0x E`；惡名 `0.0x E`；【河洛道民間】`0.0x E`。

若角色捏造證據令無辜者受罰且被街坊核實：惡名 `0.2x E`、【河洛道民間】`-0.2x E`；不受正向防刷上限抵銷。

## 重跑邊界
歷練可依每個新事件正常取得；同一 `game_id` 不得重結。現銀與正向【河洛道民間】各最多對同一角色結算 2 次；俠名因可傳播事蹟最多結算 2 次，之後同類成功為 `0.0x E`。無永久物品、武學、引薦或跨劇本必要 state。新局全體 NPC、LOC、可變 PROP／CLUE 重新實例化；同局保持。

## GM 最終核對
兩變體在第一條有意義線索前鎖定；各自有獨立真相、時間線及雙重線索；新局不倒帶；所有判定使用正式拿手；必要線索不鎖單骰；整備、期限、物質交付與防刷上限均可直接執行。