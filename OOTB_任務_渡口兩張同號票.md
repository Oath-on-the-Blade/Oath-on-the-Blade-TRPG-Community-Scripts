# 渡口兩張同號票

## 劇本規格
- `script_id: ootb-repeatable-ferry-duplicate-ticket-v1`
- 劇本版本：1.0
- 運行類別：可重覆任務
- `replay_policy: repeatable`
- 連續性：independent
- 重跑基型：兩者（日常可再發生＋分支）
- 冒險分類：一般冒險
- 建議角色：3–5 人，6–10 級；歷練基準等級 R=8
- 劇本規模：小型；預計 2–3 小時
- 類型：渡口秩序、查驗、限時抉擇
- 社會名譽：無持久變化；既有名氣只影響渡工是否先讓角色查看票冊
- 內容庫：`內容庫/拿手.md`

## 可重覆實例化
新 `game_id` 先鎖定一個變體，第一條有意義線索公開後不得切換：
- `variant_id: copied_stub`：有人照著已售票根抄出第二張票，想搶在正主前上船。
- `variant_id: clerk_double_issue`：售票人忙中誤把同一號碼寫給兩名乘客，事後因怕賠償而不敢承認。

槽位：`<NPC#1: 姓?-名?>` 渡口管事；`<NPC#2: 姓?-名?>` 第一持票人；`<NPC#3: 姓?-名?>` 第二持票人；`<NPC#4: 姓?-名?>` 售票人；`<LOC#1: 名?-渡>` 渡口；`<PROP#1>` 兩張同號船票；`<CLUE#1>` 本輪票面核驗特徵，從「筆鋒收尾／紙邊裁口／印泥缺角」選一。

每個新 `game_id` 重生全部 NPC、LOC 與可變 PROP/CLUE；同一 `game_id` 續跑保持映射與狀態。若合法上下文已知上一實例，避免完全重複；不得搜尋未載入舊存檔。不同實例同號槽位不代表同一人或同一物。玩家可見票面與口供使用本輪映射。

渡口每天都可能因抄票或忙中重號再發生同類事件；每次以 `game_id + LOC#1 + 當班NPC#4` 區分。同一角色每 `game_id` 最多結算一次；歷練終身最多 2 次，第 3 次起本篇基礎歷練為 0。現銀終身由本 `script_id` 分得上限 10 兩；超額改為當局即耗的渡船、餐食或寄物服務。無永久物品、正向相對名譽或其他可累積永久獎勵。

## 出發前整備
一般整備；安全空檔 24 小時（48 空檔回合）。開始位置為距 `<LOC#1>` 半日內普通聚落，可正常取得食水、客棧、日常雜貨與常規服務。玩家已知行程只在渡口與近岸活動、當日可返；整備完成後才開始本輪船班倒數。可快速略過逐項確認，但不視為免費取得任何物資。出發前逐名確認攜帶格上限、隨身與載具物品、留存位置，承載不得超限。

## 開場
午后最後一班載貨渡船準備離岸，`<NPC#2>` 與 `<NPC#3>` 同時拿出同號 `<PROP#1>`。兩人都聲稱自己先買。`<NPC#1>` 不願任意扣人，也不能讓兩人都占同一載重名額，請角色在 90 分鐘內查明。完成查驗整隊可得 10 兩。

高名氣角色可直接查看票冊；其他角色只需說明調查方法便可在 `<NPC#1>` 陪同下查看。高惡名角色不得單獨拿走票冊。這些差異不改 DC。

## GM 真相與時間線
共通：辰時開票；午前換過一輪印泥；未初兩名持票人到場；90 分鐘後船必須離岸。

`copied_stub`：`<NPC#3>` 在茶攤看過 `<NPC#2>` 暫放的票，照抄號碼與大致字樣，但沒有掌握 `<CLUE#1>` 的細節。`<NPC#4>` 的票冊只有一次該號售出記錄。`<NPC#3>` 想上船處理急事，會先堅稱自己票是真的；若被證明，願補買下一班而不動武。

`clerk_double_issue`：`<NPC#4>` 在兩段忙亂間把同號寫了兩次；兩張票都具本輪真實 `<CLUE#1>`，票冊邊欄卻有兩筆不同時間的收銀記號。`<NPC#4>` 怕被扣工錢，先說其中一人必是假票；面對兩筆收銀記號會承認。

## NPC 運行
`<NPC#1>` 要維持秩序與準時開船，不預設誰有罪；若角色公開羞辱乘客，會要求轉到票房內談。`<NPC#2>` 有可核對的買票時間與同行目擊，但急於上船。`<NPC#3>` 在抄票變體會迴避票面細節，在重號變體則能準確說出購票過程。`<NPC#4>` 掌握票冊與印泥更換時間；在重號變體有掩飾動機，但不會銷毀票冊。

## 線索網
結論 A「兩張票是否都由票房開出」至少三源：`<CLUE#1>`；票冊收銀記號；`<NPC#2>/<NPC#3>` 各自購票細節。結論 B「若只有一張真票，哪張是抄件」至少兩源：真票符合 `<CLUE#1>` 與票冊時段；抄件在紙邊／筆鋒／印泥細節不符。結論 C「若兩張都真，責任在何處」由兩筆收銀記號與 `<NPC#4>` 的當班順序互證。

若玩家毀掉兩票，票冊與購票口供仍能判斷是否重號；若再毀票冊且驅散全部證人，則證據鏈永久不足。

## 場景與檢定
### 驗票桌
合理並排查看兩票即可知道紙張、號碼與大印大致相同。智力（格物推理）DC13，10 分鐘：比對 `<CLUE#1>`；成功可判斷「一真一疑」或「兩張都符合當班真票特徵」。失敗多耗 20 分鐘，得到「肉眼難分」；取得票冊後可重試。

### 票房
票冊在 `<NPC#1>` 陪同下可查。智力（調查）DC13，10 分鐘：成功找到對應時段與收銀記號；失敗多耗 20 分鐘，但仍自動看到該號出現一次或兩次，不把核心資訊鎖在檢定後。魅力（說服）DC13 可讓 `<NPC#4>` 在公開對質前私下說明忙亂順序；失敗只令其要求在 `<NPC#1>` 面前談。

### 岸邊交涉
若真相已足夠，角色可提出補票、讓位或改搭下一班等方案。合理方案直接成立；若兩名乘客都堅持當班上船，魅力（說服）DC13 可促成其中一人接受全額退票加下一班優先，失敗則由 `<NPC#1>` 按先購紀錄安排，不封死結局。

## 危機與偏離
0–30 分鐘：正常查驗；30–60 分鐘：開始裝纜；60–90 分鐘：只剩最後核對；90 分鐘船離岸。船離岸後仍可查清責任，但無法改變本班乘船結果。

提前猜中者只要提出可驗證方法即可直接查。偷走票冊會令渡口報官並中止合作，但仍可從票與口供查驗。對 NPC 施暴會使船班立即拒載施暴者，且失去該 NPC 口供；物證仍可完成。直接替其中一人付下一班船資能止爭，若未查明則屬「止爭未查明」。

## 終局與結算
完整歷練個人條件：至少實際參與一個主要場景，並參與取得一項核心結論或終局安排；最後才到場者不取得基礎歷練。

### ending_id: verified_before_departure
顯示：準時查清。條件：90 分鐘內查明票務因果並安排本班席位。
- 基礎歷練：`江湖 1、計略 2`，合計 3。
- 報酬：整隊 10 兩，由 `<NPC#1>` 支付，可分割，玩家分配；未分配前共同持有。
- 船票：各歸原持有人或由渡口收回換票，角色未取得。
- 俠名 `0.0x E`；惡名 `0.0x E`；相對名譽無變化。

### ending_id: truth_after_departure
顯示：船去案明。條件：船已離岸後才查明。
- 基礎歷練：`江湖 1、計略 2`，合計 3。
- 報酬：整隊 6 兩，實際取得；可分割。
- 社會名譽無變化。

### ending_id: unresolved
顯示：同號成疑。條件：證據被玩家永久破壞或主動停止調查。
- 基礎歷練：`江湖 1`，合計 1；報酬未取得；社會名譽無變化。

## 重跑結算提醒
先套本篇歷練與現銀累積上限，再做角色等級折算與個人加成；新 `game_id` 不繼承上一局任何 NPC、LOC、PROP、CLUE 狀態。