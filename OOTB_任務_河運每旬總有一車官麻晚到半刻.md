# 河運每旬總有一車官麻晚到半刻

## 劇本規格
- `script_id`: `ootb-repeatable-high-hemp-cart-001`
- 劇本版本：1.0.0
- 運行類別：可重覆任務；`replay_policy: repeatable`
- 連續性：`independent`；劇本規模：小型；重跑基型：兩者
- 冒險分類：一般冒險；類型：運輸異常／調查
- 建議玩家：3–5；建議等級：11–13級；歷練基準 R：12
- 預計 2–3 小時；武力層級：高；預期戰鬥：0–1 場
- 需要引用的內容庫條目：`內容庫/拿手.md`【格物推理】【洞察】【感應】【騎術】【說服】【身法】
- 可能的社會名譽變化：【河洛道官府】、俠名、惡名

## 新 `game_id` 實例化
每局都是另一批官麻、車隊與涉事者。開局在第一條有意義線索前鎖定 `variant_id`，不得按玩家猜測切換；再生成並保存：`<LOC#1: 名?-驛站>`、`<LOC#2: 名?-渡口>`；`<NPC#1: 姓?-名?>` 押運書吏、`<NPC#2: 姓?-名?>` 車把式、`<NPC#3: 姓?-名?>` 臨時搬夫。新局全部姓名與地名重生成；同局續跑保持不變；已知上一局名稱時避開，但不搜尋未載入舊存檔。相同 ID 在不同局只是功能槽位。

生成 `PROP#1` 官麻封繩（顏色／普通纖維表象可變，功能固定為原車封繩）、`CLUE#1` 車轍異常（泥痕／石粉／草屑表象擇一）、`CLUE#2` 時刻差（更牌／水漏記號／驛簿欄位擇一）。新局重抽表象，同局固定；玩家可見帳頁與描述同步替換。表象不得改變證據力度、DC、責任者或結局。

世界內再次發生原因：官麻按旬由不同地方車隊送往河運轉運點，臨時改道、換車、裝卸與小規模舞弊均可再次發生；每局以 `game_id + LOC + NPC + PROP/CLUE` 區分，上一宗結案不會重置本宗。

## 出發前整備
一般；24 小時（48 個空檔回合）；開始於河洛道普通府城。食宿、馬料、繩索、燈油與普通修車工具可正常取得。玩家知道需沿官道往返兩處實例地點，單程不超過兩個時辰，當日可回；正式接案後距下一車抵達三個時辰。出發前逐名固定攜帶、載物與留存位置。

## 開場
`<NPC#1>` 發現近三旬各有一車官麻比同隊晚約半刻抵達 `<LOC#2>`，但封繩完整、重量大致相符。這一局是新的第四宗異常。查明並保住本旬交割，整隊報酬 24 兩；只確保準時交割 14 兩。

## 變體真相
### `variant_id: axle_swap`
`<NPC#2>` 的車軸有暗裂。他怕報修扣工，每次在 `<LOC#1>` 外借同規格備車短暫換載，造成半刻延誤；`<NPC#3>` 收小錢協助搬運。沒有偷麻。必要結論來源：`CLUE#1` 顯示兩段車轍輪距略異；原車軸內側有新裂；`PROP#1` 封繩只在整捆搬運時受磨、未解結。任兩項可證明換車，裂軸再證明動機。

### `variant_id: wet_bundle_exchange`
`<NPC#3>` 發現一捆官麻受潮，怕賠償，與 `<NPC#2>` 私下在 `<LOC#1>` 後棚用同等重量乾麻替換，打算日後自行補回；半刻用於搬換。必要結論來源：`PROP#1` 外結未拆但底層纖維有二次抬移壓痕；`CLUE#1` 顯示後棚短距離折返；乾麻捆的產地草繩與本批不同。任兩項可證明換捆，受潮麻藏處與兩人口供可證明動機。

未選變體的決定性線索不存在；兩變體共用的「半刻延誤」只證明中途停留。

## 場景與運行
### `<LOC#2>` 交割場
自動可比對到達時刻與封繩。`智力（格物推理）DC15` 比較 `PROP#1` 可辨搬運磨痕；失敗耗 20 分鐘，仍取得「封結未解」。【河洛道官府】正向相對名譽可令書吏主動開放前三旬非秘密時刻記錄。

### 官道與 `CLUE#1`
沿路慢查 40 分鐘可自動找到一次異常停留帶；`直覺（感應）DC15` 可在 10 分鐘內定位。`敏捷（騎術）DC15` 可追上尚未抵達的本旬車隊；失敗多耗 20 分鐘且坐騎需放慢，不造成憑空墮馬重傷。

### `<LOC#1>` 後場
選 `axle_swap` 時可見備車與裂軸；選 `wet_bundle_exchange` 時可見受潮麻藏處。`直覺（洞察）DC15` 可由 `<NPC#2>`、`<NPC#3>` 對停留時間的不同說法抓出矛盾；失敗後須提出物證或分開詢問才可重試。輕功可由外牆快速進後場，但不自動取得封閉箱櫃內資訊。

## 危機與偏離
正式接案三個時辰後本旬車抵達；若未介入，`axle_swap` 變體的裂軸在最後一里斷裂，延誤一個時辰；`wet_bundle_exchange` 變體完成替換後準時性仍差半刻。拘住任何單一 NPC 不會消除物證。玩家可先護送交割再追查，也可先截車處理風險。暴力毀車不構成有效「保交割」。

## 結局
完整歷練要求本局實際參與至少一項調查／護送並在交割或真相處置時仍參與。

### `ending_id: delivery_saved_truth_proven`｜本旬準點交割
查明本局變體真相並完成安全交割。
- 基礎歷練：重建因果 5；保住交割 4；妥善處置 3；總 12。
- 物質：整隊實際取得 24 兩，可分割，由玩家分配。
- 名譽：有正式交割紀錄時俠名 `+0.3E`、惡名 `0E`、【河洛道官府】`+0.4E`。

### `ending_id: delivery_only`｜先保住這一車
確保安全交割但未證明完整因果。
- 基礎歷練：護送與應變 5；有效線索 3；總 8。
- 物質：整隊實際取得 14 兩。
- 名譽：俠名 `+0.1E`、惡名 `0E`、【河洛道官府】`+0.2E`。

### `ending_id: cargo_lost_by_party`｜交割失敗
因角色可歸責行動令官麻遺失或毀損。
- 基礎歷練：有效調查 3；危機應變 2；總 5。
- 物質：報酬未取得；實際毀損依世界內清點追責。
- 名譽：可靠歸因時俠名 `0E`、惡名 `+0.3E`、【河洛道官府】`-0.4E`；無可靠歸因則均 `0E`。

## 重覆獎勵與後果上限
同一角色每個新 `game_id` 可正常取得該局基礎歷練；現銀報酬屬新事件新勞務，但同一角色跨局由本 `script_id` 實際分得現銀累計最多 36 兩，達上限後其應分份額改為不可轉讓的當地食宿與馬料代付、每局最多 3 兩且最多再觸發兩局。正向俠名與【河洛道官府】相對名譽由本 `script_id` 各最多累積 `+1.0E` 等值；超出部分記 0。負面名譽按實際新事件照常結算。無永久物品、武學、引薦或跨劇本 state。