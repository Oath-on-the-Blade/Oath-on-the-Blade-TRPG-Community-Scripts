# 飛鷲巡線總有一處回鈴太早

## 劇本規格
- `script_id: OOTB-HIGH-FACTION-REPEAT-EAGLE-BELL-001`
- 劇本版本：1.0.0
- 冒險分類：一般冒險
- 任務類別：飛鷲派門派專用任務
- 運行類別：可重覆任務
- `replay_policy: repeatable`
- 連續性：`independent`
- 重跑基型：兩者（日常可再發生＋小型分支）
- 門派勤務類型：內務
- 內務衝突類型：純內務
- 正式門派相對名譽對象：飛鷲派
- 建議人數：3–5人
- 建議等級：16–19級
- 歷練基準等級：R18
- 劇本規模：小型
- 預估時間：2–4小時
- 地區：朔北道飛鷲派巡路網的可替代外圍哨線；不以飛鷲崖山門本身作固定事件地點
- 世界知識依據：飛鷲派山門在朔北道飛鷲崖，重視視野、機動、守信與邊地自主，門內有長途傳訊與護送傳統
- 可能的社會名譽變化：飛鷲派門派相對名譽；無固定俠名／惡名
- 武學獎勵：無；本任務不建立、提高或外傳任何門派武學來源
- 需要引用的內容庫條目：無必需敵人；若玩家以已掌握武學處理地形，只按角色卡與相應正式條目執行，本劇本不改寫效果

## 門派專用資格與結算
- 合資格者：角色當前具有飛鷲派正式門派身份，且依 `遊玩規則/角色/門派專用任務資格硬規則.md` 仍可取得本派門派專用任務結算。
- 同行但不具上述資格者：可以完整參與冒險、取得本結局的基礎歷練與一般銀兩分配，但飛鷲派相對名譽固定為0。
- 本任務為可重覆純內務；正常合資格完整完成使用 `m=+0.5`。
- 本次一般實例固定使用 R18，故 `EGB-E1` 合資格飛鷲派角色取得飛鷲派相對名譽 `18 × 0.5 = +9` 點。
- 部分完成 `EGB-E2` 使用 `m=+0.2`，即 `+3.6` 點；失敗／撤離為 `m=0.0`。
- 每個新 `game_id` 都按本輪 ending 重新結算；門派相對名譽不受本任務重跑次數上限限制，也不與銀兩或歷練上限共用計數。

## 任務重跑規則
- 世界內再次發生原因：飛鷲派外圍傳訊路由由大量可替代哨點、風索、回鈴與接力信使維持。繩索磨耗、值守偷懶、錯判風勢、巡哨為趕交接而提前回鈴，都可能在不同哨線形成新的小型勤務事件。
- 每次事件如何區分：新的 `game_id`、新的三處哨點、新 NPC、本輪 `variant_id`、本輪回鈴與繩結表象共同區分；上一局的人物、地點與損壞不帶入。
- NPC：每個新 `game_id` 重新生成所有 `<NPC#…: 姓?-名?>`；同局續跑固定。不同局同一編號只代表勤務功能槽位。
- 地點：每局生成 `<LOC#1: 名?-哨>`、`<LOC#2: 名?-哨>`、`<LOC#3: 名?-哨>`；三者均為飛鷲派外圍巡路上的普通可替代哨點，不是飛鷲崖或其他唯一正典地點。
- `PROP#1`：本輪異常回鈴組件；每局在銅舌／木舌兩種普通材質中生成，功能與判定不變。
- `CLUE#1`：回索上的提前受力痕；每局在磨亮纖維／新勒痕／錯位結節三種表象中生成，固定證據功能是「有人在正常傳訊前拉動過回索」。
- `variant_id`：玩家看到第一處哨點的回鈴前鎖定 `V-A` 或 `V-B`。
- 歷練：每個新 `game_id` 可依實際結局取得，無跨局次數上限；同局只結算一次。
- 銀兩：門內勤務補貼每局最多支付一次；無跨局上限。
- 門派相對名譽：合資格者依每局 ending 重覆結算，無跨局上限。
- 武學、資格、師承、特殊裝備：本劇本完全不提供，無可刷取項目。
- 跨劇本關係：不得作任何其他劇本必要前置；本輪生成的哨點、NPC、鈴件、結局不升格為跨劇本固定正史。

## 任務實例化
GM 記錄：`game_id`、`variant_id`、三個 LOC 名稱、NPC 姓名、`PROP#1` 材質、`CLUE#1` 表象。合法上下文若已知上一輪映射，本輪避開即可，不主動翻查未載入舊 save。

## 出發前整備
- 整備類型：一般。
- 安全空檔：24小時（48個空檔回合）。
- 開始位置：飛鷲派巡路網上一處普通補給集結點；不固定專名，玩家只需知道它距第一哨約半個時辰。
- 可正常取得：門派勤務乾糧、飲水、普通繩索、火種、禦寒披風、基礎修繕工具；這些只按正常補給取得，不自動生成玩家永久物品。
- 已知限制：正式傳訊密碼、值哨簿正本與專用回鈴器件不得帶離勤務線；門外同行者不因參與本任務取得門派內務權限。
- 玩家已知行程：三個哨點沿同一外圍巡路分布，完整往返約三個時辰；今晚第一封正式急報在申時後一個時辰進入本線。
- 危機關係：正式接令後有4小時調查窗口；24小時整備不消耗本輪危機時鐘。
- 是否可快速略過逐項確認：否；路段含斷崖與強風區，玩家應明確確認飲水、繩索、禦寒及實際隨身配置。
- 出發前交接：逐名確認隨身、坐騎／載物、留存位置與攜帶格；勤務用查哨木籌由門內發給並須在結局交回。

## 玩家可知門內勤務
最近三輪例行試訊中，三個外圍哨點總有一處回鈴比正式傳訊早半刻。早鈴沒有造成真正失聯，因此門內尚未停線；但今晚有一封急報要走同一路由，若回鈴仍不可信，接力信使可能在錯誤時點離哨。

飛鷲派要求隊伍查明「提前回鈴」的原因、讓今晚急報安全通過，並交回足以讓值哨制度自行修正的證據。這是純內務勤務，不要求對外打擊任何勢力。

門內勤務補貼為隊伍20兩；若只保住今晚急報但未查明制度問題，10兩。

## GM 世界真相
早鈴確由門內勤務失誤造成，沒有外敵陰謀。每個變體都保留飛鷲派「天高路遠，同伴不能丟」的制度壓力：值哨人怕自己成為斷鏈的一環，於是用錯方法掩蓋問題。

### `V-A` 趕交接
`<NPC#1: 姓?-名?>` 是第二哨的年輕值哨，近來腳傷未癒，怕自己按正常時刻出發會拖慢整線。他每次提前半刻拉回索，讓下一哨以為信使已離站，自己再搶時間趕路。`<NPC#2: 姓?-名?>` 是第三哨值守，發現時序不對卻替同伴掩護，只把問題寫成風勢延誤。今晚若照做，`<NPC#1>` 會在強風段與真正急報信使錯身，造成半個時辰無人守哨。

### `V-B` 鈴件補修失當
`<NPC#3: 姓?-名?>` 是第一哨臨時修繕人。他更換 `PROP#1` 時把回索張力調得過緊，強風會讓鈴舌自行抖響。為免被記一次失職，他沒有報修，而是要求兩名值哨把早鈴當成「風聲」。今晚若風勢增強，鈴會連續誤響，第三哨可能錯誤派出備援信使，與正式信使同走狹窄索道。

## NPC
### `<NPC#1: 姓?-名?>` — 第二哨值哨
- `V-A`：早鈴直接責任者；知道自己的腳傷與提前拉索，不知道今晚急報確切內容。
- `V-B`：普通值哨；曾注意強風時無人拉索也會有短鈴，是替代目擊來源。
- 核心利益：保住自己在巡路中的可靠評價，不想成為拖累同伴的人。

### `<NPC#2: 姓?-名?>` — 第三哨值守
- `V-A`：知情掩護；相信只要沒真正失聯就不算大錯。
- `V-B`：無辜；保存兩次早鈴的時刻記錄。

### `<NPC#3: 姓?-名?>` — 臨時修繕人
- `V-A`：普通修繕人，可證明回索機械狀態正常。
- `V-B`：補修失當者；願意在玩家先展示機械證據後承認，並能現場重新調張力。

## 核心線索
- `CLUE#1` 提前受力痕：合理查看即可發現回索在正式傳訊前曾受力；直覺或智力DC15可分辨「人手急拉」與「風壓反覆扯動」的差別方向，但單一痕跡只縮小可能，不能獨斷責任。
- 值哨時刻表：三哨公開勤務時刻並排後，可見早鈴永遠領先半刻；不需判定。
- 哨間視線：天候正常時，相鄰哨可用旗號確認「人已離哨／仍在哨」，提供不依賴鈴聲的冗餘驗證。

### `V-A` 專用來源
必要結論「有人為趕路提前拉索」至少有：
1. `<NPC#1>` 靴內的新藥布與路段步時不相容，證明他確實無法按正常速度趕線；
2. `CLUE#1` 顯示一次明確急拉後立刻放鬆，與風壓反覆受力不同；
3. `<NPC#2>` 的私下時刻記錄寫著「鈴先、人後」，可在其值哨板背面找到。
`V-B` 中藥布不構成異常，且「鈴先、人後」記錄不存在。

### `V-B` 專用來源
必要結論「鈴件張力過緊、強風自鳴」至少有：
1. 無人拉索時，玩家可在強風來時現場看到 `PROP#1` 自行抖響；
2. `<NPC#1>` 曾兩次在哨內看見繩索無人而鈴響；
3. 修繕記錄顯示 `PROP#1` 更換後才開始出現早鈴，且舊張力標記仍留在固定樁上。
`V-A` 中 `PROP#1` 張力正常，不會自行響。

## 場景
### 1. `<LOC#1>` 第一哨
- 到達：哨頂可見下一段斷崖巡路，回鈴與值哨板都在明處。
- 自動：本輪 `PROP#1` 外觀、時刻表、最近三次早鈴記錄。
- 深入：檢查張力與磨痕需10分鐘；智力DC15可判斷是否異常。失敗只耗10分鐘，取得第二哨對照張力後可重試。
- 高輕功／敏捷可以快速查看外側固定樁；沒有合適移動能力也可用哨內安全繩索多花10分鐘下降，不鎖核心線索。

### 2. `<LOC#2>` 第二哨
- `V-A`：可找到 `<NPC#1>` 的腳傷與不合理出發時序。公開質問會使其先否認；若玩家展示受力痕加一項時刻矛盾，他會承認。
- `V-B`：`<NPC#1>` 可提供無人拉索仍聽見早鈴的目擊。
- 強行扣人不會自動找出原因；若玩家把全部值哨都綁起，今晚急報仍需有人接力，必須自行安排合法替代。

### 3. `<LOC#3>` 第三哨與強風段
- `V-A`：若不修正排班，`<NPC#1>` 會在急報前提前離哨。玩家可改由另一值哨接力、陪同信使、或要求他留哨。
- `V-B`：強風到來時回鈴會再次誤響。玩家可先重新調張力、暫停使用回鈴改用旗號雙確認，或安排一人守索觀察。
- 不可能主張：單靠一次大聲吶喊跨完整斷崖路段取代正式訊號；距離與風勢不足以可靠傳遞。

## 危機時鐘
- 0：正式接令。
- +1小時：三哨知道有人覆查勤務。
- +2小時：`V-A` 的 `<NPC#1>` 開始準備提前出發；`V-B` 風勢增強，鈴偶發短響。
- +3小時：若問題未處理，值哨人會按自己的錯誤補救方案行動。
- +4小時：正式急報進線；`V-A` 形成無人守哨空窗，或 `V-B` 形成雙信使同走狹段風險。

## 偏離與冗餘
- 玩家直接停用所有回鈴：合法，但必須建立旗號／人員雙確認才能安全送急報；否則只是移除工具。
- 任一 NPC 離場：兩個變體各自仍有機械痕跡＋時刻／現場驗證來源。
- 玩家先護送急報：可以保住今晚傳訊，再回查制度原因；若證據在之後仍完整，可達完整完成。
- 玩家公開羞辱值哨人：不影響客觀證據，但會使口供合作變差；不能因此臨時增加或扣除持久名譽倍率。

## 結局與結算
### `EGB-E1`「三哨同聲」
- 結算類型：完成結局。
- 條件：今晚急報安全通過，且隊伍以至少兩個獨立來源查明本變體的真正早鈴原因並留下可執行修正。
- 基礎歷練：`[辨明早鈴機制]2 + [保全急報接力]2 + [建立可執行修正]1 = 5`。
- 完整歷練參與條件：角色須實際參與檢查哨線、查證人／機械、護送或修正急報接力至少一項主要行動，並在+4小時前承擔勤務風險或時間成本。
- 銀兩：20兩實際取得；隊伍共同持有、可分割，由玩家決定分配。
- 物質：查哨木籌、回鈴器件、勤務簿均歸飛鷲派；角色只可暫時保管並須交回。
- 飛鷲派相對名譽：合資格角色 `m=+0.5`，R18，`+9`點；不合資格同行者0。
- 俠名／惡名：0。

### `EGB-E2`「急報已過」
- 結算類型：完成結局。
- 條件：保住今晚急報，但未能充分查明早鈴責任／機制，只留下可供下輪門內檢修的部分證據。
- 基礎歷練：`[保全急報接力]3 + [保留部分證據]1 = 4`。
- 完整歷練參與條件：角色須直接參與今晚急報接力、護送、停鈴改制或危機處置。
- 銀兩：10兩實際取得；共同持有、可分割。
- 物質：所有門內物件交回。
- 飛鷲派相對名譽：合資格角色 `m=+0.2`，R18，`+3.6`點；不合資格者0。
- 俠名／惡名：0。

### `EGB-E3`「巡線斷刻」
- 結算類型：失敗結局。
- 條件：急報因本輪問題延誤或信使遇險，但隊伍成功救下人員或保全能直接指出故障／失職來源的證據。
- 基礎歷練：`[救援／保全證據]2 + [確認早鈴成因片段]1 = 3`。
- 完整歷練參與條件：角色須在危機前已介入，或在失敗發生時直接參與救援／證據保全。
- 銀兩：無；未取得。
- 物質：門內器件與紀錄交回。
- 飛鷲派相對名譽：`m=0.0`，0點。
- 俠名／惡名：0。

### `EGB-E4`「交回查哨木籌」
- 結算類型：任務放棄／提前撤離。
- 條件：隊伍撤離且沒有完成急報安全方案。
- 基礎歷練：已實際確認一項早鈴客觀異常者得`[確認異常]1`，否則0。
- 完整歷練參與條件：角色須親自參與至少一處哨點查驗並取得該資訊。
- 銀兩：無；未取得。
- 物質：木籌與暫持器件交回。
- 飛鷲派相對名譽：`m=0.0`，0點。
- 俠名／惡名：0。

## GM 最終覆核摘要
本任務是飛鷲派純內務可重覆勤務；沒有固定正典人物出場、沒有把飛鷲崖作可變事件地點、沒有武學來源獎勵。每局重生 NPC／外圍哨點／非必要線索表象，兩個變體各自有完整因果與冗餘。門派相對名譽只對合資格飛鷲派角色按每局實際 R 與 ending 倍率結算。