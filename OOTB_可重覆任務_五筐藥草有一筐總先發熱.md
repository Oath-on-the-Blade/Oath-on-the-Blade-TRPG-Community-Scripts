# 五筐藥草有一筐總先發熱

## 劇本規格
- `script_id`: `ootb-repeatable-warm-herb-basket`
- 劇本版本：1.0.1
- 運行類別：可重覆任務；`replay_policy: repeatable`
- 重跑基型：日常可再發生；劇本規模：小型；連續性：independent；冒險分類：一般冒險
- 建議人數：3–5名；建議等級：2–4級；歷練基準等級 R=3；預計2–3小時
- 地區：嶺南道普通藥材集散鎮；本次藥棧與人物每局實例化
- 可能的社會名譽變化：嶺南道民間；俠名／惡名依公開處置
- 需要引用的內容庫條目：拿手「醫毒藥理、格物推理、感應、洞察、說服、巧手」；相對名譽對象「嶺南道民間」

## 重跑與實例化
每個新 `game_id` 生成另一宗普通藥材收存事故：藥棧 `<LOC#1: 名?-藥棧>`、棧主 `<NPC#1: 姓?-名?>`、收貨夥計 `<NPC#2: 姓?-名?>`、採藥人 `<NPC#3: 姓?-名?>`、買家跑腿 `<NPC#4: 姓?-名?>` 全部重新生成。`PROP#1` 為五筐同批乾草藥，`PROP#2` 為其中一筐受影響藥草，`PROP#3` 為筐底半乾竹墊；`CLUE#1` 為筐底水汽與微酸氣味，`CLUE#2` 為入棧秤重記錄，`CLUE#3` 為昨夜遮雨搬筐次序。可變外觀每局重生成，同局狀態持續保存。這是普通藥材收存事故；上一宗解決不會阻止另一批貨再次因錯誤墊料發生同類問題。本 `script_id` 無武學、引薦或永久特殊物獎勵；每局報酬與名譽只按當次事件結算。

新 `game_id` 必須重新實例化全體 NPC 的可變姓名欄、`LOC#1` 名稱，以及所有標為可變的 `PROP#`／`CLUE#` 非必要表象；同一 `game_id` 暫停／續跑時保留原映射及之後的持有、破壞、轉移、公開狀態。若目前合法載入的上下文已知上一實例的姓名、地名或形態，新實例應避開完全照抄；不得為此主動搜尋、讀取或翻查未載入的其他舊 save。不同 `game_id` 中相同的 `NPC#`、`LOC#`、`PROP#`、`CLUE#` 只代表相同功能槽位，並非同一世界人物、地點、物件或痕跡改名換外觀；所有玩家可見帳頁、口供、描述與結局須跟隨本局映射同步。

## 出發前整備
- 整備類型：一般；安全空檔：24小時（48個空檔回合）
- 開始位置：本次集散鎮客棧／住處
- 可正常取得：日常食水、普通繩布、紙墨、小刀與一般藥材市場服務
- 已知限制：藥棧要求今日午前判定哪筐可交貨；不可任意混筐或把可疑藥材當成可食用藥材帶走
- 玩家已知：全程鎮內，步行往返，不跨日；整備期不推進危機
- 可快速略過逐項確認：是；出發前逐名固定隨身、載物、留存位置與攜帶格

## 開場、真相與時間線
`<NPC#1>` 清晨發現五筐昨日下午入棧的乾草藥中，`PROP#2` 摸起來微溫，擔心整批受潮霉變；午前買家會來驗貨。委託隊伍在不毀掉可用貨物的前提下找出原因、隔離風險並判定其餘四筐是否可交。

真相：昨夜驟雨時 `<NPC#2>` 把五筐移到內牆，唯獨 `PROP#2` 底下墊了剛從雨棚收回、表面擦乾但內部仍濕的 `PROP#3`。筐底濕氣令局部藥材重新吸水並開始發酵升溫。`<NPC#3>` 採收與初乾沒有問題；`<NPC#4>` 昨晚曾提前看貨，只翻過最上層，沒有動筐底。若無人介入，`<NPC#1>` 因怕賠款把五筐全部低價退貨，採藥人與藥棧互相指責。

時間線：昨日下午五筐乾燥合格入棧；入夜驟雨，`<NPC#2>` 搬筐並臨時換墊；半夜 `PROP#2` 底層吸濕；清晨升溫被發現；午前買家到場。

## 人物認知與行動
- `<NPC#1>`：知道入棧時五筐外觀正常，不懂升溫原因；利益是保住信用又避免賣壞藥。先想整批退貨；見到可重現證據後願意只隔離一筐。
- `<NPC#2>`：知道昨夜換過墊，誤以為竹墊擦乾便無事；怕被扣工錢，所以最初只說「都搬過」。若問搬運細節或出示濕痕便坦白。
- `<NPC#3>`：知道採收、曬乾時間；害怕被誣作交濕貨。能提供同批另一袋自留樣作獨立比較。
- `<NPC#4>`：知道昨晚只翻上層，怕被指調包；願意說出當時五筐都不熱。

## 線索網
核心A「問題在入棧後形成」：`CLUE#2` 記載五筐入棧重量相近且乾燥；`<NPC#4>` 昨夜看貨時五筐均不熱；`<NPC#3>` 的同批自留樣乾燥正常。

核心B「只有筐底受濕並開始發酵」：合理掀看即可見上層乾、下層微潮；直覺（感應）DC10可先察覺筐底微酸氣味與熱源集中；智力（醫毒藥理）DC11可判斷是受潮後發熱而非毒性自行生熱。失敗只耗10分鐘，仍取得客觀濕熱分布。

核心C「濕竹墊是水分來源」：移筐即可見 `PROP#3` 下側仍濕；智力（格物推理）DC10比較五個筐底，只有此墊留下同形水印；`<NPC#2>` 的搬運說法與 `CLUE#3` 可互證換墊時間。

## 主要場景與方法
### 藥棧貨間
到達時五筐分列，`PROP#2` 已被棧主用空筐隔開。合理查看、摸筐外、比較位置不需判定。取底樣需先把上層分裝到乾布，耗10分鐘；敏捷（巧手）DC10可避免混樣，失敗會混掉一小把樣本但不毀掉其餘線索。力量直接翻倒可立即看筐底，但會混合上下層，使該筐只能整筐隔離。

智力（格物推理）DC20可在不拆筐下依五筐位置、熱點與水印推斷熱源在筐底，成功只定位檢查位置；失敗耗15分鐘。高骰不能證明不存在的下毒者，也不能令已受潮藥材恢復乾燥。重試須取得新樣本、搬開筐或得到搬運記錄。

### 後棚與帳案
`CLUE#3` 掛在後棚值事板，合理閱讀即可知道昨夜換過一塊墊。以魅力（說服）DC10向 `<NPC#2>` 說明只需重建搬運次序，可令其完整交代；失敗令其先要求棧主在場，取得濕墊後可重試。既有嶺南道民間正相對名譽或較高俠名可令棧主先保證不以一次疏忽私刑夥計，`<NPC#2>` 因而直接合作；惡名很高者則使其更怕承認，但物證仍可取得。

## 危機
開場後每30分鐘一格，共4格至買家到場。第2格棧主開始準備整批退貨；第3格採藥人要求封存五筐；第4格買家到。只要隊伍已隔離 `PROP#2` 並提出一項「其餘四筐乾燥正常」的客觀來源，買家可延後半小時驗收，不要求先解完全部因果。

## 終局與結算
### `ending_id: warm_basket_isolated`｜只退一筐
結算類型：完成。條件：確認受潮來源、隔離 `PROP#2`、保住其餘四筐並留下換墊記錄。完整參與者基礎歷練：調查2、江湖1，共3。棧主支付隊伍共10兩，實際取得、可分割，由玩家分配；無其他物質獎勵。若處理結果由藥棧、採藥人與買家共同傳開：嶺南道民間 `+0.3x E`；俠名 `+0.1x E`；惡名 `0.0x E`。

### `ending_id: warm_basket_safe_hold`｜先封貨再驗
結算類型：部分完成。條件：未完全找出濕墊因果，但正確隔離一筐並證明其餘四筐暫無同樣濕熱。基礎歷練：調查1、江湖1，共2。報酬共6兩，實際取得、可分割；嶺南道民間 `+0.1x E`，俠名／惡名 `0.0x E`。

### `ending_id: warm_basket_all_lost`｜五筐一併退回
結算類型：失敗。若未能建立安全隔離，五筐均退貨。基礎歷練：調查1，共1。無報酬；若隊伍明知只有一筐有客觀異常仍公開誣稱採藥人整批造假，嶺南道民間 `-0.2x E`、惡名 `+0.1x E`；否則均 `0.0x E`。

## 偏離與失效
提前猜中可直接搬筐驗墊；`<NPC#2>` 離場仍有水印、濕墊與記錄；濕墊被丟掉仍可由熱點、筐底水印、搬運記錄與口供重建。若玩家故意把五筐全部混合或浸水，已無合理方法證明四筐安全，只能進失敗結局。任何局都不生成唯一寶物、永久特殊傷患或跨劇本前置。