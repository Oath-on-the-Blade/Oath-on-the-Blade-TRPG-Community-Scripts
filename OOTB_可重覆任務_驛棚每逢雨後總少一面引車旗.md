# 《驛棚每逢雨後總少一面引車旗》

## 劇本規格頭
- `script_id`: `ootb-repeatable-poststation-missing-guide-flag-v1`
- 劇本版本：1.1.0
- 運行類別：可重覆任務
- `replay_policy: repeatable`
- 連續性：`independent`
- 重跑基型：日常可再發生
- 冒險分類：一般冒險
- 類型：驛路安全／器材追索／短時限調查
- 建議玩家人數：1–4 人
- 建議角色等級：2–4 級
- 歷練基準等級 R：3
- 劇本規模：小型（S=1）
- 基準總歷練 B：2
- 預計遊戲時間：1–2 小時
- 主要地區：河洛道普通山路驛棚；每個新 `game_id` 使用另一個具名實例
- 武力層級：低；不預設戰鬥
- 正式社會名譽對象：俠名、惡名、【河洛道民間】
- 重要武學／永久特殊裝備：無
- 主要物質報酬：每個 `game_id` 全隊最多 5 兩工食錢
- 需要引用的內容庫條目：無

---

## 1. 任務重跑規則

雨後、濃霧或夜間改道時，地方驛棚會臨時在岔路、泥坡與窄橋掛引車旗。旗子可能因風雨脫落、被趕車人移去警示別處，或在收隊時送錯棚。本模板每次運行都是**另一宗同類世界事件**：另一個驛棚、另一班當值人、另一名趕車人、另一面普通引車旗與另一批受影響車隊；上一局的旗不會復原、上一局 NPC 不會改名後再次出場。

### 世界內再次發生原因
河洛道山路雨後臨時改道與引車本來就是重複性外務；不同地方驛棚都可能因臨時救急、交接失敗或風雨令一面普通引車旗失位。處理完一宗事件不會消滅下一宗同類事故發生的條件。

### 每次事件如何區分
每個新 `game_id` 都保存獨立的世界日期、NPC 姓名、具名驛棚與岔路、旗面表象、雨勢、受影響車隊與該局結局。江湖履歷應理解為多次處理相似外務，而不是同一件事重跑。

### 跨劇本關係
本任務不得成為任何其他劇本的必要前置，不得把任何特定 `game_id`、`ending_id`、NPC、LOC、PROP 或 CLUE 當成後作必要開場條件。其他劇本若提及「山路上常有這類旗務事故」，只能是非前置背景。

### 重覆獎勵上限
- 基礎歷練：每個新 `game_id` 依該局實際結局結算一次；同一角色跨局無本劇本次數上限，仍先按角色等級折算，再套個人加成。
- 工食錢：每局全隊最多 5 兩；每次都是新外務，跨局無累積上限。
- 【河洛道民間】正向相對名譽：同一角色由本 `script_id` 最多取得 **3 次**正向結算；第 4 次起正向倍率改為 `0.0x E`。負向變化只在新的公開可歸因惡行實際成立時按該局結算，不重算舊事。
- 俠名正向：同一角色由本 `script_id` 最多取得 **1 次**；之後本任務俠名正向倍率為 `0.0x E`。惡名若由新的公開可歸因行為造成，可再次結算。
- 武學、資格、師承、引薦、永久特殊裝備：永不授予。

---

## 2. 任務實例化

在玩家接觸第一條有意義線索前，GM 必須固定並保存以下映射：

### 2.1 基本實例鍵
- 本次 `game_id`。
- 本次世界日期／時段與雨勢：例如晨雨剛停、夜雨後起霧、午后驟雨初歇；只改表面環境，不改核心真相或危機節奏。
- 本次受影響車隊表象：商車／糧車／木料車之一；只改敘事，不改機械。

### 2.2 NPC 功能槽位
- `<NPC#1: 姓?-名?>`：驛棚當值人。
- `<NPC#2: 姓?-名?>`：昨夜最後一名收旗腳夫。
- `<NPC#3: 姓?-名?>`：趕車人，昨夜曾移動旗子。

新 `game_id` 重新生成三人的全部可變姓名欄；同一 `game_id` 暫停／續跑保留原姓名。若合法上下文已知上一實例姓名，新局避開，不為此翻查未載入舊 save。不同 `game_id` 的同一 `NPC#` 只是相同劇本功能槽位的不同世界人物，不是同一人改名。

### 2.3 具名地點槽位
- `<LOC#1: 名?-驛棚>`：本次地方驛棚。
- `<LOC#2: 名?-岔口>`：本次泥坡岔路。
- `<LOC#3: 名?-窄橋>`：本次橋頭／沖溝旁窄橋。

`名?` 只生成專名核心，固定尾綴不重複。新 `game_id` 重新生成全部 LOC 名稱；同局保留。玩家可見告示、路牌、口供與任務紀錄全部同步使用本輪已保存映射。不同 `game_id` 的同一 LOC 只是場景功能槽位，不是同一地點改名，上一局的封路、毀損或修復不得帶入。

### 2.4 `PROP#` 關鍵實體槽位

#### `<PROP#1>`｜失位的普通引車旗
- 固定功能：昨夜原本設在 `<LOC#2>`，被 `<NPC#3>` 移到 `<LOC#3>` 警示新沖坑，今晨倒入橋邊灌木；它能把「有人主動移旗」與「移旗用於救急」兩段因果連起來。
- 不可變資料：普通驛路引車旗；所有權屬本次 `<LOC#1>` 驛務；無特殊價值；旗桿長度足以留下扛桿／落桿痕跡；最後位置固定為 `<LOC#3>` 附近灌木。
- 可變表象：旗面主色從青／黃／白三者選一；補綴位置從上角／下角／旗尾選一；邊緣磨損樣式從毛邊／一處舊裂／兩處針腳選一。這些表象不改證據力度、DC、所有權或真相。
- 同步引用：場景描述、`<NPC#2>` 的記憶、`<NPC#3>` 的口供、找回旗時的描述與結局全部使用本輪已保存表象。

#### `<PROP#2>`｜簡易備旗
- 固定功能：驛棚可借出的臨時導引物，使玩家即使未找回 `<PROP#1>` 也可恢復可靠引導。
- 不可變資料：普通公用備旗、所有權屬 `<LOC#1>`、可正常立在岔口、局末歸還。
- 可變表象：旗面顏色必須與 `<PROP#1>` 不同；可有無補綴。表象不改機械。

新 `game_id` 重新生成所有可變 PROP 表象；同局暫停／續跑保留原映射與其持有、破壞、轉移狀態。不同 `game_id` 的 `<PROP#1>`／`<PROP#2>` 不是同一件世界物品換外觀。

### 2.5 `CLUE#` 線索呈現槽位

#### `<CLUE#1>`｜從岔口往橋頭移動長桿的路徑痕跡
- 支持結論：原旗曾被人主動由 `<LOC#2>` 移向 `<LOC#3>`。
- 不可變事實：痕跡方向由岔口往橋頭，且與單純風吹走不符。
- 可變呈現：本局從「旗桿底端間歇點痕」「扛桿人腳印旁固定距離戳痕」「濕泥上長桿擦過的短弧痕」三者選一。
- 取得方式：在 `<LOC#2>` 觀察 10 分鐘；`直覺（感應）DC10` 成功立即辨出方向。失敗仍看見痕跡但需再花 10 分鐘沿路核對，不能無成本原地重擲。

#### `<CLUE#2>`｜橋頭曾有人急停避坑的痕跡
- 支持結論：移旗目的與橋頭新沖坑有關。
- 不可變事實：昨夜有車／牲口在坑前急偏，時間與雨後新坑一致。
- 可變呈現：從「車輪急偏的弧形轍」「牲口蹄印在坑前散開」「拖繩在泥上斜拉一道新痕」三者選一。
- 取得方式：到 `<LOC#3>` 即可看見基本痕跡；`智力（格物推理）DC12` 可在 10 分鐘內把它與新坑、旗位連成因果。失敗只得到「昨夜有人在此急避障礙」，再配合 `<NPC#3>` 或 `<PROP#1>` 仍可成立。

新 `game_id` 重新生成可變 CLUE 呈現；同局固定。玩家可見描述、口供與結局不得殘留未選中的預設形態。不同 `game_id` 的同一 CLUE 只是線索功能槽位，不是同一條痕跡改外觀。

---

## 3. 開場與整備

晨雨剛停，`<NPC#1>` 發現三面引車旗只收回兩面。再過 90 分鐘，第一批載重車會經 `<LOC#2>`；若缺少清楚引導，車隊可能走進被雨沖軟的舊路。

玩家目標不是一定找回原旗，而是確認原旗去向並在車隊到達前恢復可靠引導。

### 整備
- 整備類型：`突發`。
- 開始位置：`<LOC#1>` 驛棚前。
- 安全空檔：0 小時；沒有完整空檔回合。開場危機已發生，角色只能使用當下實際隨身物與場內明列可借資源。
- 玩家已知行程：全部場景都在驛棚、相鄰岔口與窄橋，步行往返不足 30 分鐘，不跨日；普通食水可在驛棚取得，不構成補給壓力。
- 場內可借：`<PROP#2>`、普通繩、竹竿、油布、粉筆。這些是世界內本來就在驛棚的公用物，不是追溯塞入角色背包；局末按實際歸還／消耗結算。
- 出發交接：逐名確認實際隨身物、攜帶格、坐騎／車輛承載與留存位置；0 小時不得回溯購物、製作、補滿或假定「合理所以有帶」。

### 既有社會名譽
- 較高【河洛道民間】相對名譽可令 `<NPC#1>` 願意立刻把備旗與驛棚工具交玩家暫借，不需先證明信用；低名譽角色仍可借，但 `<NPC#1>` 會記下保管者。
- 顯著惡名會令普通車夫避免單獨與角色爭執，但不令 `<NPC#3>` 自動招供；真相仍須靠物證／說服。

---

## 4. GM 真相

昨夜 `<NPC#3>` 趕一輛空車經過時，看見 `<LOC#3>` 橋頭有新沖出的坑。原本掛在 `<LOC#2>` 的 `<PROP#1>` 正好在附近，他擅自把旗移到橋頭警示後車，卻沒有回 `<LOC#1>` 報告。

夜班 `<NPC#2>` 收旗時只走原路，因此以為旗被風吹走。今晨風又把旗桿吹倒，`<PROP#1>` 落進橋邊灌木。原岔口舊路則因一夜積水變軟，若沒有引導，重車容易陷輪。

這不是偷竊案，而是一次合理救急加一次失敗交接。玩家即使從未找到 `<PROP#1>`，仍可用 `<CLUE#1>`、`<CLUE#2>`、NPC 口供與軟路實況完成任務。

---

## 5. 真實時間線與危機

### 真實時間線
- 昨夜雨最急時：`<LOC#3>` 橋頭形成新沖坑。
- 稍後：`<NPC#3>` 看見坑，將 `<PROP#1>` 從 `<LOC#2>` 移到 `<LOC#3>`。
- 收旗時：`<NPC#2>` 按舊路線只找到另外兩面旗。
- 天亮前：風把 `<PROP#1>` 吹倒進灌木。
- 開場：`<NPC#1>` 發現少旗；90 分鐘後第一批重車抵達。

### 危機時鐘
每 30 分鐘 1 格：
0. 車隊尚遠；舊路已有積水。
1. 第一名先行腳夫抵近岔口。
2. 車隊可望見岔口；若無旗或人手，會按舊習走軟路。
3. 首輛重車進入岔口。若未引導，車輪陷入軟泥，後車堵塞；不強迫造成死亡。

只要玩家已立好 `<PROP#2>`、找回並重設 `<PROP#1>`、派人指路或以繩／竹竿形成明確安全線，危機時鐘仍走到車隊抵達，但不再自動觸發陷輪。

---

## 6. 線索網與人物

### 必要結論 A：原旗曾被人主動移動
至少有：
1. `<CLUE#1>` 指向橋頭。
2. `<LOC#2>` 原旗孔完整、沒有風折斷桿的碎片；直接檢查即可成立。
3. 夜間路過者／先行腳夫可回憶橋頭後來出現過與本輪 `<PROP#1>` 表象一致的旗影。

### 必要結論 B：移旗是為警示橋坑
至少有：
1. `<CLUE#2>`。
2. `<NPC#3>` 在看到 `<PROP#1>` 或被告知「主要問題是交接，不是偷竊」後會承認救急移旗。
3. `<PROP#1>` 最終倒落位置就在橋坑警示視線附近，旗桿方向與風吹倒後落點相符。

### 必要結論 C：原岔口舊路現在不宜重車
至少有：
1. 表層積水下是深軟泥；用驛棚竹竿探入即可直接確認。
2. `直覺（生存）DC10`，5 分鐘，可判斷重車進入會有高陷輪風險；失敗仍能得出保守結論「應先封舊路再觀察」。
3. 本地車夫知道該段雨後曾有陷輪紀錄；此背景只支持風險，不證明誰移旗。

### NPC 行動
- `<NPC#1>`：重安全與交接；若玩家先恢復引導，立即提供驛務記錄與工具。
- `<NPC#2>`：怕被扣工錢，只隱瞞自己沒有繞橋檢查；不知道誰移旗。用 `魅力（說服）DC10` 可令其立刻說明收旗路線；失敗多耗 10 分鐘並需先看驛務記錄。
- `<NPC#3>`：怕被說成偷旗。玩家指出橋坑或承認其救急動機時無需判定便會承認；若直接指控偷竊，可用 `魅力（說服）DC12` 分開「救急」與「未交接」兩項責任。失敗不刪真相，仍可由物證成立。

低骰只增加耗時、需要第二來源或使證詞較保守，不刪核心資訊。

---

## 7. 可行方案與偏離

### 恢復引導的典型方法
- 找回 `<PROP#1>` 並重新立於安全位置：找到後耗時 10 分鐘，自動成立。
- 借 `<PROP#2>` 立備旗：兩人協作或有竹竿／繩時自動成立，耗時 10 分鐘。
- 留一名角色或驛工現場指路：自動成立，但該人被綁定在 `<LOC#2>`，直到車隊通過或找到替代標記。
- 用繩、粉筆與竹竿劃安全線：`智力（格物推理）DC10`，10 分鐘；成功建立清楚引導，失敗只表示標記容易誤讀，需再花 10 分鐘改成立旗或人工指路。

### 偏離處理
- 玩家先去橋頭：可直接遇到 `<CLUE#2>` 與 `<PROP#1>` 落點，不必先訪談。
- 玩家先猜「有人為救急移旗」：`<NPC#1>` 會允許直接查橋頭與收旗路線；證據吻合即可縮短流程。
- `<NPC#3>` 離場／被打昏：A、B 仍可由 `<CLUE#1>`、`<CLUE#2>`、`<PROP#1>` 與旗位成立。
- `<PROP#1>` 被水沖走或玩家毀掉：仍可用 `<PROP#2>` 恢復引導；A 由 `<CLUE#1>`＋旗孔，B 由 `<CLUE#2>`＋`<NPC#3>`／夜間目擊成立。
- 玩家偷走 `<PROP#2>`：驛棚會派人指路，但該局工食錢取消；被多人看見並拒絕歸還時使用下方負面名譽變體。
- 玩家毆打 `<NPC#3>`：不會自動獲得口供；若公開且造成可傳播傷害，使用負面名譽變體。
- 玩家完全放棄：首車陷泥後驛棚自行封舊路，進 `abandoned`。

---

## 8. 終局與結算

`E` = 角色等級折算後、歷練型絕活／背景／其他個人額外歷練加成之前的實得基礎總歷練。

### `ending_id: route_restored_truth_known`｜角色卡顯示名稱：雨後引路｜結算類型：完成結局
- 成立條件：車隊進入岔口前恢復可靠引導，並建立「旗被移往橋頭救急＋交接失敗」的主要因果。
- 角色完整參與條件：實際參與一次路線安全處置，且實際取得／驗證 A 或 B 至少一項核心來源。只參與安全處置者取得 `行旅 1`；只參與調查者取得 `計略 1`；兩者皆參與取得完整歷練。
- 基礎歷練：`行旅 1、計略 1`，基礎總計 **2**。
- 世界後果：本次車隊安全通過；`<NPC#1>` 把「臨時移旗必須回報」加入本班交接提醒，但不建立全河洛道新制度。
- 銀兩／物質結果：`<NPC#1>` 代表本次驛務當場實際交付 **5 兩**工食錢給隊伍，成為隊伍共同持有、可分割、尚未分配的銀兩；由玩家結算時決定分配。`<PROP#1>` 若找回，所有權仍屬 `<LOC#1>` 並歸還；`<PROP#2>`、繩、竹竿等借用品歸還，正常耗損由驛務承擔。
- 社會名譽：車隊先行腳夫、`<NPC#1>` 與數名趕車人都能辨認角色並知道其避免陷輪，形成可在本地車路傳播的共同評價：俠名 `+0.1x E`、惡名 `0.0x E`、【河洛道民間】`+0.2x E`。俠名若該角色已用本 `script_id` 取得過 1 次正向，改為 `0.0x E`；【河洛道民間】若已取得過 3 次本劇本正向，改為 `0.0x E`。

### `ending_id: route_restored_only`｜角色卡顯示名稱：先把路標回來｜結算類型：完成結局
- 成立條件：在車隊到達前恢復安全引導，但未查清原旗去向或移旗原因。
- 角色完整參與條件：實際參與立旗、人工指路、封舊路或等價安全處置；只在結果完成後加入者為 0。
- 基礎歷練：`行旅 1`，基礎總計 **1**。
- 世界後果：車隊安全通過；驛棚稍後自行追查少旗。
- 銀兩／物質結果：驛務當場實際交付 **4 兩**，隊伍共同持有、可分割、尚未分配；玩家共同決定分配。借用品全部依實際歸還；未找回的 `<PROP#1>` 仍屬驛務的失落公物，不轉成玩家財產。
- 社會名譽：俠名 `0.0x E`、惡名 `0.0x E`、【河洛道民間】`0.0x E`。理由：這被視為完成受託路務，沒有形成額外突出公共評價。

### `ending_id: late_recovery`｜角色卡顯示名稱：車輪陷泥後｜結算類型：失敗結局
- 成立條件：首車已陷入軟路，但玩家之後查到主要真相或協助恢復通行。
- 個人歷練變體：實際查明 A＋B 的角色取得 `計略 1`；實際參與把陷車拖出／建立後續安全引導的角色取得 `行旅 1`；同一角色兩者皆做可取得 `計略 1、行旅 1`，基礎總計最高 **2**。只旁觀者為 0。
- 世界後果：首車延誤、後車堵塞，但不預設死亡；之後驛棚封舊路並重設引導。
- 銀兩／物質結果：若玩家至少完成真相查明或解困之一，驛務實際交付 **2 兩**，隊伍共同持有、可分割、尚未分配；若兩者皆未完成則未取得。借用品照實歸還。
- 社會名譽：一般延誤下俠名 `0.0x E`、惡名 `0.0x E`、【河洛道民間】`0.0x E`。

### `ending_id: abandoned`｜角色卡顯示名稱：岔口照舊｜結算類型：任務放棄
- 成立條件：玩家在恢復可靠引導前明確離開且不再介入。
- 個人歷練：離開前若實際確認 A 或 B 任一核心結論，可取得 `計略 1`；若只完成一項實質臨時安全措施但沒有維持到車隊通過，可取得 `行旅 1`；基礎總計最高 **1**。未實質介入為 0。
- 世界後果：首車可能陷泥，驛棚之後自行封路。
- 銀兩／物質結果：工食錢 **未取得**。所有借用品仍屬 `<LOC#1>`；未歸還形成對驛務的實際追索／債務，不成為合法財產。
- 社會名譽：單純放棄為俠名 `0.0x E`、惡名 `0.0x E`、【河洛道民間】`0.0x E`。

### 公開惡行結算變體（可附著於任何對應結局）
若角色在多人可辨認的情況下偷走 `<PROP#2>` 並拒絕歸還、或無必要地毆打車夫造成傷害，且消息由驛工／車隊傳開：該角色本局俠名 `0.0x E`、惡名 `+0.2x E`、【河洛道民間】`-0.2x E`。這是新一局的新公開行為，負面結算可再次成立；沒有可靠身份歸因時相關負相對名譽為 `0.0x E`。

---

## 9. 重跑與最終結算核對
- 每局的 `<PROP#1>`、`<PROP#2>`、繩、竹竿均屬本次驛務或地方公用物，不轉為玩家永久財產。
- 工食錢是每個 `game_id` 的全隊總額，不複製到每名角色。
- 新 `game_id` 必須重新生成 NPC／LOC／PROP／CLUE 的可變資料；同局所有玩家可見文字使用同一保存映射。
- 本任務沒有 `variant_id`；核心真相每局相同，但人物、地點、事件日期與非必要表象不同，且每局是一宗新的世界事件。
- 本任務不授予武學、門派資格、引薦或永久特殊裝備。
- 達到正向名譽次數上限後，再完成本任務仍可按結局取得歷練與工食錢，但相應正向名譽倍率改為 `0.0x E`。
- 名氣只由俠名＋惡名推導，本劇本不直接給名氣。
