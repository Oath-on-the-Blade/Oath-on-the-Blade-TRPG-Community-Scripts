# 千機峽外翻車架少了一枚限位銷

## 劇本規格
- `script_id: ootb_tianji_referral_repeatable_cart_stop_pin_20260829`
- 劇本版本：1.0.0
- 任務類別：門派引薦可重覆任務（非門派專用任務）
- 運行類別：可重覆任務
- `replay_policy: repeatable`
- 連續性：`independent`
- 冒險分類：一般冒險
- 建議等級：6–10 級
- 歷練基準等級：R=8
- 建議人數：3–5 PC
- 劇本規模：小型
- 預計時間：3–4 小時
- 重跑基型：兩者（日常可再發生＋小型分支）
- 地區：雲東道千機峽外圍商道的一次本輪實例
- 正式相對名譽對象：天機閣、雲東道民間
- 世界知識依據：`世界知識庫/組織/江湖/門派/天機閣.md`
- 需要引用的內容庫條目：`內容庫/拿手.md`、`內容庫/相對名譽對象.md`

## 門派定位
天機閣山門在雲東道千機峽，理念為「器有其主，技有其責」；重要器物應留製作者印記、用途與維修方法，反對無差別陷阱，也反對把機關吹噓成法術。本任務使用的是千機峽外圍常見器械維護與事故記錄職責，不讓固定閣主、七匠或九巧徒出場，也不改寫任何門派傳承。

本任務不是門派專用任務；任何角色皆可受託。合資格角色可取得個人持久獎勵：`天機閣引薦｜可宣告使用以申請入門｜未使用`。引薦只是一個申請機會，不保證入門成功；一份只支援一次正式入門申請，不可轉讓、出售、贈與或共有，同一角色同時最多持有一份有效天機閣引薦。

## 任務重跑規則
千機峽外商道常有車架、滑輪、絞盤與限位件送往各地維修；不同車隊、不同器件與不同工班都可能再次發生「器械少一枚限位銷、卻說不清是遺漏還是臨時拆用」的小事故。每個新 `game_id` 都是另一批人、另一套車架與另一宗事件。

- 所有本劇本 NPC 使用 `<NPC#編號: 姓?-名?>`；新 `game_id` 全部重新生成姓名。
- 本輪專用地點：`<LOC#1: 名?-車棚>`、`<LOC#2: 名?-坡道>`、`<LOC#3: 名?-料棚>`；新 `game_id` 重新生成專名，類型固定。
- `<PROP#1>` 為本輪限位銷的識別記號，每局從「尾端兩點銼記／銷帽半月缺口／中段一道紅漆環」選一；功能固定，只識別本輪器件。
- `<CLUE#1>` 為拆取限位銷留下的工具痕，每局從「鉗口平行夾痕／細錘敲擊點／銷孔旁薄刮痕」選一；均可協助判斷是按程序拆取還是受力滑脫，不改 DC。
- GM 在玩家接觸第一條有意義線索前鎖定並保存 `VAR-A`／`VAR-B`，不得依玩家猜測改換真相。
- 同一 `game_id` 暫停／續跑時，NPC、LOC、PROP、CLUE、variant、器械狀態與公開記錄全部保留。
- 不同 `game_id` 的同一功能 ID 只代表功能槽位，不是同一人物、地點或器件改名。
- 本任務不得成為任何後作必要前置；某輪 NPC、LOC、PROP、CLUE、variant 與 ending 不得升格為唯一正史。

### 可重複獎勵與上限
- 基礎歷練：每個新 `game_id` 完整參與可正常取得，無累積上限；同一 `game_id` 不可重領。
- 隊伍銀兩：每宗新事故可按結局取得，無累積上限。
- 俠名：同一角色由本 `script_id` 累積正向增加最多 +6 點；達上限後本劇本正向俠名倍率為 `0.0×E`。
- 天機閣、雲東道民間正向相對名譽：同一角色由本 `script_id` 對每個對象各累積最多 +6 點；超出只補至 +6；負向結果仍依實際公開歸因結算。
- 天機閣引薦：已是天機閣正式成員者不能取得；已有有效未使用天機閣引薦者不能取得第二份。曾使用而入門失敗／引薦失效者，只能在新的 `game_id` 再次完整參與並重新符合本篇條件後取得新一份。
- 替代獎勵：合資格但已是門人、已有有效引薦，或自願不取引薦的角色，可選 3 兩個人酬金，或由本輪外務程序支付一次當日食宿與普通器材維修耗材補給；兩者擇一。這不是引薦的銀兩價格，也不是開局免費整備配給。

## 任務實例化
1. 記錄 `game_id`。
2. 生成 `<NPC#1>` 天機閣外圍器械記錄員、`<NPC#2>` 車隊領工、`<NPC#3>` 修車匠、`<NPC#4>` 商旅目擊者姓名。
3. 生成 `<LOC#1>`、`<LOC#2>`、`<LOC#3>` 專名。
4. 鎖定並保存 `<PROP#1>`、`<CLUE#1>` 本輪形態。
5. 鎖定並保存 `VAR-A`／`VAR-B`。
6. 記錄每名 PC 的正向名譽剩餘上限與天機閣成員／引薦持有狀態。

## 引薦發放權限
`<NPC#1>` 只是本輪外圍器械記錄員，沒有自行發薦權。他只負責把事故原因、器件印記、修復過程、參與者姓名與個人表現寫進外務考核紀錄。千機峽外務制度允許把在器械事故中能先保護旁人、尊重器物用途與所有權、如實區分設計責任與操作責任、並能用可核驗方法完成交接的外來江湖人，提交給有權審核入門引薦的門內考核單位。批准者不作本任務出場 NPC，也不固定為某位世界知識庫人物。

符合個人條件者由 `<NPC#1>` 提交紀錄，門內批准後以蓋印回條確認，GM 才把 `天機閣引薦｜可宣告使用以申請入門｜未使用` 寫入角色卡。取得引薦不自動成為門人、不通過入門判定、不取得免費門派整備、不取得任何 `[來自天機閣]` 武學來源、不提高武學解鎖上限，也不因「拿到引薦」本身增加天機閣相對名譽。

## 開場與委託
一支商隊在 `<LOC#1>` 準備把可翻轉卸貨的木車架送過 `<LOC#2>`，點檢時發現控制翻架角度的一枚限位銷不在原位。若貿然上坡，車架受力後可能突然翻下。`<NPC#1>` 已要求停車，請 PC 在下一批商旅到達前查明限位銷是按程序拆下、被臨時挪用，還是因受力滑脫，並確保車架恢復到可核驗狀態。

- 查清原因並安全恢復／封存車架：隊伍 18 兩。
- 未找回限位銷，但查清原因並令危險車架停止使用、完成書面交接：隊伍 12 兩。
- 車架、限位銷、工具與貨物均有既定所有權，不屬戰利品。

## 共通 NPC
### `<NPC#1: 姓?-名?>` 天機閣外圍器械記錄員
知道：車架上有用途與維修印記；限位銷缺失時不得載重翻架。不了解本輪原因。核心利益：把責任寫清楚，不讓器械被當成「邪門機關」。會記錄 PC 是否先停用危險器械、是否保留痕跡、是否如實交接。

### `<NPC#2: 姓?-名?>` 車隊領工
VAR-A：知道曾命人臨時拆銷救另一台卡死車架，但忘了把取用寫入工單。VAR-B：確信沒有人拆銷，卻忽略昨日車架曾超載。
核心利益：保住交期與工班資格。證據明確後願交出完整工單。

### `<NPC#3: 姓?-名?>` 修車匠
VAR-A：親手按程序拆下 `<PROP#1>`，拿去救急，後暫放 `<LOC#3>`；以為領工會補記。VAR-B：只做過普通上油，沒有拆銷；知道超載會使銷孔磨偏。

### `<NPC#4: 姓?-名?>` 商旅目擊者
VAR-A：看見修車匠拿一枚短金屬銷取道具棚。VAR-B：昨日看見車架載貨明顯超出平日，轉彎時曾猛震一下。

## 真相變體
### VAR-A｜臨時挪用未補記
**真相**：另一台車架昨夜卡住，`<NPC#2>` 口頭同意 `<NPC#3>` 依程序拆下 `<PROP#1>` 暫作同規格替件；故障排除後，限位銷被放在 `<LOC#3>` 分格盒，兩人都以為對方會補工單與裝回。這是交接失誤，不是偷竊或機關失控。

**時間線**：故障車卡死 → 口頭批准拆銷 → 留下 `<CLUE#1>` 的正常拆取痕 → 替件救急 → 銷暫放料棚 → 危機 3 時另一工班準備把分格盒送走 → 危機 4 時本車架若仍未停用，會在載貨試翻時失去限位。

**必要結論：限位銷是按程序工具拆取，事故在於記錄與交接中斷。**
- `<CLUE#1>` 對稱、受力方向正常，沒有滑脫拉傷。
- 工具架缺少的鉗／細錘與痕跡吻合，且已在工單登記昨夜使用。
- `<NPC#4>` 看見 `<NPC#3>` 持短金屬件往 `<LOC#3>`。

### VAR-B｜超載磨偏滑脫
**真相**：沒有人拆銷。昨日領工為趕一趟貨，讓車架超載翻轉一次；銷孔受側向力磨偏，`<PROP#1>` 在回程震動中逐步滑出，最後落在 `<LOC#2>` 坡腳草溝。`<NPC#2>` 不知道銷已滑脫，只以為異響來自車輪。

**時間線**：超載翻架 → 銷孔磨偏 → 回程連續震動 → `<PROP#1>` 滑脫落在坡腳 → 危機 3 時下一批貨上架 → 危機 4 時若未停用，翻架受力時會突然越過安全角度。

**必要結論：限位銷自行滑脫，根因是超載造成銷孔偏磨。**
- `<CLUE#1>` 呈單向拉磨而非成對工具夾痕。
- 銷孔一側有新鮮偏磨，與昨日超載方向一致。
- `<NPC#4>` 可證昨日超載與猛震；坡腳可找到 `<PROP#1>`，表面有連續擦痕而非工具壓痕。

兩個 variant 的決定性線索互相停用：VAR-A 不存在「銷孔單側偏磨＋坡腳滑脫銷」；VAR-B 不存在「對稱工具痕＋料棚分格盒」。

## 出發前整備
- 整備類型：自訂。
- 安全空檔：1 小時，共 2 個空檔回合。
- 開始位置：`<LOC#1>` 車棚外。
- 世界內原因：下一批商旅尚有一個時辰到達，停車區暫時安全；之後道路壅塞與裝貨壓力上升。
- 可正常取得：食水、乾糧、普通繩索、木楔與一般修車工具；`<NPC#1>` 可借量尺、粉筆和一把普通鉗具，任務後歸還。
- 玩家已知：三個主要地點相距不遠；車架未復核前不得載重試翻；任務不跨日。
- 危機：安全窗口後每額外 30 分鐘危機 +1。
- 休息：不自動提供短休／長休。
- 出發交接：逐角確認攜帶格、借用品、工具與留存位置，確認未超載。

## 既有社會名譽
- 天機閣正向相對名譽：`<NPC#1>` 願直接開放器械用途印記與昨夜工具登記；負向者仍可查，但需在其面前逐項核對，多耗 10 分鐘。無論名譽多高，都不能跳過實際器械安全檢查。
- 雲東道民間正向相對名譽：商旅願意配合暫停通行並先提供昨日車隊異常目擊；負向者仍可由 `<NPC#1>` 作保取得核心資訊，多耗 10 分鐘。
- 高俠名可令 `<NPC#2>` 較早交出完整工單；高惡名則使其先要求 `<NPC#1>` 在場。均不作責任證據。

## 危機時鐘
0. 車架停用，痕跡完整。
1. 商旅開始聚集，領工催促恢復通行。
2. 第二批貨到棚外，若沒有明確警戒會有人準備裝車。
3. VAR-A：料棚分格盒準備被送往別處；VAR-B：下一批貨開始上架。
4. 若 PC 未停用／封存車架，載重試翻會越過安全角度，造成貨物傾落與旁人受傷風險；若已設警戒，危機 4 只代表交期損失，不強迫事故發生。

## 場景一：車架點檢
- 自動資訊：限位銷缺失；器械上有用途與維修印記；未插銷時翻架角度沒有可靠硬限位。
- 智力（格物推理）DC13，10 分鐘：判斷 `<CLUE#1>` 偏向工具拆取或受力滑脫；失敗耗時 20 分鐘，只能確定「痕跡不是近期鏽蝕自然脫落」。取得標準同規格銷孔對照後可重試。
- 直接成立：用木楔、繩索把翻架物理固定並掛停用標記，可先消除危機 4 的人身事故風險，但不能恢復正常運行。
- 不能成立：只靠高骰、名氣或「懂機關」宣告車架安全；缺少限位件或等效實體限制時不能載重試翻。

## 場景二：工單與料棚
- 智力（識字）DC12，10 分鐘：從工具登記、口頭補記空欄與批次時間找出 VAR-A 的交接缺口，或 VAR-B 沒有任何拆銷登記；失敗只多耗 10 分鐘由 `<NPC#1>` 說明欄位，不封鎖必要資訊。
- VAR-A：到 `<LOC#3>` 合理搜索 10 分鐘可找到分格盒內 `<PROP#1>`，不需擲骰。
- VAR-B：料棚沒有本輪限位銷；不得為了迎合猜測臨時生成一枚。

## 場景三：坡道
- VAR-B：直覺（生存）DC13，15 分鐘可沿昨日車轍與金屬擦痕找到坡腳草溝；失敗耗時 30 分鐘且危機 +1。取得 `<NPC#4>` 指出猛震位置後可直接縮小到草溝。
- VAR-A：坡道只留下正常車轍，沒有滑脫金屬痕；這是反證，不應額外生成假線索。
- 敏捷可加快在邊坡取回 `<PROP#1>`；用繩索固定後可直接安全下坡，多耗 10 分鐘。

## 場景四：責任與修復交接
- VAR-A：找回原銷並確認銷孔無變形後，可由具備工具的人按原用途裝回，再由 `<NPC#1>` 見證空載低角度試動；若銷孔受損則不得硬裝。
- VAR-B：即使找回原銷，偏磨銷孔仍不安全。正確處置是停用、固定、記錄「需更換／修正銷孔」，不能把原銷插回就宣稱修好。
- 兩個 variant 都要求把原因與處置寫入交接紀錄；這是天機閣理念「器有其主，技有其責」的具體實踐。

## 引薦個人條件
只有達成完成結局後，個別角色同時符合以下條件，才可被 `<NPC#1>` 提交引薦考核紀錄：
1. 實際參與至少一項器械點檢、痕跡判讀、危險隔離、證據交接或安全修復；
2. 在知道車架可能失限後，沒有故意推動載重冒險試翻；
3. 沒有偷取、私藏或毀掉本輪器件／工單以牟利；
4. 在最終交接中如實區分「器械設計／磨損」「操作／超載」「記錄／交接」責任，不把不確定事項冒充確定；
5. 願意讓本輪處置與姓名寫入外務考核紀錄。

符合者可選天機閣引薦或本篇替代獎勵；各角色獨立選擇。

## 偏離與邊界
- 提前指控偷銷：兩個 variant 都未預設偷竊；需依痕跡修正。
- 原銷毀損／找不到：仍可藉銷孔、工具記錄、工單、目擊與坡道痕跡重建原因，進入安全封存結局。
- `<NPC#2>` 或 `<NPC#3>` 離場：物證與另一 NPC／目擊仍足以重建必要結論。
- 私製一枚替代銷：只有材料、尺寸、強度與用途確有世界內依據，且經 `<NPC#1>` 見證檢查，才可能作臨時等效件；不能憑「會機關」無成本造出合格關鍵件。
- 引薦不等於武學來源；本篇不提供任何天機閣武學、秘笈或解鎖上限。

## 結局
以下 `E` 為角色等級折算後、個人額外歷練加成前的實得基礎總歷練；正向名譽再套用本 `script_id` 累積上限。

### END-01｜角色卡顯示名稱：器責分明｜完成結局
- `ending_id: tianji_cart_pin_clear`
- 成立：查清本輪 variant 的真正原因，車架被安全恢復或正確封存，並留下可核驗交接紀錄。
- 物質：18 兩實際交付，隊伍共同持有、可分割，由玩家結算時分配；器械與工具返還權利人。
- 俠名／惡名：俠名 `+0.3×E`；惡名 `0.0×E`。
- 相對名譽：天機閣 `+0.4×E`，前提是角色本人在外務紀錄中被可靠歸因為完成點檢／安全處置者；雲東道民間 `+0.3×E`，由商旅見證避免事故與恢復道路秩序。未被可靠歸因的角色該項為 0。
- 引薦：符合「引薦個人條件」且資格允許的角色，可選 `天機閣引薦｜可宣告使用以申請入門｜未使用`；不取者按「可重複獎勵與上限」選一項替代獎勵。
- 歷練：計略 2、江湖 2；基礎總歷練 4。
- 完整參與：至少參與一個核心調查／器械處置場景及最終交接。

### END-02｜角色卡顯示名稱：封架待修｜完成結局
- `ending_id: tianji_cart_pin_safe_hold`
- 成立：未能找回限位銷或未能完全判明責任，但 PC 正確識別危險、停止載重使用並完成可核驗封存交接。
- 物質：12 兩實際交付，隊伍共同持有、可分割。
- 俠名／惡名：俠名 `+0.2×E`；惡名 `0.0×E`。
- 相對名譽：天機閣 `+0.2×E`，只給被記錄為安全封存處置者；雲東道民間 `+0.2×E`。
- 引薦：本結局不提供引薦，因責任與器械因果尚未完整到足以形成入門背書；已有門人／引薦者也無替代個人獎勵。
- 歷練：江湖 2、計略 1；基礎總歷練 3。
- 完整參與：實際參與危險識別與封存交接。

### END-03｜角色卡顯示名稱：失限翻架｜失敗結局
- `ending_id: tianji_cart_pin_failure`
- 成立：PC 明知缺失限位仍推動載重試翻，或放任危險車架在無警戒狀態進入危機 4，造成貨物傾落／人員受傷風險。
- 物質：0 兩；委託金未取得。
- 俠名：`0.0×E`。
- 惡名：若冒險操作與實際受害能可靠公開歸因到 PC，`+0.2×E`，否則 0。
- 相對名譽：可靠歸因時天機閣 `-0.3×E`、雲東道民間 `-0.2×E`；未可靠歸因則 0。
- 引薦：無。
- 歷練：江湖 1；基礎總歷練 1。
- 完整參與：至少參與一次處置場景。

## GM 最終核對
- 本篇是門派引薦可重覆任務、非門派專用；小型、`repeatable`、`independent`。
- 天機閣定位只使用「器有其主，技有其責」及器械責任，不讓固定高層成為 repeatable NPC。
- 新 `game_id` 重新實例化 NPC／LOC／PROP／CLUE；兩個 variant 在第一條有意義線索前鎖定。
- 引薦由制度性有權單位批准，普通記錄員只遞交；引薦不等於入門或武學來源。
- 每個合資格角色至少有一項非引薦替代個人獎勵；同派有效引薦上限為一份。
- 18 兩隊伍酬勞位於 R=8、小型一般冒險物質收益參考帶內。