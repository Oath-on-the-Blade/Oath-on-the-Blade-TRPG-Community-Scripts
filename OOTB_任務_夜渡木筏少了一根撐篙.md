# 夜渡木筏少了一根撐篙

## 劇本規格
- `script_id: ootb_repeatable_night_ferry_missing_pole_20260829`
- 劇本版本：1.0.1
- 運行類別：可重覆任務
- `replay_policy: repeatable`
- 連續性：`independent`
- 冒險分類：一般冒險
- 建議等級：6–10 級
- 歷練基準等級：R=8
- 建議人數：3–5 PC
- 劇本規模：小型
- 預計時間：2.5–3.5 小時
- 重跑基型：兩者（日常可再發生＋小型分支）
- 地區：江南道支流渡口的一次本輪實例
- 正式相對名譽對象：江南道民間
- 需要引用的內容庫條目：`內容庫/拿手.md`、`內容庫/相對名譽對象.md`

## 任務重跑規則
這不是同一根撐篙反覆失蹤。江南道支流各渡口每日都有夜筏、備篙、纜繩與臨時擺渡；不同渡口、不同筏主、不同乘客都可能再次發生「夜渡前少了一根備用撐篙」的事件。

- 每個新 `game_id` 代表另一宗世界內事件。
- 所有本劇本 NPC 使用 `<NPC#編號: 姓?-名?>`；新 `game_id` 全部重新生成姓名。
- 本輪專用地點：`<LOC#1: 名?-渡>`、`<LOC#2: 名?-蘆灣>`、`<LOC#3: 名?-茶棚>`；新 `game_id` 重新生成專名，類型固定。
- `<PROP#1>` 為本輪缺失撐篙的識別特徵，每局從「柄尾兩圈麻線／篙頭鐵箍缺一角／中段刻三道淺槽」選一；功能固定，只用來識別同一根篙。
- `<CLUE#1>` 為從筏棚到岸邊的移動痕跡，每局從「濕泥拖痕／碎蘆葉沾水線／砂面長直壓痕」選一；均證明撐篙曾被移往水邊，不改 DC 或證據力度。
- GM 必須在玩家接觸第一條有意義線索前鎖定並保存 `VAR-A` 或 `VAR-B`，不得根據玩家猜測或擲骰改換真相。
- 同一 `game_id` 暫停／續跑時，NPC、LOC、PROP、CLUE、variant，以及物件取得、移動、毀損、公開與保管狀態全部保留。
- 若合法上下文已知上一實例的姓名、地名、PROP 或 CLUE 形態，新 `game_id` 應避免完全照抄；不得為此搜尋未載入的舊存檔。
- 不同 `game_id` 的同一功能 ID 只是相同功能槽位，不是同一人物、地點、物件或痕跡改名。
- 玩家可見告示、簿冊、口供與描述凡引用本輪 NPC／LOC／PROP／CLUE，全部同步使用本輪已保存映射。
- 本任務不得成為其他劇本的必要前置；某輪 NPC、地名、撐篙、variant 與 ending 不得升格為後作唯一正史。

### 可重複獎勵與上限
- 基礎歷練：每個新 `game_id` 完整參與可正常取得，無累積上限；同一 `game_id` 不可重領。
- 銀兩：每個新 `game_id` 可按結局取得，無累積上限；每宗渡口事件是獨立工錢。
- 俠名：同一角色由本 `script_id` 累積正向增加最多 +6 點；達上限後本劇本正向俠名倍率視為 `0.0×E`。
- 江南道民間正向相對名譽：同一角色由本 `script_id` 累積最多 +6 點；達上限後本劇本正向倍率為 `0.0×E`。負向結果仍依可靠公開歸因結算。
- 本劇本不提供唯一資格、武學、特殊裝備或其他永久唯一獎勵。

## 任務實例化
1. 記錄本次 `game_id`。
2. 生成 `<NPC#1>` 筏主、`<NPC#2>` 夜班幫工、`<NPC#3>` 趕夜路貨客、`<NPC#4>` 茶棚照看者姓名。
3. 生成 `<LOC#1>`、`<LOC#2>`、`<LOC#3>` 專名。
4. 生成並保存 `<PROP#1>`、`<CLUE#1>` 本輪形態。
5. 鎖定並保存 `VAR-A`／`VAR-B`。
6. 記錄每名 PC 本 `script_id` 的俠名與江南道民間正向名譽剩餘上限。

## 開場與委託
`<LOC#1>` 今夜要在二更前送一批藥材與三名旅客過河，點筏時卻發現兩根撐篙只剩一根。單篙仍能勉強操筏，但遇急流無法安全轉向。`<NPC#1>` 請 PC 在天完全黑前找回撐篙，或至少查明去向並安排安全替代。

- 找回撐篙並查清原因：隊伍 18 兩。
- 未找回但能安全阻止危險夜渡並查清去向：隊伍 12 兩。
- 撐篙、木筏與藥材均有既定所有權，不屬戰利品。

## 共通 NPC
### `<NPC#1: 姓?-名?>` 筏主
知道：黃昏前兩根篙都在；夜渡若只靠一根篙，急彎時風險明顯增加。想要：不要誤渡，更不要翻筏。願把筏棚、繫具與昨晚記錄交 PC 查看。

### `<NPC#2: 姓?-名?>` 夜班幫工
VAR-A：曾把篙借給附近漁人推陷泥小船，對方尚未歸還；怕被筏主責罵而隱瞞。VAR-B：沒有外借，但黃昏把篙靠牆未綁緊，後被上游漂木撞落水；怕被扣工錢。

### `<NPC#3: 姓?-名?>` 趕夜路貨客
知道：黃昏經過筏棚時看見 `<NPC#2>` 在整理篙；VAR-A 看見一名陌生漁人扛長杆往 `<LOC#2>`；VAR-B 聽到水邊重物落水聲。想要：準時過河，但若證明不安全會接受延期。

### `<NPC#4: 姓?-名?>` `<LOC#3>` 茶棚照看者
知道：誰在黃昏後經過，以及 `<LOC#2>` 水勢何時開始加快；不知道完整原因。

## 真相變體
### VAR-A｜私借未還
- `variant_id: VAR-A`

**真相**：`<NPC#2>` 把 `<PROP#1>` 撐篙借給一名臨時漁人，用來把陷在淺泥的舟推回水道；漁人之後在 `<LOC#2>` 看到自己的舟纜將斷，先去補纜，來不及把篙送回。他沒有偷竊意圖。

**時間線**：借篙 → 漁人扛往 `<LOC#2>` 留下 `<CLUE#1>` → 漁舟陷泥 → 補纜拖延 → 危機 3 時水漲把篙沖離岸 → 危機 4 時流入下游深灣。

**必要結論：撐篙被正常拿出筏棚，並由借用者帶往蘆灣。**
- 筏棚門栓無破壞，掛篙繩被正常解開。
- `<CLUE#1>` 從棚口連到水邊，有人步跡與長杆痕並行。
- `<NPC#3>` 看見陌生漁人扛長杆往 `<LOC#2>`。

### VAR-B｜鬆靠落水
- `variant_id: VAR-B`

**真相**：`<NPC#2>` 收棚時沒有把 `<PROP#1>` 綁回篙架，只斜靠在外牆；上游一段漂木撞岸，令撐篙滑落水中，之後順緩流漂到 `<LOC#2>` 蘆叢卡住。沒有人借或偷篙。

**時間線**：撐篙斜靠外牆 → 漂木撞岸 → 篙落水留下 `<CLUE#1>` → 順流漂向 `<LOC#2>` → 危機 3 時水位上升鬆開蘆叢 → 危機 4 時漂入深灣。

**必要結論：撐篙由外力落水，沒有人把它扛走。**
- 篙架掛繩仍打着原結，表示撐篙未從架上正常解下；外牆有長杆滑痕。
- `<CLUE#1>` 從牆根直接到水線，無平行人足跡。
- `<NPC#3>` 黃昏後聽到一次重物落水，沒有看見搬篙者。

兩個 variant 的決定性線索互相停用：VAR-A 不出現「掛繩原結＋直接落水」；VAR-B 不出現「有人步跡與長杆痕並行＋陌生漁人目擊」。

## 出發前整備
- 整備類型：自訂。
- 安全空檔：1 小時，共 2 個空檔回合。
- 開始位置：`<LOC#1>` 筏棚前。
- 世界內原因：二更前仍有一小段查找與備渡時間，之後水勢和夜色都會加重風險。
- 可正常取得：食水、乾糧、燈油、普通繩索；筏主可借一盞風燈與一條牽拉繩，任務後歸還。
- 玩家已知：三個主要地點均在步行半時辰內；河邊濕滑；任務不跨日。
- 危機：安全窗口後每額外 30 分鐘危機 +1。
- 休息：窗口不自動提供短休／長休；若休息，世界時間正常流逝。
- 出發交接：逐角確認攜帶格、借用品、繩索與留存位置，確認未超載。

## 既有社會名譽
- 江南道民間正向相對名譽：`<NPC#4>` 願先說出黃昏後經過的人與水勢時段，節省 10 分鐘；負向者仍可由 `<NPC#1>` 作保取得同一核心資訊。
- 高俠名可讓 `<NPC#2>` 更相信 PC 會按實責處理，首次質問時不先逃避；惡名高者使其先保持距離。均不改變物證。
- 合理水路人脈可直接知道 `<LOC#2>` 是附近長杆、漂木最容易卡住的回水處，但仍需實際到場找篙。

## 危機時鐘
0. 撐篙仍可追回。
1. 夜色加深，河岸搜尋時間增加。
2. 河水上升，`<LOC#2>` 蘆邊開始沒過腳踝。
3. 撐篙即將鬆脫；若無人處理，下一級被帶離蘆灣。
4. 撐篙漂入下游深灣，本夜無法安全追回；夜渡必須取消或另找合格替代。

## 場景一：筏棚
- 自動資訊：篙架、掛繩、外牆、水線與 `<CLUE#1>` 可查看。
- 直覺（生存）DC13，10 分鐘：判斷 `<CLUE#1>` 是「人帶長杆離開」還是「長杆滑落水」；失敗耗時 20 分鐘，但仍可確定撐篙曾離開棚前。取得 `<NPC#3>` 目擊後可重試。
- 智力（格物推理）DC12，10 分鐘：檢查掛繩是否被正常解過；失敗只無法判定結法時序，由 `<NPC#1>` 提供日常繫法後可直接再查。
- 不能成立：只看「篙不見」不能推定偷竊；兩個 variant 都允許篙離棚但因果不同。

## 場景二：茶棚與詢問
- 有一項物證後，魅力（說服）DC13、5 分鐘可令 `<NPC#2>` 完整交代自己最後如何處理撐篙；失敗使其暫停談話。新增 `<NPC#3>` 的獨立目擊或找到撐篙後可直接重談。
- `<NPC#4>` 提供時段資訊，不知道責任答案；不得把其口供擴張成「看見真相」。

## 場景三：蘆灣
- 自動資訊：到場後，合理搜索 10 分鐘即可找到本輪與 variant 相符的篙位置或最後卡痕；必要結論不鎖在單骰。
- 敏捷（身法）DC13，1 分鐘：沿濕滑岸石快速抵近篙位；失敗會滑退並耗時 10 分鐘，不直接造成永久傷害。改用繩索固定、繞行緩坡或等同安全方法可直接成立，但多耗 10–20 分鐘。
- 力量（體能）DC13，1 回合：在危機 3 把將鬆脫的撐篙從水流中拉回；失敗篙移遠一段，危機 +1。兩人用繩協作可直接成功。
- 不可能：沒有接觸、繩索或可達落腳點時，不能只靠遠處高骰把篙「吸」回岸上。

## 安全夜渡決策
即使沒有找回撐篙，PC 仍可避免事故：
- 找到另一根長度、強度合格的普通撐篙並由 `<NPC#1>` 實際檢查，可替代本輪缺失篙；這需要世界內真實來源，不可憑空生成。
- 若沒有合格替代，說服或強迫冒險夜渡都不能令單篙安全性問題消失。公開指出急彎與回水風險後，`<NPC#3>` 等乘客會接受延期。

## 偏離與邊界
- 提前指控 `<NPC#2>` 偷竊：兩個 variant 都不支持「永久竊取」；需按物證修正結論。
- 撐篙被沖走：仍可依筏棚痕跡、目擊與水勢重建原因，進入安全延期結局。
- `<NPC#2>` 逃離：筏棚、`<CLUE#1>`、`<NPC#3>` 仍可支持必要結論。
- 私吞找回的篙：屬侵占筏主財物，不計獎勵，並可形成負向名譽。

## 結局
以下 `E` 為角色等級折算後、個人額外歷練加成前的實得基礎總歷練；所有正向名譽再套用本 `script_id` 累積上限。

### END-01｜角色卡顯示名稱：夜筏復篙｜完成結局
- `ending_id: night_ferry_pole_restored`
- 成立：查清本輪原因並在危機 4 前找回 `<PROP#1>` 撐篙，夜渡恢復安全條件。
- 物質：18 兩實際交付，隊伍共同持有、可分割，由玩家結算時分配；撐篙返還 `<NPC#1>`。
- 俠名／惡名：俠名 `+0.3×E`；惡名 `0.0×E`。
- 相對名譽：江南道民間 `+0.4×E`，由筏主、乘客與茶棚形成可核驗傳播。
- 歷練：江湖 2、計略 2；基礎總歷練 4。
- 完整參與：至少參與一個調查場景及蘆灣處置／最終安全決策。

### END-02｜角色卡顯示名稱：停渡保平安｜完成結局
- `ending_id: night_ferry_safe_delay`
- 成立：撐篙未能及時找回，但查清去向／原因，並阻止不安全單篙夜渡或取得合格替代。
- 物質：12 兩實際交付，隊伍共同持有、可分割。
- 俠名／惡名：俠名 `+0.2×E`；惡名 `0.0×E`。
- 相對名譽：江南道民間 `+0.3×E`，由筏主與乘客知道 PC 主動避免不安全夜渡並形成地方傳播。
- 歷練：江湖 2、計略 1；基礎總歷練 3。
- 完整參與：實際參與查因或安全決策。

### END-03｜角色卡顯示名稱：夜筏冒險｜失敗結局
- `ending_id: night_ferry_risk_ignored`
- 成立：原因未查清，且 PC 明知單篙風險仍推動夜渡，造成筏主取消委託或實際險情。
- 物質：0 兩；委託金未取得。
- 俠名／惡名：俠名 `0.0×E`；只有在公開可可靠歸因到 PC 推動明顯不安全夜渡並造成實際受害時，惡名 `+0.2×E`，否則 `0.0×E`。
- 相對名譽：符合上述公開可靠歸因時江南道民間 `-0.3×E`；否則 `0.0×E`。
- 歷練：江湖 1；基礎總歷練 1。
- 完整參與：至少參與一次處置場景。

## GM 最終核對
- 本篇是小型、`repeatable`、`independent`；每次重跑都是另一宗渡口事件。
- NPC／LOC／PROP／CLUE 均按新 `game_id` 實例化，兩個 `variant_id` 在第一條有意義線索前鎖定。
- 每個 variant 的必要結論至少三個相互一致來源；決定性線索互相停用。
- 可重複正向俠名與江南道民間名譽各有 +6 累積上限；無唯一永久獎勵。
- 18 兩主要酬勞位於 R=8、小型一般冒險物質收益參考帶內。