# 三車炭有一車總先濕

## 劇本規格
- `script_id`: `ootb-repeatable-wet-charcoal-cart`
- 劇本版本：1.0.1
- 運行類別：可重覆任務
- `replay_policy: repeatable`
- 重跑基型：小型分支
- 劇本規模：小型
- 連續性：independent
- 冒險分類：一般冒險
- 建議人數：3–5名；建議等級：2–4級；R=3；2–3小時
- 地區：江南道普通河港聚落
- 可能社會名譽：江南道民間；俠名／惡名依結局
- 需要引用的內容庫條目：拿手「格物推理、洞察、感應、巧手、體能、說服、威嚇」

## 可重覆實例化
每個新 `game_id` 都是另一宗炭車受潮糾紛。第一條有意義線索前鎖定 `variant_id=V1_漏篷` 或 `V2_調包`，不得因玩家猜測切換。重新生成 `<LOC#1: 名?-渡>` 及 `<NPC#1: 姓?-名?>` 炭行掌事、`<NPC#2: 姓?-名?>` 趕車人、`<NPC#3: 姓?-名?>` 河棚主、`<NPC#4: 姓?-名?>` 糾紛人。新局若合法上下文已知上一實例姓名／地名／形態則避開，不越權搜尋舊存檔；同一 `game_id` 暫停續跑保留全部映射。不同局同一 NPC／LOC 編號只是功能槽位，不是同一人物或地點。

關鍵槽位：`PROP#1` 問題炭車（固定功能：承載受潮／調包痕跡；車色、篷布顏色、普通木料每局生成）、`PROP#2` 扣繩（固定功能：留下重結／受力痕跡；繩色與普通纖維表象每局生成）、`CLUE#1` 水線／濕度分布、`CLUE#2` 重結壓痕、`CLUE#3` V1篷角磨痕或V2手車輪泥、`PROP#3` V2被調換的兩筐炭（只在V2啟用；筐形與非關鍵外觀每局生成，惟「與本批炭種不同」的證據功能固定）。新局重生成可變表象；同局持有、破壞、公開狀態持續保存；玩家可見帳記與描述跟隨映射。永久獎勵上限：無武學、引薦或永久特殊物；銀兩、歷練與名譽可按每宗新事件正常取得。

## 出發前整備
一般；24小時（48空檔回合）；開始於 `<LOC#1>` 附近市集。可正常取得日常食水、普通繩索、油布、燈火、常用工具與客棧服務。玩家已知全程不離渡口一里、約半日、可能翻查濕貨與車篷；整備期不推進本案。可快速略過逐項確認。出發前逐名固定隨身、坐騎載物、留存位置與攜帶格。

## 開場與委託
晨雨後三車同批木炭入渡，`PROP#1` 下層濕透，另兩車乾。`<NPC#1>` 願付 **12兩整隊**，要求午后卸貨前查清水從何來、誰應負責；只排除一名嫌疑人而未解釋受潮則4兩。

## 世界真相
### V1_漏篷
`PROP#2` 前夜鬆脫，斜雨由篷角灌入。`<NPC#2>` 清晨怕扣工錢，重結扣繩並把最濕炭翻到底層。`<NPC#3>` 看見他碰篷角但誤以為收篷；`<NPC#4>` 昨晚確曾爭價怒罵，卻未碰貨。

### V2_調包
`<NPC#4>` 欠 `<NPC#3>` 短債，夜裡借手車，以兩筐自家受潮炭 `PROP#3` 換走同量乾炭；搬動時掀起車側油布。`<NPC#2>` 清晨發現扣法不同，怕被說失職而自行重扣；`<NPC#3>` 只知借車，不知用途。

若玩家不介入，午后炭行與棚主互索賠。兩變體皆為本格人為／物理原因。

## NPC運行
`<NPC#1>` 求止損與保信用，接受可重現證據。`<NPC#2>` V1知疏失、V2只知扣法變過；指出 `CLUE#2` 後說服DC11可完整交代，威嚇DC12亦可但令公開關係惡化。`<NPC#3>` 願讓玩家檢棚；直接問借手車即承認，洞察DC10可先看出遲疑。`<NPC#4>` V1只有爭價；V2在兩項獨立證據成立後先求補價，若公開堵住才逃入市集，不主動拼命。

## 線索網
A「不是棚頂整體漏雨」：`CLUE#1` 只在問題車向內；棚地積水只在外沿（感應／格物推理DC10）；另兩車同棚而乾。B「有人動過問題車」：`CLUE#2` 有新舊壓痕（巧手／格物推理DC11）；`<NPC#3>` 目擊 `<NPC#2>` 清晨碰篷；V2另啟用 `CLUE#3` 輪泥，由車側通向 `<NPC#4>` 貨棚。

V1決定性來源：`CLUE#3` 鬆繩纖維與篷角磨痕可對上（格物推理DC11）；車內濕度本應由上向下遞減卻被翻層。V2決定性來源：`PROP#3` 炭種與本批不同（格物推理DC12或詢問炭販）；`<NPC#4>` 貨棚有相反乾炭碎屑及借車證詞。每個必要結論至少兩個來源；一名NPC離場不鎖死。

## 場景與方法
### 三輛炭車
合理翻看即可取得水線、繩結與炭層外觀。格物推理DC10–12分析，巧手DC11辨重結。失敗耗時30分鐘且資訊不精確；重試需新比較樣本、工具或新證詞。粗暴倒炭入泥會造成可預見約3兩貨損並失去部分原位痕跡。

### 河棚與手車道
有梯具攀梁直接成立；徒手體能DC10，失敗只延誤10分鐘。V2輪泥合理沿地查看自動取得，下一輪卸貨後會被踩亂，但炭種與借車證詞仍在。

### 市集問話
江南道民間正相對名譽可令攤販較快提供見聞；負相對名譽／高惡名令其要求 `<NPC#1>` 作保，只改時間與態度。高骰不能取得NPC不知道的事。

極難但可能：只靠殘餘水痕推回灌水角度，格物推理DC20，成功只定位篷角／車側，不直接得出責任者。不可能：隔街聞出誰調包、以名氣直接證罪。失敗後無實質新條件不得輪流釣骰。

## 危機
接案後每2小時一格：午前買家催問；午后卸貨踩亂地面痕跡；傍晚 `<NPC#4>` 離渡。離渡後仍可用貨棚碎屑、借車證詞與炭種收束。

## 終局
### `ending_id: wet_cart_cause_proved`｜水路查清
完整參與：接案後參與至少一個主要場景且終局仍在隊伍。基礎歷練：調查2、江湖1，共3。12兩整隊實際取得，扣除玩家造成的可證貨損後共同持有，結算時分配。公開以可核驗證據澄清且無濫暴：俠名 `+0.1x E`、江南道民間 `+0.2x E`、惡名 `0.0x E`；私下結案三者 `0.0x E`。V2追回乾炭屬炭行原物，玩家僅暫時保管至交還。

### `ending_id: wet_cart_partial`｜只止住爭執
基礎歷練：調查1、江湖1，共2。4兩整隊實際取得。俠名、惡名、江南道民間均 `0.0x E`。

### `ending_id: wet_cart_wrong_accusation`｜錯案留痕
基礎歷練：調查1，共1。銀兩未取得。若以不足證據公開誣指無辜並傳開：惡名 `+0.1x E`、江南道民間 `-0.2x E`、俠名 `0.0x E`。

## 偏離
提前猜中可直接驗證。NPC死亡／逃走仍有冗餘物證。拒接則事件自行演成索賠。若玩家永久毀掉問題車、棚地痕跡及全部證詞來源，完整原因已無合理來源，只能進部分結局；GM不得憑空生成新證據。
