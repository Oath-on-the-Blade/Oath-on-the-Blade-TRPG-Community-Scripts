# 七盤曬藥有一盤總先返青

## 劇本規格
- `script_id`: `ootb-baicao-repeatable-green-drying-tray`
- 劇本版本：1.0.1
- 運行類別：可重覆任務；`replay_policy: repeatable`
- 重跑基型：日常可再發生；劇本規模：小型；連續性：independent；冒險分類：一般冒險
- 門派專用任務：百草閣
- 門派勤務類型：內務；內務衝突類型：純內務
- 承接資格：隊伍至少1名百草閣正式成員
- 建議人數：3–5名；建議等級：2–5級；歷練基準等級 R=4；預計2–3小時
- 地區：嶺南道萬藥嶺百草閣山門；本次曬藥院每局實例化
- 可能的社會名譽變化：百草閣（門派勤務特別基數R）；俠名／惡名通常0
- 需要引用的內容庫條目：拿手「醫毒藥理、格物推理、感應、洞察、說服、巧手」；相對名譽對象「百草閣」

## 世界基準與重跑
百草閣山門位於嶺南道萬藥嶺，理念為「識毒為救，知生知害」，重視採集、試藥、劑量與解法記錄。本勤務只處理普通藥材炮製記錄，不改變門派正典，也不涉及門派武學授權。

每個新 `game_id` 重新生成山門內普通曬藥院 `<LOC#1: 名?-院>`、當值門人 `<NPC#1: 姓?-名?>`、炮製學徒 `<NPC#2: 姓?-名?>`、雜役 `<NPC#3: 姓?-名?>`、取藥門人 `<NPC#4: 姓?-名?>`。`PROP#1` 為七盤同批普通葉藥，`PROP#2` 為返青的一盤，`PROP#3` 為該盤下方新換的厚竹編盤底；`CLUE#1` 為盤底局部水汽，`CLUE#2` 為翻曬時刻記錄，`CLUE#3` 為昨夜洗盤與晾盤記錄。可變藥材種類由GM從不涉及特殊毒性、且不改變判定結構的普通乾燥葉藥中鎖定一種；若選擇會改變炮製法的材料則不可使用。本 `script_id` 無武學來源、引薦或永久特殊物；門派相對名譽依每宗獨立勤務正常累積。

新 `game_id` 必須重新實例化全體 NPC 的可變姓名欄、`LOC#1` 名稱，以及所有標為可變的 `PROP#`／`CLUE#` 非必要表象；同一 `game_id` 暫停／續跑時保留原映射及之後的持有、破壞、轉移、公開狀態。若目前合法載入的上下文已知上一實例的姓名、地名、藥材表象或其他可變形態，新實例應避開完全照抄；不得為此主動搜尋、讀取或翻查未載入的其他舊 save。不同 `game_id` 中相同的 `NPC#`、`LOC#`、`PROP#`、`CLUE#` 只代表相同功能槽位，並非同一世界人物、地點、物件或痕跡改名換外觀；所有玩家可見炮製簿、口供、描述與結局須跟隨本局映射同步。

## 出發前整備
一般；24小時（48個空檔回合）；開始於百草閣門人休整區。日常食水、普通紙墨、乾布、竹夾與一般炮製小工具可按門規取得；藥材不得帶離山門或自行服用。玩家已知全程山門內、半日可完成；整備期不推進危機，可快速略過逐項確認。出發前逐名固定隨身、留存與攜帶格。

## 開場、真相與時間線
`<NPC#1>` 發現七盤昨日同時開始翻曬的葉藥，今日清晨只有 `PROP#2` 顏色返青、摸起來較軟。兩個時辰後要把合格乾藥送入普通藥庫。勤務要求找出差異、隔離不合格藥材、留下下一班可重現的炮製記錄。

真相：`<NPC#3>` 昨晚洗過七個竹盤，六盤早洗早晾，`PROP#3` 因臨時補編而最後才洗；表面乾時內層竹篾仍含水。`<NPC#2>` 清晨按時翻藥，誤以為七盤條件相同。厚盤底在夜間把濕氣回送給 `PROP#2`，使其返青。無人投毒或偷換。若玩家不介入，`<NPC#1>` 為保守起見把七盤全退回重曬，耽誤一批普通外診備藥。

## 人物
`<NPC#1>` 重視可重現記錄，願意接受任何能分開「藥材問題」與「器具含水」的證據。百草閣正相對名譽可令其直接開放前一週炮製簿；其他合資格隊伍說明勤務需要亦可查閱，只多耗10分鐘。
`<NPC#2>` 相信自己翻曬時刻無誤，害怕被判失職；看到不同盤底水汽後會主動協助復測。
`<NPC#3>` 知道最後一盤晾得最晚，不知道厚竹篾藏水；以魅力（說服）DC10可令其完整重述洗盤次序，失敗只使其先找值事作證，取得 `CLUE#3` 後可重試。
`<NPC#4>` 昨夜經過時見七盤都未放藥，只記得最後一盤仍靠近洗盤水槽，可作獨立時間來源。

## 線索網
A「藥材入盤前同批狀態一致」：收料小樣仍乾燥；`CLUE#2` 顯示七盤同批同時翻曬；`<NPC#4>` 可證昨夜器具處理先於放藥。

B「返青由盤底回潮造成」：合理翻起少量葉片即可見濕軟集中於底層；直覺（感應）DC10察覺水汽集中在盤底；智力（醫毒藥理）DC11比較上下層可判斷是回潮而非新鮮葉片混入。失敗耗10分鐘但仍保留客觀上下層差異。

C「新厚竹盤最後洗、內層未乾」：`CLUE#3` 明列洗盤次序；智力（格物推理）DC10比較七盤厚度與重量可見 `PROP#3` 較重；拆看一處非承重編口可見內層竹篾仍濕。

## 場景與方法
### 曬藥院
七盤可自由比較、換位、稱重。把 `PROP#2` 上下層分樣需乾布與10分鐘；敏捷（巧手）DC10可保持樣本不混，失敗只需重新取另一小份。力量直接傾倒會混層，仍可隔離整盤但失去「底層較濕」這一來源。

以智力（格物推理）DC20只靠盤重與返青分布推斷盤底藏水，成功只定位器具條件，仍需實物確認；失敗耗15分鐘。高骰不能證明不存在的投毒，也不能把返青藥材直接判為可入庫。

### 洗盤處與炮製簿
合理查看即可見六盤舊竹色相近、`PROP#3` 有新補編痕。閱讀 `CLUE#3` 耗10分鐘；智力（格物推理）DC10可把洗盤時間與厚度差串起，失敗只是不立即形成結論。若拆編口，需普通竹夾、10分鐘與敏捷（巧手）DC10；失敗造成一處可修復斷篾，不毀掉濕痕。重試須換工具、取得新記錄或換一處檢查點。

## 危機
領取勤務後每1小時一格。第1格 `<NPC#1>` 準備把七盤全部標成待重曬；第2格取藥人到場。只要玩家先隔離 `PROP#2` 並提出一項其餘六盤乾燥一致的客觀來源，六盤可先入庫，不必等完整責任判定。

## 終局與結算
### `ending_id: green_tray_corrected`｜六盤照收，一盤重曬
結算類型：完成。條件：證明器具回潮、隔離 `PROP#2`、處理 `PROP#3` 並留下「厚竹盤須延長晾乾後再裝藥」記錄。完整參與者基礎歷練：調查2、江湖1，共3。正式承接、實際參與且功績可由百草閣可靠歸因者：百草閣 `+0.5×R=+2點`。俠名、惡名 `0.0x E`。山門不發現銀；普通修盤材料按門規領用，不形成角色物質收益；武學／引薦／特殊物：無。

### `ending_id: green_tray_isolated`｜先隔離返青盤
結算類型：部分完成。未完整找出洗盤次序，但證明只有一盤底層回潮並安全隔離。基礎歷練：調查1、江湖1，共2。百草閣 `+0.2×R=+0.8點`；俠名、惡名 `0.0x E`；其他永久獎勵無。

### `ending_id: green_tray_all_rework`｜七盤全部重曬
結算類型：失敗。未能在期限前分清安全批次。基礎歷練：調查1，共1。若隊伍曾合理隔離一盤但值事另行決定全退，百草閣 `0.0×R`；若明知六盤乾燥仍自行混盤，百草閣 `-0.2×R=-0.8點`。俠名、惡名0；無物質收益。

## 門派外同行與偏離
隊伍至少1名百草閣正式成員即可承接；門派外角色可同行，按實際可歸因功績取得本任務明列的百草閣相對名譽。本任務不授予百草閣武學來源。提前猜中可直接換盤、稱重、拆驗；`<NPC#3>` 離場仍有 `CLUE#3`、盤重與內層濕痕；記錄被毀仍有 `<NPC#4>`、新補編痕與盤底水汽。若七盤被玩家故意混合浸水，已無合理方法保證六盤原本合格，只能進全批重曬結局。