# 河埠錯掛了一面貨旗

## 劇本規格
- `script_id`: `ootb_repeatable_wrong_cargo_flag_v1`
- 劇本版本：1.0.0
- 運行類別：可重覆任務
- `replay_policy`: `repeatable`
- 連續性：`independent`
- 重跑基型：兩者
- 冒險分類：一般冒險
- 建議等級：2–5級
- 歷練基準等級：R=4
- 劇本規模：小型
- 建議人數：3–5名玩家角色
- 預估時間：2–3小時
- 地域：江南道，一處新生成的運河河埠 `<LOC#1: 名?-埠>`
- 需要引用的內容庫條目：`內容庫/拿手.md`【識字】【格物推理】【說服】【體能】
- 可能的社會名譽變化：俠名、惡名、江南道民間

## 每次新 game_id 的實例化
本任務每次都是江南道另一宗新貨運糾紛，不倒帶舊事件。新 `game_id` 必須在玩家接觸第一條有意義線索前鎖定 `variant_id`，再重新生成全體 NPC 姓名、`<LOC#1>` 專名，以及下列關鍵槽位的可變表象；同局暫停／續跑全部保留。若合法上下文已知上一實例名稱或表象，新局避開即可，不主動翻查未載入舊存檔。不同 `game_id` 的同一 NPC／LOC／PROP／CLUE 編號只代表相同功能槽位的不同世界人物、地點、物件與痕跡。

- `<PROP#1>`：被錯掛貨旗；功能固定為指示卸貨棚位。新局從布旗／木牌旗中選一種，顏色與非關鍵邊角破損重新生成；材質差異不改判定。
- `<PROP#2>`：正確貨票；功能固定為記載貨主、船號與棚位。新局可為折頁票／竹夾票，文字內容隨本輪 NPC 與 LOC 映射替換。
- `<CLUE#1>`：改掛痕跡；功能固定為證明貨旗曾在開埠後被人移位。新局可呈新繩結／新釘孔／濕泥擦痕之一，三者證據力度相同。

## 變體
### `variant_id: hurried_porter`
腳夫 `<NPC#2: 姓?-名?>` 為趕在雨前清空棧板，誤把 `<PROP#1>` 移到相鄰棚位；發現後怕扣工錢而沒有報告。貨主 `<NPC#3: 姓?-名?>` 因而認為自己的兩包藥材被另一船偷換。

### `variant_id: debt_switch`
小商販 `<NPC#3: 姓?-名?>` 欠另一貨主錢，故意請腳夫 `<NPC#2: 姓?-名?>` 把 `<PROP#1>` 移位，想讓自己的普通乾貨先被錯裝上離埠船，再藉混亂拖延債主查貨。`<NPC#2>` 只以為是臨時調棚。

未選變體的決定性口供不存在；GM不得因玩家猜測更換變體。

## 玩家入口
河埠管事 `<NPC#1: 姓?-名?>` 請角色在下一班船離埠前查清一宗錯棚爭執。兩批貨已被搬到同一棚口，雙方都拒絕讓船。管事承諾整隊6兩；若在一個時辰內查清並讓貨物各歸其主，再加2兩。

## 出發前整備
- 整備類型：自訂。
- 安全空檔：1小時（2個空檔回合）。
- 開始位置：`<LOC#1>` 管事棚。
- 可取得：清水、粗繩、油紙、普通燈具；無稀有物資。
- 玩家已知：下一班船一個時辰後離埠；拖過時限會造成兩船延誤。
- 出發前交接：逐名確認實際攜帶物。

## 世界真相與時間線
共同事實：今晨兩船依 `<PROP#2>` 登記不同棚位；開埠後 `<PROP#1>` 被移位；兩批貨外包相似，爭執因此爆發。
1. 辰初：`<NPC#1>` 按貨票分棚。
2. 辰中：依已鎖定變體，`<NPC#2>` 移動 `<PROP#1>`。
3. 片刻後：第二批貨到，兩邊都依眼前貨旗卸貨。
4. 開場：雙方封住棚口等待裁斷。
5. 若玩家不介入：一個時辰後船期錯過，兩貨主互相索賠，河埠停工半日。

## 主要 NPC
### `<NPC#1: 姓?-名?>`｜河埠管事
知道原始棚位與貨票制度，不知道誰移旗。核心利益是恢復運作。看到兩項相符證據即接受重排貨物。

### `<NPC#2: 姓?-名?>`｜腳夫
在 `hurried_porter` 中知道自己誤移且害怕扣工；在 `debt_switch` 中知道自己受託移旗但不知道債務目的。若玩家先證明貨物沒有失竊，他較願合作。

### `<NPC#3: 姓?-名?>`｜小商販／貨主
在 `hurried_porter` 中真心懷疑被偷換；在 `debt_switch` 中是移旗發起者，想拖延債務。被出示 `<PROP#2>` 與 `<CLUE#1>` 後，後者會改求私下賠償而不再否認。

## 必要結論與冗餘
### A：兩批貨沒有被偷換
- `<PROP#2>` 的船號與包數可直接對上兩船。
- 包繩上的裝船結法與各船剩餘貨物一致。
- 管事晨間點數記錄與現場總數相符。

### B：貨旗在開埠後被移動
- `<CLUE#1>`。
- 原位置地面仍有與旗座相符的乾痕。
- 鄰棚腳夫見過 `<NPC#2>` 搬旗，但不知道原因。

每個變體內 A、B 均至少有兩個非同一人物來源；即使 NPC 離場仍可完成。

## 主要場景
### 管事棚
合理查看 `<PROP#2>` 自動取得船號、包數與棚位。智力（識字）DC10，10分鐘比對兩張票與晨簿；成功立即確認沒有多出或少掉貨包，失敗耗10分鐘但仍知道應去現場逐包核對；取得任一船主協助後可重試。

### 爭議棚口
合理查看可發現原旗位乾痕與現旗位置不合。智力（格物推理）DC11，10分鐘比較 `<CLUE#1>`、旗座與泥痕；成功確認移動發生在開埠後。失敗不毀痕跡，但耗10分鐘；等雨停或取得燈具後可重試。力量（體能）DC10可在兩名腳夫協助下安全移開壓住票夾的貨包；失敗只耗5分鐘，增加協助者即可重試。

### 腳夫歇棚
魅力（說服）DC10，10分鐘，以「先還原貨物、再談責任」為條件交涉；成功取得 `<NPC#2>` 完整口供。失敗使其要求先看到 A 的任一物證；取得後可重試。江南道民間正向相對名譽可令旁觀腳夫先提供目擊，不替代物證。

## 危機
每20分鐘危機+1，3時船班關艙。危機1雙方停止搬貨；2船家要求先卸爭議貨；3錯過船期。玩家若先證明A，兩邊可先恢復裝卸，危機不再因爭吵額外惡化。

## 偏離
- 玩家直接拆包：可核貨，但須重新封裝，耗10分鐘。
- 玩家威嚇腳夫：可能取得片面口供，不能取代A、B物證。
- 玩家放走任一NPC：物證仍足夠結案。
- 玩家自行賠錢：可暫時解散爭執，但若未還原棚位，仍進入「原因未明」結局。

## 主要結局與結算
### `wrongflag_restored`｜貨棚復位
- 結算類型：完成結局
- 完整歷練參與條件：實際參與至少兩個主要場景，並參與還原A或B至少一項；只參與末段搬貨者取得基礎歷練1點。
- 基礎歷練：計略1、江湖1；總計2。
- 物質：整隊6兩實際取得；時限內完成再有2兩實際取得，玩家自行分配。
- 社會名譽：無固定變化；若公開協調並避免鬥毆，俠名 `0.1x E`、江南道民間 `+0.1x E`。

### `wrongflag_cause_unclear`｜貨走了，原因未明
- 結算類型：完成結局
- 完整歷練參與條件：參與至少兩個主要場景並實際使兩批貨正確離埠。
- 基礎歷練：行旅1；總計1。
- 物質：整隊6兩實際取得；加獎2兩未取得。
- 社會名譽：0。

### `wrongflag_abandoned`｜船期已過
- 結算類型：任務放棄
- 完整歷練參與條件：無完整歷練；曾取得A或B任一可靠結論者得計略1。
- 基礎歷練：總計0（上述個人tag例外另記）。
- 物質：委託金未取得。
- 社會名譽：0。

## 可重覆後果與上限
每次新 `game_id` 是新河埠、新人物、新貨物糾紛。銀兩與歷練可按每宗新事件正常取得；正向俠名與【江南道民間】相對名譽由本 `script_id` 累積各最多 `0.3x E`，達上限後後續成功不再增加；負面名譽仍按實際公開行為結算。任務不產生永久唯一物品或跨劇本必要前置。