# 驛棚每旬都有一袋草料提前發熱

## 劇本規格
- `script_id: ootb_repeatable_warm_fodder_v1`；劇本版本：1.3
- 運行類別：可重覆任務；`replay_policy: repeatable`；劇本規模：小型；連續性：independent
- 重跑基型：日常可再發生＋小型分支；冒險分類：一般冒險
- 建議等級：4–5級；R4；預計1–2小時
- 地區：荊湖道普通官道驛路；本次驛棚 `<LOC#1: 名?-驛棚>`
- 可能社會名譽：【荊湖道民間】、俠名、惡名；結局歷練詳見結局章
- 需要引用：`內容庫/拿手.md`（格物推理、醫毒藥理、生存、體能、說服）

## 新 `game_id` 初始化
在首項有意義線索前完成並保存：重新生成 `<LOC#1>`；重新生成 `<NPC#1: 姓?-名?>` 管事、`<NPC#2: 姓?-名?>` 腳夫、`<NPC#3: 姓?-名?>` 馬販；擲1d2鎖定 `variant_id`：1=`damp_stack`、2=`oil_rag`；再生成下列槽位。
- `PROP#1` 熱草袋：固定功能＝承載本次異常熱源；可變表象＝袋布顏色、補丁位置、普通繩色，從GM自選至少兩種普通外觀中生成，不改重量、危險或證據力度。
- `CLUE#1` 批次記號：固定功能＝把同批三袋連在一起；可變表象＝木牌刻痕／繩結色線二選一。
- A專用 `CLUE#2` 渡口殘留：固定功能＝證明同批草料遇水；可變表象＝泥色／附著普通水草種類可變，不改「同源濕痕」。
- B專用 `PROP#2` 油布：固定功能＝發熱源且可與馬車剩餘布拼合；可變表象＝普通布色與非關鍵污痕可變，材質與油性不可變。
同一 `game_id` 暫停／續跑保留全部姓名、地點、variant、PROP/CLUE映射及其後持有／破壞狀態。若合法上下文已知上一實例名稱／外觀，新局避開完全照抄；不得越權搜尋未載入舊save。不同實例同一NPC/LOC/PROP/CLUE只是功能槽位，不是同一世界人物、地點或物件改名換皮。玩家可見帳簿、口供與描述一律跟隨本局映射。

## 開場與真相
`<NPC#1>` 發現 `PROP#1` 入庫半日便從內部發熱，出8兩請人查明並安全處理。棚內另有二十餘袋乾草與寄養牲口。

A `damp_stack`：昨日草料過河遇雨，`<NPC#2>` 怕扣工錢，把濕草夾在乾草中壓緊；他只以為會發霉。證據：`PROP#1` 中心濕軟、同批 `CLUE#2` 濕痕、`<NPC#3>` 見過重新綁袋。

B `oil_rag`：馬販伙計誤把擦軸 `PROP#2` 塞進空草袋，`<NPC#2>` 裝袋時未發現。證據：熱點集中 `PROP#2`、其裁口可與馬車剩餘布拼合、`<NPC#2>` 知道空袋曾借出。非本變體決定性線索不存在，GM不得按玩家猜測換答案。

昨日至今完成裝袋；今晨發熱。開場2小時後第二處熱點明顯；3小時後起煙。`<NPC#1>` 誤疑暗火報復，利益是保棚；`<NPC#2>` 在A隱瞞淋雨、B只知借袋；`<NPC#3>` 在A是目擊者、B知道少了油布。

## 場景一：草棚
合理查看自動知道熱從袋內傳出，現場有水缸、木叉、空地。把 `PROP#1` 移到空地拆散並隔離直接停止危機。
- 智力（格物推理）DC10：定位內部熱源；失敗仍知道必須拆袋。
- 智力（醫毒藥理）DC12：A辨濕草悶積；B辨油性殘留；失敗仍排除毒物。
- 直覺（生存）DC10：依 `CLUE#1` 找同批另外兩袋。
- 力量（體能）DC10：單人迅速搬袋；兩人或木叉直接成立；失敗耗時10分鐘，增加協助／木叉後可重試。
未拆袋不能只靠聞味鎖定變體。

## 場景二：批次簿與裝袋角
自動得入庫時間、腳夫班次、袋數。智力（格物推理）DC10：A比對 `CLUE#2`，B找出空袋借用記號。魅力（說服）DC10取完整行程；失敗不抹除帳簿與物證，提出物證後NPC直接補充事實。

## 場景三：渡口／馬車
A只有同源濕痕與重新綁袋目擊；B只有 `PROP#2`、剩餘擦布與裁口拼合。每跨場景30分鐘；先拆散全部熱袋則危機停止。

## 冗餘與偏離
A由濕草、`CLUE#2`、目擊任兩項確認；B由 `PROP#2`、裁口、借袋證詞任兩項確認。NPC死亡／離開不阻斷物證鏈。若玩家主動毀掉全部合理來源，只能安全結案，GM不生成新證據。提前猜中可直接驗證。

## 結局
### `ending_id: fodder_safe_cause_proven`｜顯示：熱源查明｜結算：完整完成
參與排危及至少一段查證，且取得兩項獨立原因來源。歷練：計略1、江湖1（總2）；只排危江湖1；只查證計略1；事後到場0。隊伍實際取得8兩，共同持有、可分割，由玩家分配。公開無濫權：【荊湖道民間】`+0.2×E`、俠名`+0.1×E`。

### `ending_id: fodder_safe_only`｜顯示：先除火患｜結算：部分完成
歷練：江湖1、計略1（總2；未參與調查者只得江湖1）。隊伍實得6兩；2兩查明獎金未取得。公開排危：【荊湖道民間】`+0.1×E`。

### `ending_id: fodder_harmful_handling`｜顯示：驛棚受損｜結算：失敗／持續後果
歷練：江湖1（總1；未參與造成後果者0）。酬勞未取得。無故傷人、勒索或明知可隔離仍放任起火且可靠歸因：惡名`+0.2×E`、【荊湖道民間】`-0.2×E`。

## 重跑上限
每次是另一宗世界事件。只有完整完成令 `repeat_count+1`。同一角色固定銀兩只在前3次完整完成可領；第4次起0兩。隊伍任一角色已達3次，整隊固定酬勞即0，不能輪換刷取。本劇本無武學來源、引薦、永久特殊物品或可刷人情；社會名譽按每次真實可傳播事件結算。

## 出發前整備
一般；24小時（48空檔回合）；荊湖道普通聚落。日常食水、普通工具與住宿可正常取得；任務半日、不跨日，棚內已有水缸、木叉與空地。玩家只知道草料異常發熱。危機從抵達接手後起算。可快速略過逐項確認；出發前逐名固定隨身、坐騎載物、留存位置與攜帶格。
