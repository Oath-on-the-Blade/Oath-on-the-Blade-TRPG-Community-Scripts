# 驛路少了一匹換馬

## 劇本規格
- `script_id: ootb_repeatable_postroad_missing_remount_20260829`
- 劇本版本：1.0.0
- 運行類別：可重覆任務
- `replay_policy: repeatable`
- 連續性：`independent`
- 冒險分類：一般冒險
- 建議等級：6–10 級
- 歷練基準等級：R=8
- 建議人數：3–5 PC
- 劇本規模：小型
- 預計時間：2.5–3.5 小時
- 重跑基型：兩者（日常可再發生＋小型分支）
- 地區：江南道官道沿線的一處本次實例驛站周邊
- 正式相對名譽對象：江南道民間、江南道官府
- 需要引用的內容庫條目：`內容庫/拿手.md`、`內容庫/相對名譽對象.md`

## 任務重跑規則
這不是同一匹馬反覆失蹤。江南道官道驛站每日都有換馬、短途雇車、私商借棚與臨時寄養；不同驛站、不同人員、不同馬匹均可能再次發生「換馬未按時回欄」的小事故。

- 每個新 `game_id` 都代表另一宗世界內事件。
- 全部 NPC 都使用 `<NPC#編號: 姓?-名?>`，新 `game_id` 必須重新生成姓名；同一局保持不變。
- 本任務專用地點：`<LOC#1: 名?-驛>` 為本次驛站，`<LOC#2: 名?-坡>` 為附近緩坡牧地，`<LOC#3: 名?-茶棚>` 為官道旁茶棚。新 `game_id` 全部重新生成專名；三者類型固定，不會因名稱改變機械。
- `<PROP#1>` 為失蹤馬的本次繫具識別物，功能固定為「能把馬與驛站簿冊對上」；形態每局從「編繩頸圈／木牌繫帶／布製鞍尾籤」選一。材質差異不影響取得難度、耐久或證據力度。
- `<CLUE#1>` 為馬離開驛站後留下的路徑痕跡，功能固定為「證明馬先往 `<LOC#2>` 方向，而非直接被牽上官道」；每局從「蹄印夾新草汁／路肩被擦倒的露草／短繩拖過的泥痕」選一，均以直覺（生存）處理。
- 開局在玩家接觸第一條有意義線索前，GM 必須鎖定 `variant_id`：`VAR-A` 或 `VAR-B`，並保存。
- 跨劇本關係：本任務不得作任何其他劇本的必要前置；某次實例的 NPC、地名、物件、結局與變體不得被後作當唯一正史。

### 可重複獎勵與上限
- 基礎歷練：每個新 `game_id` 完整參與都可正常取得，無累積上限；同一 `game_id` 不可重領。
- 銀兩：每個新 `game_id` 可按結局取得，無累積上限；它是每宗新事故的獨立工錢。
- 俠名：同一角色由本 `script_id` 累積增加最多 6 點；達上限後本劇本的正向俠名倍率視為 `0.0x E`。惡名若因實際公開行為成立，無預設免除或負值上限。
- 江南道民間、江南道官府正向相對名譽：同一角色由本 `script_id` 對每個對象累積增加最多 6 點；達上限後該對象正向倍率視為 `0.0x E`。負向結果按實際公開歸因照常結算。
- 本劇本不提供唯一資格、武學、特殊裝備或其他永久唯一獎勵。

## 任務實例化
開局依序記錄：
1. 本次 `game_id`。
2. 生成 `<NPC#1>` 驛丞助手、`<NPC#2>` 馬夫、`<NPC#3>` 送件腳夫、`<NPC#4>` 茶棚照看者的姓名。
3. 生成 `<LOC#1>`、`<LOC#2>`、`<LOC#3>` 專名。
4. 鎖定 `<PROP#1>` 與 `<CLUE#1>` 本輪形態。
5. 隨機或自行選定 `VAR-A`／`VAR-B`；在第一條有意義線索前保存，不得按玩家猜測更改。
6. 記錄每名 PC 本 `script_id` 的俠名／兩個相對名譽正向累積剩餘上限。

## 開場與委託
`<LOC#1>` 清晨點馬時少了一匹原定給急腳換乘的健馬。驛站不能長時間扣住其他公務馬，`<NPC#1>` 請 PC 在兩個時辰內把馬找回，或至少確定馬的去向與是否有人私牽。

酬勞：
- 找回馬且查清本次原因：隊伍 16 兩。
- 找回馬但原因未明：隊伍 10 兩。
- 若馬已受傷但角色避免傷勢惡化並查清原因：隊伍 12 兩。
- 馬匹、繫具與驛站器材均屬驛站，不是戰利品。

## 共通 NPC 功能
### `<NPC#1: 姓?-名?>` 驛丞助手
知道：昨晚最後點馬時仍在；今晨 `<PROP#1>` 也不在欄位。想要：在急腳抵達前解決，不把小事故擴大。限制：無權私自取消公務馬調度，但可讓 PC 查本次馬簿。

### `<NPC#2: 姓?-名?>` 馬夫
知道：昨晚自己最後一次餵水；對繫具是否扣實沒有把握。害怕：被認定監守自盜。初始會淡化自己離欄喝茶的時間。

### `<NPC#3: 姓?-名?>` 送件腳夫
知道：夜裡經過馬欄附近；VAR-A 與 VAR-B 的所見不同。想要：別被扣留耽誤送件。

### `<NPC#4: 姓?-名?>` `<LOC#3>` 照看者
知道：昨夜誰來過茶棚、哪個時段聽到馬嘶。只提供時間線，不知道真相全貌。

## 真相變體
### VAR-A｜鬆扣驚馬
**真相**：`<NPC#2>` 夜裡餵水後沒有把老舊扣環扣到底；遠處驟響使馬掙開，帶著 `<PROP#1>` 先跑向 `<LOC#2>`，後在坡下草溝被短繩纏住。沒有人偷馬。

**短時間線**：
1. `<NPC#2>` 餵水後離欄去 `<LOC#3>` 喝茶。
2. 扣環只卡半格。
3. 半夜官道重車響動，馬驚掙脫。
4. 馬先往牧坡跑，留下 `<CLUE#1>`；之後短繩纏在灌木。
5. 若 PC 不介入，危機 3 時馬再次掙扎擦傷腿；危機 4 時路過車隊發現並把馬暫牽走，查找範圍擴大。

**啟用線索**：
- 馬欄扣環沒有撬痕，只有未完全扣住造成的偏磨。
- `<NPC#3>` 只聽到馬先在欄內急嘶，沒有看到人影牽馬。
- `<CLUE#1>` 從欄外連到 `<LOC#2>`，步距由亂到長，符合驚跑而非被慢牽。

**必要結論**：馬是自行掙脫。至少由扣環磨痕＋路徑痕跡兩源支持；`<NPC#3>` 為第三源。

### VAR-B｜借馬遮掩
**真相**：`<NPC#3>` 因自己送件坐騎傷蹄，怕誤期受罰，趁 `<NPC#2>` 離欄時私牽驛馬走小路試換；到了 `<LOC#2>` 才發現驛馬不熟自己，便把馬暫拴坡下，打算天亮前牽回。途中 `<PROP#1>` 被他拆下藏在 `<LOC#3>` 柴垛附近，想讓人以為馬自己脫逃。

**短時間線**：
1. `<NPC#2>` 離欄喝茶。
2. `<NPC#3>` 私牽驛馬離欄，先走往 `<LOC#2>` 的小路。
3. 他拆下 `<PROP#1>`，回程藏近 `<LOC#3>`。
4. 馬被拴在坡下，繩結太緊；危機 3 時開始磨傷頸側。
5. 若無人介入，危機 4 時 `<NPC#3>` 會趁亂再去放馬，試圖讓它自行回驛。

**啟用線索**：
- 欄門扣環是從外側正常打開，沒有掙脫偏磨。
- `<CLUE#1>` 前段步距平穩，旁邊有一組人足跡；到坡下才出現停步與繫繩痕。
- `<PROP#1>` 在 `<LOC#3>` 附近，且 `<NPC#4>` 記得 `<NPC#3>` 夜裡獨自停留過。

**必要結論**：有人私牽但沒有打算永久偷走。至少由欄門狀態＋人馬並行痕跡兩源支持；`<PROP#1>` 與茶棚時間線為補強。

## 出發前整備
- 整備類型：自訂。
- 安全空檔：1 小時（2 個空檔回合）。
- 開始位置：`<LOC#1>` 前院。
- 世界內原因：兩個時辰後急腳抵達；驛站只願先給一小時讓角色確認裝備、查簿與問明已知路線。
- 可正常取得：普通食水、乾糧、燈油、繩索；可借一盞風燈與一條普通牽馬繩，任務後歸還。
- 已知限制：附近沒有危險野獸通報；坡地晨露濕滑；官道交通會逐漸增加。
- 玩家已知行程：三個主要地點均在步行半時辰內，任務不跨日。
- 危機：安全窗口後，每額外 30 分鐘危機 +1。
- 出發前交接：逐名確認攜帶格、隨身物與借用品；角色擁有但未攜帶的物品不能遠距使用。

## 既有社會名譽
- 江南道官府正向相對名譽：`<NPC#1>` 願直接交本次馬簿供查看；負向者仍可查，但他全程陪同，多耗 10 分鐘。
- 江南道民間正向相對名譽：`<NPC#4>` 願先說出夜間客人時段；負向者需先買茶或由 `<NPC#1>` 作保，核心資訊不被永久封鎖。
- 高俠名可讓路人主動協助看守已找到的馬；高惡名會讓路人保持距離。兩者都不能替代物證。

## 危機時鐘
0. 馬仍失蹤，急腳尚未抵達。
1. 官道開始繁忙，較舊足跡變難分辨，但 `<CLUE#1>` 仍可取得。
2. 急腳先遣人到站催馬，`<NPC#1>` 壓力增加。
3. VAR-A：馬掙扎擦傷腿；VAR-B：過緊繫繩磨傷頸側。
4. VAR-A：路過車隊把馬暫牽走；VAR-B：`<NPC#3>` 試圖放馬自行回驛。任務仍可結案，但「無傷找回」失效。

## 共通場景
### 1. 馬欄
- 自動資訊：少一匹馬；欄門可檢查；餵水桶仍在。
- 智力（格物推理）DC12、5 分鐘：VAR-A 看出扣環偏磨與未扣到底；VAR-B 看出扣環是正常開啟且沒有掙脫痕。失敗只確認「沒有暴力破門」；取得同欄另一扣環作對照後可重試。
- 直覺（感應）DC12、5 分鐘：找到欄外最初方向，接往 `<CLUE#1>`；失敗耗 5 分鐘，改從路肩較少踩踏處查看可重試。

### 2. `<LOC#3>`
- 自動資訊：`<NPC#4>` 記得 `<NPC#2>` 夜裡來喝茶。
- 魅力（說服）DC12、10 分鐘：讓 `<NPC#4>` 願意按時段完整回憶夜裡聲響與客人。失敗後若角色先取得驛站簿上對應時刻，可帶具體時間重新詢問。
- VAR-B：`<PROP#1>` 藏在柴垛近處；合理翻查可自動找到，不鎖在判定後。

### 3. `<LOC#2>` 與坡下
- `<CLUE#1>` 在路肩可取得。直覺（生存）DC12、10 分鐘：VAR-A 判斷馬是快速驚跑；VAR-B 判斷有人與馬同行。失敗仍能沿痕跡找到坡下，但不能單靠此源判斷速度／陪同行走；取得馬的實際位置後可反向比對重試。
- 找到馬後，直覺（馴獸）DC13、10 分鐘可安全接近並解困／解繩。失敗使馬更緊張並多耗 10 分鐘，但不直接造成永久傷害；換一名角色、提供食物或先安靜等候 10 分鐘後可重試。
- 力量（體能）不能取代馴獸去「硬壓」驚馬而無風險；若角色強行壓制，GM 按可行方法判定，至少會提高馬與角色受傷風險。

## 終局與結局
### REP-END-01｜角色卡顯示名稱：換馬歸欄｜完成結局
成立：危機 4 前找回馬並正確判明本輪原因。
- 角色參與：實際參與馬欄／路徑調查及找馬或責任處理之一。
- 物質結果：16 兩實際取得，隊伍共同持有、可分割。
- 社會名譽：俠名 `+0.1x E`；江南道民間 `+0.1x E`；江南道官府 `+0.2x E`；均受本 `script_id` 正向 6 點累積上限。
- 歷練：江湖 1、計略 2（基礎總計 3）。

### REP-END-02｜角色卡顯示名稱：馬先回來｜完成結局
成立：找回馬，但沒有取得足夠證據區分本輪原因。
- 物質結果：10 兩實際取得，隊伍共同持有、可分割。
- 社會名譽：江南道官府 `+0.1x E`，受 6 點上限；其他為 0。
- 歷練：江湖 1、計略 1（基礎總計 2）。

### REP-END-03｜角色卡顯示名稱：傷馬返驛｜完成結局
成立：危機 3 後找回已擦傷／磨傷的馬，角色避免傷勢繼續惡化並完成基本交接。
- 物質結果：12 兩實際取得，隊伍共同持有、可分割。
- 社會名譽：俠名 `+0.1x E`、江南道官府 `+0.1x E`，受上限。
- 歷練：江湖 2、計略 1（基礎總計 3）。

### REP-END-04｜角色卡顯示名稱：驛路另調｜失敗／放棄
成立：角色主動放棄，或危機 4 後仍未找到馬且不再追查。
- 世界後果：驛站另調公務馬，後續自行追查本輪事件。
- 物質結果：無委託金。
- 社會名譽：若角色只是未能完成而仍交回已知資訊，無變化；若明確受託後無故當眾棄任，江南道官府 `-0.1x E`，前提是身份與棄任可可靠歸因。
- 歷練：依實際經歷，最高江湖 1（基礎總計最高 1）。

## VAR-B 責任處理補充
`<NPC#3>` 私牽是違規，但本劇本不要求 PC 必須拘人。把馬與 `<PROP#1>` 交回 `<NPC#1>` 並如實說明證據即可完成。若玩家替 `<NPC#3>` 隱瞞，仍可能找回馬，但因原因未如實交接只能進 REP-END-02；若公開羞辱或毆打不反抗的 NPC，依實際傳播可產生惡名，不能因「抓到犯錯者」免除後果。

## GM 最終核對
- 新 `game_id` 重新生成 NPC、LOC、PROP、CLUE 並先鎖定 variant。
- VAR-A／VAR-B 各自有完整因果、時間線與至少兩個必要結論來源。
- 沒有固定世界 NPC 或某次實例人物跨局延續。
- 所有重複獎勵都明列可否重複與累積上限／無上限。
- 本任務永遠不是其他劇本的必要前置。
