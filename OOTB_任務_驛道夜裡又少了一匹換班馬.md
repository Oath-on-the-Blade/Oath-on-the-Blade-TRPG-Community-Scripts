# 驛道夜裡又少了一匹換班馬

## 劇本規格
- `script_id`: `ootb-repeatable-post-road-missing-relay-horse`
- 劇本版本：1.0.0
- `replay_policy`: `repeatable`
- 連續性：`independent`
- 運行類別：可重覆任務
- 重跑基型：兩者（日常可再發生＋小型分支）
- 冒險分類：一般冒險
- 建議玩家人數：3–5
- 建議角色等級：6–8級
- 歷練基準等級 R：7
- 劇本規模：小型
- 預計遊戲時間：2–3.5 小時
- 武力層級：低至中；預期 0–1 場追逐或短衝突
- 主要地區：每次新 `game_id` 實例化一處河洛道或相鄰官道的 `<LOC#1: 名?-驛>` 與附近 `<LOC#2: 名?-坡/林/渡>`
- 核心衝突一句話摘要：夜班交接時少了一匹換班馬，馬槽鎖未破、繫繩卻完整留下；每次事件都是真實的新一宗失馬。
- 核心任務目標：在四小時內找回或確定換班馬去向，阻止下一班急遞因缺馬中斷，並查明本次事件屬哪一種預寫真相。
- GM 專用真相摘要：新 `game_id` 開始時鎖定 `variant_id`：A 為臨時馬販收買驛卒偷帶馬離驛；B 為驛卒為救受傷親屬擅借驛馬趕往附近醫館。兩者均為可再發生的新事件，不使用上一局人物、地點或證物。
- 出發前整備：一般
- 出發前安全空檔：8 小時（16 個空檔回合）
- 整備開始位置與已知補給條件：本次實例的 `<LOC#1>` 所在聚落或附近安全住處；普通食宿、燈油、繩索、馬料與一般工具可取得
- 危機／期限關係：正式接案後 4 小時；期限到時急遞到站，若無可用換班馬便延誤
- 銀兩與物質獎勵：每個新 `game_id` 整隊 18 兩；同一角色由本 `script_id` 可累積取得銀兩無硬上限，但每局只能按一次結局領取
- 可能的社會名譽變化：俠名、惡名、【河洛道民間】；同一角色由本 `script_id` 正向俠名累積上限 +2，達上限後仍可重跑但不再增加；惡名依實際行為照常記錄、不因重跑免除
- 需要引用的內容庫條目：`內容庫/拿手.md`【格物推理】【洞察】【說服】【感應】

## 新 `game_id` 實例化
在玩家接觸第一條有意義線索前，GM 一次完成並保存：
1. 生成 `<LOC#1: 名?-驛>` 與 `<LOC#2: 名?-坡/林/渡>`；若選坡、林、渡會影響地形，只從本篇對應場景段落使用已預寫差異。
2. 生成所有 NPC 姓名：`<NPC#1: 姓?-名?>` 驛丞、`<NPC#2: 姓?-名?>` 夜班驛卒、`<NPC#3: 姓?-名?>` 鄰班腳夫、`<NPC#4: 姓?-名?>` 本次外部涉事者。
3. 生成本次馬匹識別 `<PROP#1: 毛色?-換班馬>`，至少保存毛色、額／腿一處自然斑記；不得沿用上一 `game_id`。
4. 在 A、B 中隨機或秘密選定一個 `variant_id` 並鎖定。
5. 實例化線索外觀：`<CLUE#1: 蹄印細節?>`、`<CLUE#2: 繩結磨痕?>`、`<CLUE#3: 隨身小物?>`；同一局保持一致。

不同 `game_id` 的同號 NPC、LOC、PROP、CLUE 都是新的世界人物／地點／物件，不承接上一局傷勢、關係、死亡、名聲或物權。不得把任何可重覆任務實例當成其他劇本的必要前置。

## 變體 A：`variant_id: bought-out-night-hand`
### 真相
`<NPC#2>` 欠下一筆小額賭債，被路過的 `<NPC#4>` 馬販以六兩收買，趁夜班交接用備用繩牽走 `<PROP#1>`。原繫繩完整留下，是因 `<NPC#2>` 沒割繩，只解開後重新打回空槽。馬販把馬帶向 `<LOC#2>` 等同伴接走。

必要時間線：子初收買；丑初牽馬；丑正 `<NPC#3>` 看見一人牽馬離驛但以為是例行飲水；寅初點馬發現短缺。

核心利益：`<NPC#2>` 怕失去差事且想清債；`<NPC#4>` 想低價收馬轉賣。兩人都不知道角色何時介入。

變體 A 線索：
- `<CLUE#2>` 顯示原繫繩被解開再重新打回，沒有急拉斷裂；【格物推理】DC 12。
- `<NPC#3>` 看見牽馬者身形與 `<NPC#2>` 相近，並聽到第二人低聲催促。
- `<CLUE#3>` 為 `<NPC#4>` 掉下的馬刷小銅扣或飼料袋角，與 `<LOC#2>` 停留處同類物件吻合。

## 變體 B：`variant_id: borrowed-for-urgent-family`
### 真相
`<NPC#2>` 收到親屬在鄰村重傷的口信，因找不到可租快馬，擅自借用 `<PROP#1>` 趕往醫館，打算天亮前送回。他沒有收錢，也沒有想永久佔有驛馬。`<NPC#4>` 是送信人，留在 `<LOC#2>` 附近等回音。

必要時間線：子正收到口信；丑初牽馬；丑正經 `<LOC#2>` 抄近路；寅初驛站點馬發現短缺。若玩家不介入，第三小時 `<NPC#2>` 已在回程，第四小時末可能趕不上急遞換班。

變體 B 線索：
- `<CLUE#2>` 同樣顯示正常解繩，但結尾打的是 `<NPC#2>` 平日慣用的收尾結；【感應】或【格物推理】DC 12 可比對其工作繩。
- `<NPC#3>` 聽見 `<NPC#2>` 問「最近醫館哪條路快」。
- `<CLUE#3>` 是急寫地址的小紙角或藥鋪包紙，指向附近醫館方向；【洞察】不可直接判定清白，只能支持其急迫性。

## 共通開場
`<LOC#1>` 寅初點馬，少了 `<PROP#1>`。馬槽鎖完好、原繫繩仍掛在柱上，夜間也沒有驚馬聲。`<NPC#1>` 請角色在下一班急遞到站前查明去向；若能找回馬或安排確定替代方案，整隊支付 18 兩。

## 共通必要結論與冗餘
### A：馬匹是被正常牽走，不是自行脫逃
1. 原繫繩完整且重打回空槽，磨痕方向顯示曾被人解開。
2. 驛門外有單人牽馬而非奔逃的蹄印；【感應】DC 12。
3. `<NPC#3>` 的目擊證詞。

### B：本次去向為 `<LOC#2>` 方向
1. 出驛蹄印與 `<PROP#1>` 斑記／蹄鐵缺口一致。
2. `<CLUE#3>` 指向該方向。
3. `<NPC#4>` 或途中路人可給第二條獨立目擊；即使其拒談，蹄印與物件仍成立。

### C：責任性質
每個變體各自至少兩來源：A 為收買痕跡＋馬販物件／口供；B 為醫館去向＋送信人／醫館收治記錄。不同變體的決定性證據不能混用。

## 場景
### `<LOC#1>` 馬槽與值房
- 自動資訊：空槽、完整繩、夜班名冊。
- 【格物推理】DC 12 判讀繩結；失敗仍知道繩未斷。
- 【洞察】DC 13 可發現 `<NPC#2>` 的說法避開「自己何時離開值房」；若 B 且 `<NPC#2>` 已離驛，改為從其凌亂鋪位看出匆忙出門。
- 重試需新證物或新證詞。

### 官道追跡
- 【感應】DC 12 追出牽行蹄印；大雨後提高至 DC 15，但 `<CLUE#3>` 成為保底方向來源。
- 高輕功可縮短追趕時間 30 分鐘，但不能憑空知道正確方向。

### `<LOC#2>`
- 若為「坡」：開闊、可先看見人馬；追逐較易，無額外遮蔽。
- 若為「林」：蹄印在落葉間較淡，【感應】DC +2；角色可利用林間捷徑包抄。
- 若為「渡」：涉事者若已過渡，渡夫成為獨立目擊來源；角色需花 30 分鐘等下一趟小舟或自行合法安排渡河。
- A：`<NPC#4>` 想拖延至同伴接馬；被截住後若退路封死便投降，不為一匹馬拼死。
- B：`<NPC#2>` 正趕回或守在醫館外；說明急遞期限後願立刻返驛，也接受由角色陪同。

## 危機時鐘
- 第 2 小時：`<NPC#1>` 開始尋找鄰驛借馬，成功機會取決於角色是否已傳回方向；若有確定線索，可先派人送訊。
- 第 3 小時：A 的馬販同伴將接近 `<LOC#2>`；B 的 `<NPC#2>` 開始返程。
- 第 4 小時：急遞抵達。若 `<PROP#1>` 已回驛或角色已確定借到替代馬，任務核心危機解除；否則急遞延誤。

## 偏離與失敗來源
- 玩家殺死涉事者：馬匹、蹄印、`<CLUE#3>` 與驛站記錄仍可完成主要判定；死者動機若無第二來源，責任性質只能列未明。
- 玩家直接買一匹新馬：若確實在期限內取得合格替代，可解急遞危機，但失馬去向仍需至少兩來源才算完整查明。
- 玩家錯猜變體：GM 不改 `variant_id`；所有啟用線索必須保持一致。
- 玩家毀掉蹄印：保留物件、記錄、路人／渡夫證詞等替代來源。

## 終局與結算
### `ending_id: horse-returned-cause-proven`｜角色卡顯示：馬歸班續｜結算類型：完成
- 條件：找回或安全控制 `<PROP#1>`，急遞未延誤，且以至少兩條獨立來源查明本次責任性質。
- 完整歷練參與條件：參與至少一個調查／追跡場景及危機解決或最終協商。
- 基礎歷練：調查 2、追跡／挑戰 1、危機處理 2＝5。
- 銀兩：整隊實際取得 18 兩；隊伍共同持有並自行分配。
- 社會名譽：若在 B 中既保住急遞又協助合法補報借馬，俠名 +1，受本 `script_id` 正向累積上限 +2 約束；A 中依法據實處理無固定俠名。若私吞馬匹或明知故放賣馬，惡名 +1。

### `ending_id: relay-saved-cause-unclear`｜角色卡顯示：班續案未清｜結算類型：部分完成
- 條件：急遞靠找回馬或替代馬續行，但責任性質未能可靠證明。
- 參與條件：實質參與危機解決。
- 基礎歷練：危機處理 2、調查 1＝3。
- 銀兩：整隊實際取得 12 兩；6 兩查核尾款未取得。
- 社會名譽：無固定正向變化；若無證據公開誣指，惡名 +1。

### `ending_id: relay-delayed`｜角色卡顯示：急遞誤時｜結算類型：失敗
- 條件：第四小時末無可用換班馬且無有效替代安排。
- 參與條件：接案後參與至少一個主要場景。
- 基礎歷練：調查 1、危機失守 1＝2。
- 銀兩：未取得固定酬勞；若角色已提交可核實失馬去向但未及找回，取得 6 兩查核費。
- 社會名譽：無固定變化；若延誤源自角色故意扣馬勒索，惡名 +1。

### 可重覆持續後果
- 每個 `game_id` 的 NPC 傷亡、地點損害、馬匹物權與地方糾紛只屬該次事件，依正常世界狀態保存，但不得帶進下一 `game_id` 當模板預設。
- 同一角色由本 `script_id` 的正向俠名最多累積 +2；達上限後同類良好表現只作敘事認可。
- 銀兩與歷練每個新 `game_id` 可依實際完成重新取得；同一 `game_id` 不可重領或重算。
- 本任務沒有武學、永久裝備、門派引薦或其他可重複永久獎勵。

中途加入／離開者只取得實際參與所對應 tag，再作個人等級折算。
