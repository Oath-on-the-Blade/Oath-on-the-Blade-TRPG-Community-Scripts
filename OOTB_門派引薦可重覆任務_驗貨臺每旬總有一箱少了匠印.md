# 驗貨臺每旬總有一箱少了匠印

## 劇本規格
- `script_id`: `ootb-tianji-repeatable-missing-maker-mark`
- 劇本版本：1.0.1
- 任務類別：門派引薦可重覆任務（非門派專用任務）
- 運行類別：可重覆任務
- `replay_policy`: `repeatable`
- 連續性：`independent`
- 劇本規模：小型
- 重跑基型：兩者（日常可再發生＋小型分支）
- 冒險分類：一般冒險
- 建議玩家：3–5；建議等級：3–5級；歷練基準 R：4
- 預計3–4小時；武力層級：低
- 地區：雲東道千機峽外地方驗貨路線；每個新 `game_id` 生成一處 `<LOC#1: 名?-驗貨臺>`，不使用山門內固定場所
- 世界知識：天機閣山門在千機峽，理念「器有其主，技有其責」；重要器物重視製作者印記、用途與維修方法
- 引用：`內容庫/拿手.md`【格物推理】【感應】【說服】；`內容庫/敵人圖鑑.md`【惡少打手】；`內容庫/相對名譽對象.md`【天機閣】【雲東道民間】；入門申請依 `遊玩規則/角色/門派系統.md`
- 門派武學獎勵：無；不建立、不提高任何 `[來自〈天機閣〉]` 武學來源，也不教授具名門派武學
- 委託：驗貨臺整隊10兩；引薦是個人可選獎勵，不屬銀兩報價

## 新 `game_id` 實例化
玩家接觸第一條有意義線索前，GM鎖定並保存：
1. `<LOC#1>` 專名；新局重新生成。若合法上下文已知上一實例名稱則避開，不為此搜尋未載入舊存檔。
2. `<NPC#1: 姓?-名?>` 驗貨記錄人、`<NPC#2: 姓?-名?>` 送貨匠戶、`<NPC#3: 姓?-名?>` 臨時搬運人姓名；新局全部重生成，已知上一實例姓名時逐一避開。同一 `game_id` 暫停／續跑保持原姓名；不同局同一編號只是功能槽位。
3. `variant_id`：`V-A_雨蝕補印` 或 `V-B_調箱遮責`。鎖定後不得因玩家猜測或路線切換。
4. `<PROP#1: 名?>` 匠印樣式從「圓框字印／方框圖印／雙線工號印」生成一種；`<PROP#2: 名?>` 普通器件從「水車木軸套／普通滑輪架／閘板銷座」生成一種；`<CLUE#1: 名?>` 封繩／泥痕特徵從「封繩纖維方向／箱底泥色層次／角板壓痕」生成一種。這些只改表象，不改真相、證據力度或DC；新局重生成，同局保留映射及後續持有／破壞／公開狀態。
5. 本次引薦採天機閣外務驗責制度：`<NPC#1>` 依驗貨臺授權記錄表現，送交千機峽外務值房批准；遞交者與批准制度分開。普通搬運人或匠戶無發薦權，無固定世界人物出場。
6. 不讀取其他劇本作前置；上一局人物、地點、器件、傷亡與物證不帶入。官道與驗貨工作持續存在，因此下一個新 `game_id` 可自然發生另一批貨物的新責任爭議。

## 出發前整備
一般整備24小時（48個空檔回合），開始於千機峽外安全市鎮。可取得食水、乾糧、紙筆、燈油、繩索、普通木工與金工小工具；驗貨臺提供量尺、封泥與拓紙，僅供當次驗貨使用，不成玩家私產。玩家知道一批普通器件中有一箱缺少應有匠印，今日內要決定是否放行；器件沒有神異能力。整備期間該箱單獨封存，危機不推進；接受委託後開始。出發前逐名確認攜帶格上限、實際隨身與已用格、坐騎／載物及承載格、留存位置，並確認均未超載。

## 共通開場
`<LOC#1>` 每旬替千機峽外來往匠戶核對普通器件的製作者印記與維修記錄。今日一批貨中，一箱 `<PROP#2>` 封繩完整，箱角卻少了應有 `<PROP#1>`。驗貨記錄人 `<NPC#1>` 要玩家在日落前查清責任與安全邊界。

## 變體 A：`V-A_雨蝕補印`
### 世界真相與時間線
送貨匠戶 `<NPC#2>` 的原匠印昨夜遇雨時被箱角滲水泡壞。`<NPC#2>` 怕整批退貨，叫搬運人 `<NPC#3>` 用臨時木章補印；`<NPC#3>` 不熟印泥比例，補印也脫落，只留淺壓痕。器件本身合格，錯誤在隱瞞損毀並使用未登記臨時章。昨夜酉時遇雨；戌時原印剝落；亥時補印；今晨兩人約定稱「本來就沒印」。日落前若無人查清，整箱因責任不明退回，延誤附近水車維修。

必要結論A1「原印曾存在」：箱角舊印壓痕／包布同色印泥／出貨底冊拓樣，任兩項。A2「器件未調換」：尺寸與底冊一致／墊木受壓順序連續／送貨重量一致，任兩項。V-B的裂件、新削木屑與調箱證物在本變體不存在。

## 變體 B：`V-B_調箱遮責`
### 世界真相與時間線
`<NPC#3>` 搬運時摔裂原箱兩件器件，怕賠工錢，趁雨棚混亂調入另一批同類器件，再削薄原匠印所在箱角。`<NPC#2>` 只知道箱角有新削痕，不知道調換。昨夜戌時摔箱；戌末調件；亥時削角；今晨 `<NPC#2>` 沉默；酉時前 `<NPC#3>` 試圖把裂件從後棚運走。

必要結論B1「箱角人為削去」：新木屑／刨刀寬度與削面／封繩未斷但角板可拆，任兩項。B2「器件被調換」：兩件尺寸與底冊差一分／墊木壓痕不合／後棚裂件有原批次記號，任兩項。V-A的舊印泥包布與臨時木章在本變體不存在。

## 主要 NPC
### `<NPC#1: 姓?-名?>` 驗貨記錄人
天機閣外務體系授權的地方記錄人，凡人，不代表山門教授武學。知道驗貨規程、底冊與引薦送審制度；不知道本次真相。核心利益是讓責任可追、器物可維修。對【天機閣】正相對名譽角色會直接說明過往合作紀錄但仍不免驗；負相對名譽時要求全程雙人見證，不提高DC。若角色查明責任、保全可用器件並留下可追維修記錄，他可把個人表現送外務值房核准引薦。

### `<NPC#2: 姓?-名?>` 送貨匠戶
凡人。V-A是要求補印者；V-B只知箱角異常。展示兩項客觀矛盾後，`魅力（說服）DC11` 可令其完整交代自己知道的部分；失敗使其要求先看底冊或實物，取得新證據後才可重試。

### `<NPC#3: 姓?-名?>` 臨時搬運人
V-A為凡人；V-B使用【惡少打手】但不主動傷人，責任敗露後先逃。V-B若退路被封或低於一半氣血即投降。

## 主要場景
### 一、缺印木箱
合理近看自動看見箱角淺痕或削痕。`智力（格物推理）DC12` 可分辨V-A壓痕保留印章邊界，或V-B木纖維呈新削方向；失敗不改客觀痕跡，只耗半小時。取得拓紙、斜光或拆下可檢角板後才可重試。沒有接觸、量尺或可視木面時，直接判定器件是否同批不能成立。

### 二、底冊與器件核對
底冊提供 `<PROP#1>` 拓樣、器件尺寸與出貨重量。逐件核對需一小時；`智力（格物推理）DC11` 成功可縮短至半小時並取得一項深入差異。V-A尺寸、重量全合；V-B兩件尺寸差一分。失敗仍完成基礎核對，只不能縮短時間；須取得另一名匠戶協助或重新量測工具後才可重試加速判讀。

### 三、雨棚與後棚
V-A可找到舊印泥包布與臨時木章；V-B可找到裂件與新木屑。`直覺（感應）DC12` 可從物件位置判斷近期有人急著藏東西；只指向藏匿，不直接判責。失敗耗半小時；改從底冊對照或取得 `<NPC#2>` 指認後可重試。V-B若酉時前公開封棚，`<NPC#3>` 改從側門離開；玩家可交涉攔下或追逐，無需強制戰鬥。

## 引薦取得與替代獎勵
引薦按角色個別判定。角色須全部符合：實際參與至少一項核心器物／責任判定；未故意偽造證據、把猜測冒充匠責或明知器件危險仍要求放行；並促成可追溯結果——V-A把「器件合格」與「補印違規」分開記錄，V-B把調換件、裂件與責任鏈分開記錄。

符合者若尚未加入天機閣且沒有有效未使用的天機閣引薦，可取得：`〈天機閣〉引薦｜可宣告使用以申請入門｜未使用`。引薦屬該角色本人、具名、不可轉讓／出售／贈與／共享，不保證入門成功，只支援一次正式入門申請，同一角色同時最多持有1份有效天機閣引薦。

已是天機閣門人、已有有效天機閣引薦，或主動不選引薦者，改選個人替代獎勵：由驗貨臺當場實際支付2兩個人酬金，或由驗貨臺直接支付一次普通工具修繕／旅途食宿（上限2兩），兩者擇一；這不是引薦買價。

若先前引薦已使用且入門失敗／失效，角色只能在新的 `game_id` 再次完整參與並符合上述條件後取得新引薦；同一 `game_id` 不得重領。取得引薦本身不使角色成為門人、不通過入門、不給門派配給、不給武學來源或武學解鎖，也不因「拿到引薦」本身額外增加【天機閣】相對名譽。

## 重覆上限
- 歷練：每個新 `game_id` 依實際參與正常結算。
- 隊伍委託：每次整隊10兩；同一角色由本 `script_id` 額外取得的正向個人物質獎勵最多記錄3次。
- 引薦：同時最多1份有效引薦；已使用且失效後可在新 `game_id` 重新爭取，無跨局總次數上限。
- 【天機閣】正相對名譽與俠名：同一角色由本 `script_id` 最多累積2次；之後正倍率視為0。
- 惡名與負相對名譽依每次公開行為結算，不設免責上限。
- 無門派武學來源、門派身份或永久器物。

## 偏離與邊界
- 直接放行：沒有可追責記錄便不能取得引薦。
- 全部退貨：可避免風險；未查明責任只算部分完成。
- 提前猜中：可直接驗證，GM不得切換 `variant_id`。
- 殺死或趕走 `<NPC#2>`／`<NPC#3>`：底冊、箱體與後棚物證仍可完成核心判斷。
- 向普通天機閣門人索引薦：無效；只由預寫外務驗責制度核准。
- 索取天機武學：本篇無此獎勵，`<NPC#1>` 無授藝權。

## 終局與結算
### `ending_id: mark-duty-traced`｜角色卡顯示：匠責可追
條件：查明本次器件狀態與責任鏈，留下可追溯記錄並作安全處置。基礎歷練：調查1、江湖1、計略1，共3點；V-B實際追截搬運人時另戰鬥或藏身1點（按實際方法擇一），最高4點。物質：整隊10兩實際取得；驗貨工具仍屬驗貨臺。符合個人條件者取得引薦或替代獎勵。

在尚未達正向跨局上限且成果由 `<NPC#1>` 送入天機閣外務消息網時：俠名 `+0.1x E`、惡名 `0.0x E`、【天機閣】`+0.2x E`、【雲東道民間】`0.0x E`；達上限後相應正倍率為0。

### `ending_id: mark-safe-no-duty`｜角色卡顯示：貨箱暫封
條件：避免不明器件流出，但未查清責任鏈。基礎歷練：調查1、江湖1，共2點。物質：整隊6兩實際取得。無引薦。俠名、惡名、【天機閣】、【雲東道民間】均 `0.0x E`。

### `ending_id: mark-false-blame`｜角色卡顯示：匠印成疑
條件：錯誤放行調換器件，或無證據公開栽贓匠戶。基礎歷練：江湖1，共1點；無委託酬勞。一般錯誤但未公開栽贓時四項均 `0.0x E`；公開栽贓且驗貨臺有可核實記錄時，俠名 `0.0x E`、惡名 `+0.2x E`、【天機閣】`-0.2x E`、【雲東道民間】`0.0x E`。無引薦。

每次結算保存 `game_id`、`variant_id`、NPC／LOC／PROP／CLUE映射及後續狀態、引薦狀態與本 `script_id` 正向獎勵累積次數。